{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"fpstreams v2","text":"<p>fpstreams builds typed, lazy data pipelines over ordinary Python iterables and async iterables. The v2 API has four focused entry points:</p> Entry point Use it for <code>flow(source)</code> General synchronous transformations and reductions <code>aflow(source)</code> Asynchronous I/O, merging, time-based operators, bounded concurrency <code>rows(source)</code> Records, expressions, joins, grouping, reshape, and data-system I/O <code>pairs(source)</code> Key/value transformations and per-key aggregation <p>The current stable release is <code>2.0.0</code>.</p>"},{"location":"#installation","title":"Installation","text":"<pre><code>pip install fpstreams\n</code></pre> <p>Python 3.11 or newer is required. Optional integrations are installed separately:</p> <pre><code>pip install \"fpstreams[async]\"\npip install \"fpstreams[arrow]\"\npip install \"fpstreams[data]\"\npip install \"fpstreams[polars]\"\n</code></pre>"},{"location":"#your-first-flow","title":"Your first flow","text":"<pre><code>from fpstreams import flow, item\n\nresult = (\n    flow(range(1, 10))\n    .filter(item % 2 == 0)  # Keep even values.\n    .map(item * item)  # Square each remaining value.\n    .take(3)  # Stop after three results.\n    .to_list()\n)\n\nassert result == [4, 16, 36]\n</code></pre> <p>A pipeline has three parts:</p> <ol> <li>A source, such as a list, generator, deferred factory, database cursor, or    async iterator.</li> <li>Zero or more lazy transformations, such as <code>map</code>, <code>filter</code>, <code>window</code>, or    <code>group_by</code>.</li> <li>A terminal operation, such as <code>to_list</code>, <code>count</code>, <code>first</code>, <code>collect</code>, or    <code>aggregate</code>, which executes the plan.</li> </ol> <p>Lists and other reiterable sources can execute repeatedly. Iterators are one-shot. Use <code>flow.defer(factory)</code> when every execution must open a fresh source.</p>"},{"location":"#aggregate-once","title":"Aggregate once","text":"<p>Named aggregations share a single traversal:</p> <pre><code>from fpstreams import agg, flow\n\nsummary = flow([1, 2, 3, 4]).aggregate(\n    count=agg.count(),\n    total=agg.sum(),\n    mean=agg.mean(),\n)\n\nassert summary == {\"count\": 4, \"total\": 10, \"mean\": 2.5}\n</code></pre> <p>Use a <code>Collector</code> when the result is a general container or reduction. Use an <code>Aggregator</code> for composable statistics, especially named and grouped aggregation.</p>"},{"location":"#work-with-records","title":"Work with records","text":"<p>String selectors address record fields. <code>col()</code> builds row expressions without repetitive lambdas.</p> <pre><code>from fpstreams import agg, col, rows\n\norders = [\n    {\"region\": \"eu\", \"status\": \"paid\", \"amount\": 24},\n    {\"region\": \"us\", \"status\": \"paid\", \"amount\": 20},\n    {\"region\": \"eu\", \"status\": \"cancelled\", \"amount\": 99},\n    {\"region\": \"eu\", \"status\": \"paid\", \"amount\": 24},\n]\n\nresult = (\n    rows(orders)\n    .where(col(\"status\") == \"paid\")\n    .group_by(\"region\")\n    .aggregate(\n        orders=agg.count(),\n        revenue=agg.sum(\"amount\"),\n    )\n    .sort_by(\"region\")\n    .to_list()\n)\n\nassert result == [\n    {\"region\": \"eu\", \"orders\": 2, \"revenue\": 48},\n    {\"region\": \"us\", \"orders\": 1, \"revenue\": 20},\n]\n</code></pre> <p><code>Rows</code> can read and write CSV, JSONL, SQLite, DB-API sources, Arrow, Parquet, pandas, and Polars. Optional third-party packages are imported only when those adapters are used.</p>"},{"location":"#run-asynchronous-work-safely","title":"Run asynchronous work safely","text":"<pre><code>import asyncio\n\nfrom fpstreams import aflow\n\n\nasync def request(value: int) -&gt; int:\n    await asyncio.sleep(0.01)\n    return value * 10\n\n\nasync def main() -&gt; None:\n    values = await (\n        aflow([1, 2, 3, 4])\n        .map_async(request, concurrency=2, ordered=True)  # At most two requests run.\n        .timeout(1.0)  # Fail instead of waiting forever.\n        .to_list()\n    )\n    assert values == [10, 20, 30, 40]\n\n\nasyncio.run(main())\n</code></pre> <p>Concurrency is bounded. On completion, short-circuit, timeout, or failure, fpstreams cancels outstanding tasks and closes the owned async iterator.</p>"},{"location":"#understand-execution","title":"Understand execution","text":"<p>The default engine is <code>auto</code>. It selects a fused Python loop, a native Rust kernel, or a hybrid plan based on the source, operations, and requested terminal.</p> <pre><code>from fpstreams import flow, item\n\npipeline = flow([1, 2, 3])\nexplanation = pipeline.explain(terminal=\"count\").to_dict()\n\nassert explanation[\"selected_engine\"] == \"python\"\nassert explanation[\"complexity\"] == \"O(1)\"\nassert explanation[\"data_movement\"][\"copies_source\"] is False\n</code></pre> <p>Call <code>with_engine(\"python\")</code> or <code>with_engine(\"native\")</code> to test a specific engine. A forced native plan fails clearly when it is unsupported; <code>auto</code> can fall back or split the pipeline into stages.</p> <p>Identity lists and tuples stay in Python for materialization, sum, and count so they are not scanned and copied into Rust. An unchanged reiterable source with a known exact size can answer <code>count()</code> without opening the source.</p>"},{"location":"#keep-memory-bounded","title":"Keep memory bounded","text":"<p>Most transformations stream values without materializing the source. Some operations inherently need retained state. v2 exposes bounded alternatives:</p> <ul> <li><code>external_sort(buffer_size=..., tempdir=...)</code> writes sorted runs to temporary files.</li> <li><code>Rows.join(..., partitions=..., tempdir=..., limits=...)</code> partitions large joins.</li> <li><code>Rows.group_by(...).spill(partitions=..., tempdir=..., limits=...)</code> partitions grouping.</li> <li>Buffer-sensitive operations raise <code>BufferLimitError</code> instead of growing without limit.</li> </ul> <p><code>SpillLimits</code> has finite defaults for partition rows and bytes, per-key matches, total output, and repartition depth. Highly skewed or expanding inputs can fail at those limits; \u201cbounded\u201d means bounded by configuration, not guaranteed completion. Temporary resources are cleaned up on normal completion, errors, limit failures, and early exit.</p> <p>CSV is written raw by default. Enable <code>spreadsheet_safe=True</code> for untrusted text that will be opened in spreadsheet software. JSONL input has an 8 MiB per-record default; <code>max_record_bytes=None</code> disables it for trusted input.</p>"},{"location":"#moving-from-v1","title":"Moving from v1","text":"<p><code>Stream</code>, <code>AsyncStream</code>, and <code>ParallelStream</code> remain import aliases to ease migration, but new code should use <code>flow</code>, <code>aflow</code>, <code>rows</code>, and <code>pairs</code>.</p> <p>The old <code>core</code> and standalone <code>ParallelStream</code> implementations were removed. The import alias and <code>Flow.parallel()</code> compatibility strategy remain. New code can use <code>parallel_map()</code> for thread or process mapping and <code>map_async()</code> for async work. Because v2 changes terminal and error semantics, test existing pipelines before upgrading production code.</p>"},{"location":"roadmap/","title":"v2 status and roadmap","text":"<p>fpstreams 2 replaces the v1 implementation. The current stable version is <code>2.0.0</code>.</p>"},{"location":"roadmap/#included-in-20","title":"Included in 2.0","text":"<ul> <li>Domain-oriented package layout with small compatibility facades.</li> <li>Lazy <code>Flow</code>, <code>AsyncFlow</code>, <code>Rows</code>, and <code>Pairs</code> APIs.</li> <li>Placeholder and row expression systems.</li> <li>Collectors, named aggregators, and grouped aggregation.</li> <li>Fused synchronous and asynchronous Python execution.</li> <li>Native Rust numeric plans plus automatic hybrid planning.</li> <li>Bounded concurrent async mapping, merge operations, timeouts, debounce, and   time-windowed buffering.</li> <li>CSV, JSONL, SQLite, DB-API, Arrow, Parquet, pandas, and Polars adapters.</li> <li>External sorting and partitioned spill paths for joins and grouping.</li> <li>One-shot source enforcement and deterministic resource cleanup.</li> <li>Strict typing, Python/Rust parity tests, and wheel/sdist packaging.</li> </ul>"},{"location":"roadmap/#20-stabilization-completed","title":"2.0 stabilization completed","text":"<ul> <li>Terminal-aware execution explanations and exact-size count routing.</li> <li>Cached flat scalar-expression evaluators and linear native-prefix planning.</li> <li>Single registries for synchronous and asynchronous operation dispatch.</li> <li>Skew-aware spill repartitioning with finite partition, match, state, and output limits.</li> <li>Spreadsheet-safe CSV as an opt-in mode and bounded JSONL records by default.</li> <li>Patched development dependencies, SHA-pinned Actions, automated dependency updates,   verified release artifacts, and SHA-256 manifests.</li> <li>Machine-readable release benchmarks and branch-coverage gates for high-risk modules.</li> </ul>"},{"location":"roadmap/#stability-commitments","title":"Stability commitments","text":""},{"location":"roadmap/#freeze-public-behavior","title":"Freeze public behavior","text":"<p>Public names, signatures, exceptions, source-consumption rules, and engine fallback behavior are stable within the v2 release line. Breaking changes belong in a future major release. v2 does not add an alias for every v1 method.</p>"},{"location":"roadmap/#add-native-operations-only-with-parity-tests","title":"Add native operations only with parity tests","text":"<p>Add native operations only when they preserve Python ordering, equality, overflow, error, and cleanup semantics. Every new kernel needs Python/native parity tests and an explicit unsupported path.</p>"},{"location":"roadmap/#keep-spill-behavior-inspectable","title":"Keep spill behavior inspectable","text":"<p>Keep spill diagnostics, partition selection, and resource limits visible for external sorts, joins, and grouped aggregation. Materializing operations should remain obvious in API documentation and plan explanations.</p>"},{"location":"roadmap/#keep-adapters-current","title":"Keep adapters current","text":"<p>Keep Arrow as the preferred columnar interchange path and validate adapter behavior across supported pandas, PyArrow, and Polars releases. Third-party data packages remain optional dependencies.</p>"},{"location":"roadmap/#possible-work-after-20","title":"Possible work after 2.0","text":"<ul> <li>Additional streaming joins and merge operations for already-sorted inputs.</li> <li>More approximate or bounded statistical aggregators.</li> <li>Richer plan diagnostics and structured execution metrics.</li> <li>Additional <code>Option</code>/<code>Result</code> traversal helpers.</li> <li>Narrow extension hooks for custom sources, aggregators, and native kernels.</li> </ul> <p>These ideas stay out of 2.0 unless they can be added without weakening API consistency, memory bounds, or Python/Rust parity.</p> <p>Research directions such as bounded/unbounded capability typing, mergeable aggregator algebra, incremental Rows, Arrow/Substrait pushdown, async cancel-scope redesign, and free-threaded Python support remain future work. The 2026-08-14 stabilization audit did not implement or claim completion of those items.</p>"},{"location":"roadmap/#what-fpstreams-will-not-do","title":"What fpstreams will not do","text":"<p>fpstreams will not replace NumPy, pandas, Polars, or a distributed query engine. It is a pipeline layer that passes data to those systems when appropriate. Ordinary transforms are not distributed automatically, and unbounded inputs are not silently materialized.</p>"},{"location":"roadmap/#release-validation","title":"Release validation","text":"<p>The v2 release gate covers:</p> <ul> <li>no known Python/native semantic divergence in supported plans;</li> <li>clean tests, lint, strict typing, Rust formatting, clippy, and package builds;</li> <li>documented behavior for one-shot sources, cancellation, spilling, and fallbacks;</li> <li>clean installation and native/Python smoke tests for every wheel and the sdist;</li> <li>repository and focus-module branch-coverage thresholds;</li> <li>SHA-pinned CI actions, minimal publish credentials, and artifact hash manifests;</li> <li>a migration path for supported v1 entry-point aliases.</li> </ul>"},{"location":"reference/async_flow/","title":"AsyncFlow","text":"<p><code>AsyncFlow[T]</code> combines async iteration with bounded concurrent mapping, merging, rate control, latest-request switching, timeouts, and buffer-by-time operations.</p> <p>Use <code>aflow(source)</code> with an iterable or async iterable.</p>"},{"location":"reference/async_flow/#creating-an-async-flow","title":"Creating an async flow","text":"Call Behavior <code>aflow(source)</code> Wrap a synchronous or asynchronous iterable <code>aflow.defer(factory)</code> Call the source factory for each execution <code>aflow.from_file(path)</code> Read text lines without blocking the event loop <code>aflow.interval(seconds)</code> Emit increasing integers on a timer <code>aflow.paginate(fetch_page)</code> Fetch pages until the returned cursor is <code>None</code>"},{"location":"reference/async_flow/#control-request-rates","title":"Control request rates","text":"<p>Place <code>throttle</code> before concurrent work when an API accepts only a fixed number of requests per time window. The first window may start with a burst; later items wait without filling a background queue.</p> <pre><code>results = await (\n    aflow(requests)\n    .throttle(5, per=1.0)  # Start at most five requests per second.\n    .map_async(send_request, concurrency=3)  # Keep at most three requests in flight.\n    .to_list()\n)\n</code></pre> <p>Use <code>spaceout(seconds)</code> when every pair of emissions needs a minimum gap. <code>delay(seconds)</code> waits once, before the first upstream item is requested.</p>"},{"location":"reference/async_flow/#keep-only-the-latest-work","title":"Keep only the latest work","text":"<p><code>switch_map</code> is useful for search boxes, live filters, and other inputs where a newer value makes older work irrelevant. When a new outer item arrives, fpstreams cancels and closes the previous inner source. The last inner source can finish after the outer source ends.</p> <pre><code>matches = await (\n    aflow(query_changes)\n    .switch_map(search_pages)  # Cancel the previous search when the query changes.\n    .to_list()\n)\n</code></pre> <p><code>search_pages</code> may return an iterable, an async iterable, or an awaitable containing either one.</p>"},{"location":"reference/async_flow/#methods","title":"Methods","text":""},{"location":"reference/async_flow/#fpstreams.AsyncFlow","title":"fpstreams.AsyncFlow","text":"<p>               Bases: <code>AsyncFlowTerminalsMixin[T]</code>, <code>Generic[T]</code></p> <p>A lazy pipeline over synchronous or asynchronous iterable sources.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.to_list","title":"to_list  <code>async</code>","text":"<pre><code>to_list() -&gt; list[T]\n</code></pre> <p>Consume the async flow and collect its items in a list.</p> <p>Returns:</p> Type Description <code>list[T]</code> <p>A list containing the consumed results in encounter order.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.to_tuple","title":"to_tuple  <code>async</code>","text":"<pre><code>to_tuple() -&gt; tuple[T, ...]\n</code></pre> <p>Execute the async pipeline and collect its items in a tuple.</p> <p>Returns:</p> Type Description <code>tuple[T, ...]</code> <p>A tuple containing the resulting values.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.to_set","title":"to_set  <code>async</code>","text":"<pre><code>to_set() -&gt; set[T]\n</code></pre> <p>Execute the async pipeline and collect distinct hashable items.</p> <p>Returns:</p> Type Description <code>set[T]</code> <p>A set containing the distinct resulting values.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.join","title":"join  <code>async</code>","text":"<pre><code>join(separator: str = '') -&gt; str\n</code></pre> <p>Convert items to strings and join them with separator.</p> <p>This is a string terminal operation; it consumes the flow and does not perform a relational join.</p> <p>Parameters:</p> Name Type Description Default <code>separator</code> <code>str</code> <p>The string inserted between adjacent string representations.</p> <code>''</code> <p>Returns:</p> Type Description <code>str</code> <p>One string containing every item separated by <code>separator</code>.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.partition","title":"partition  <code>async</code>","text":"<pre><code>partition(\n    predicate: Callable[[T], bool | Awaitable[bool]],\n) -&gt; tuple[list[T], list[T]]\n</code></pre> <p>Collect matching and non-matching items in separate lists.</p> <p>Parameters:</p> Name Type Description Default <code>predicate</code> <code>Callable[[T], bool | Awaitable[bool]]</code> <p>A callable that decides whether an item matches.</p> required <p>Returns:</p> Type Description <code>tuple[list[T], list[T]]</code> <p>A tuple containing the resulting values.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.partition_results","title":"partition_results  <code>async</code>","text":"<pre><code>partition_results() -&gt; tuple[list[Any], list[Exception]]\n</code></pre> <p>Separate Result values into successes and failures.</p> <p>Returns:</p> Type Description <code>tuple[list[Any], list[Exception]]</code> <p>A tuple containing the resulting values.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.first","title":"first  <code>async</code>","text":"<pre><code>first(default: Any = _MISSING) -&gt; T | Any\n</code></pre> <p>Return the first item and close the async source immediately.</p> <p>Parameters:</p> Name Type Description Default <code>default</code> <code>Any</code> <p>The value returned when no matching item is available.</p> <code>_MISSING</code> <p>Returns:</p> Type Description <code>T | Any</code> <p>The first item, or <code>default</code> when the flow is empty.</p> <p>Raises:</p> Type Description <code>EmptyFlowError</code> <p>If the flow is empty and no default is supplied.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.last","title":"last  <code>async</code>","text":"<pre><code>last(default: Any = _MISSING) -&gt; T | Any\n</code></pre> <p>Return the last item, or default when the flow is empty.</p> <p>Parameters:</p> Name Type Description Default <code>default</code> <code>Any</code> <p>The value returned when no matching item is available.</p> <code>_MISSING</code> <p>Returns:</p> Type Description <code>T | Any</code> <p>The last item, or <code>default</code> when the flow is empty.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.find","title":"find  <code>async</code>","text":"<pre><code>find(\n    predicate: Callable[[T], bool | Awaitable[bool]],\n    default: Any = _MISSING,\n) -&gt; T | Any\n</code></pre> <p>Return the first matching item, a default, or raise EmptyFlowError.</p> <p>Parameters:</p> Name Type Description Default <code>predicate</code> <code>Callable[[T], bool | Awaitable[bool]]</code> <p>A callable that decides whether an item matches.</p> required <code>default</code> <code>Any</code> <p>The value returned when no matching item is available.</p> <code>_MISSING</code> <p>Returns:</p> Type Description <code>T | Any</code> <p>The first matching item, or <code>default</code> when no item matches.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.find_index","title":"find_index  <code>async</code>","text":"<pre><code>find_index(\n    predicate: Callable[[T], bool | Awaitable[bool]],\n) -&gt; int | None\n</code></pre> <p>Return the index of the first matching item, or None.</p> <p>Parameters:</p> Name Type Description Default <code>predicate</code> <code>Callable[[T], bool | Awaitable[bool]]</code> <p>A callable that decides whether an item matches.</p> required <p>Returns:</p> Type Description <code>int | None</code> <p>The matching or computed value, or <code>None</code> when unavailable.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.index_of","title":"index_of  <code>async</code>","text":"<pre><code>index_of(value: T) -&gt; int | None\n</code></pre> <p>Return the index of the first equal value, or None.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>T</code> <p>The value consumed by this operation.</p> required <p>Returns:</p> Type Description <code>int | None</code> <p>The matching or computed value, or <code>None</code> when unavailable.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.nth","title":"nth  <code>async</code>","text":"<pre><code>nth(index: int, default: Any = _MISSING) -&gt; T | Any\n</code></pre> <p>Return the item at a positive or negative index.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>int</code> <p>The zero-based item or field position to select.</p> required <code>default</code> <code>Any</code> <p>The value returned when no matching item is available.</p> <code>_MISSING</code> <p>Returns:</p> Type Description <code>T | Any</code> <p>The selected item, or <code>default</code> when the index is out of range.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.count","title":"count  <code>async</code>","text":"<pre><code>count() -&gt; int\n</code></pre> <p>Count all items produced by the async flow.</p> <p>Returns:</p> Type Description <code>int</code> <p>The number of matching input items.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.collect","title":"collect  <code>async</code>","text":"<pre><code>collect(\n    collector: Callable[[Iterable[T]], C | Awaitable[C]]\n    | Collector[T, Any, C]\n    | None = None,\n    /,\n    **collectors: Collector[T, Any, Any],\n) -&gt; C | dict[str, Any]\n</code></pre> <p>Reduce the flow with one Collector or named Collectors.</p> <p>One collector returns its finished value. Named collectors share one async traversal and return a dictionary.</p> <p>Parameters:</p> Name Type Description Default <code>collector</code> <code>Callable[[Iterable[T]], C | Awaitable[C]] | Collector[T, Any, C] | None</code> <p>The collector used to reduce input items.</p> <code>None</code> <code>**collectors</code> <code>Collector[T, Any, Any]</code> <p>Named collectors evaluated during the same traversal.</p> <code>{}</code> <p>Returns:</p> Type Description <code>C | dict[str, Any]</code> <p>The collector result, or a dictionary of results for named collectors.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.aggregate","title":"aggregate  <code>async</code>","text":"<pre><code>aggregate(**aggregations: Aggregator) -&gt; dict[str, Any]\n</code></pre> <p>Compute several named aggregations while traversing the async flow once.</p> <p>All named aggregators are updated during the same asynchronous traversal.</p> <p>Parameters:</p> Name Type Description Default <code>**aggregations</code> <code>Aggregator</code> <p>Named aggregators evaluated during the same traversal.</p> <code>{}</code> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>A dictionary containing the computed keys and values.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.mean","title":"mean  <code>async</code>","text":"<pre><code>mean() -&gt; float | None\n</code></pre> <p>Return the arithmetic mean, or None for an empty flow.</p> <p>Returns:</p> Type Description <code>float | None</code> <p>The matching or computed value, or <code>None</code> when unavailable.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.variance","title":"variance  <code>async</code>","text":"<pre><code>variance(*, ddof: int = 1) -&gt; float | None\n</code></pre> <p>Return the variance, or None when too few values are available.</p> <p>Parameters:</p> Name Type Description Default <code>ddof</code> <code>int</code> <p>Delta degrees of freedom used in the variance divisor.</p> <code>1</code> <p>Returns:</p> Type Description <code>float | None</code> <p>The matching or computed value, or <code>None</code> when unavailable.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.std","title":"std  <code>async</code>","text":"<pre><code>std(*, ddof: int = 1) -&gt; float | None\n</code></pre> <p>Return the standard deviation, or None when too few values are available.</p> <p>Parameters:</p> Name Type Description Default <code>ddof</code> <code>int</code> <p>Delta degrees of freedom used in the variance divisor.</p> <code>1</code> <p>Returns:</p> Type Description <code>float | None</code> <p>The matching or computed value, or <code>None</code> when unavailable.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.reduce","title":"reduce  <code>async</code>","text":"<pre><code>reduce(\n    function: Callable[[Any, T], Any | Awaitable[Any]],\n    initial: Any = _MISSING,\n) -&gt; Any\n</code></pre> <p>Combine items from left to right with an optional initial value.</p> <p>Parameters:</p> Name Type Description Default <code>function</code> <code>Callable[[Any, T], Any | Awaitable[Any]]</code> <p>The callable applied by this operation.</p> required <code>initial</code> <code>Any</code> <p>The initial accumulator value. When omitted, the first item is used where supported.</p> <code>_MISSING</code> <p>Returns:</p> Type Description <code>Any</code> <p>The final left-to-right accumulator.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.reduce_right","title":"reduce_right  <code>async</code>","text":"<pre><code>reduce_right(\n    function: Callable[[T, Any], Any | Awaitable[Any]],\n    initial: Any = _MISSING,\n    *,\n    max_items: int | None = None,\n) -&gt; Any\n</code></pre> <p>Combine buffered items from right to left.</p> <p>Parameters:</p> Name Type Description Default <code>function</code> <code>Callable[[T, Any], Any | Awaitable[Any]]</code> <p>The callable applied by this operation.</p> required <code>initial</code> <code>Any</code> <p>The initial accumulator value. When omitted, the first item is used where supported.</p> <code>_MISSING</code> <code>max_items</code> <code>int | None</code> <p>The maximum number of source items allowed in the right-side buffer.</p> <code>None</code> <p>Returns:</p> Type Description <code>Any</code> <p>The final right-to-left accumulator.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.reduce_by","title":"reduce_by  <code>async</code>","text":"<pre><code>reduce_by(\n    key: Selector,\n    function: Callable[[R, T], R | Awaitable[R]],\n    *,\n    initializer: Callable[[], R | Awaitable[R]],\n) -&gt; dict[Any, R]\n</code></pre> <p>Reduce items independently for each selected key.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>Selector</code> <p>The callable or selector used to derive a key.</p> required <code>function</code> <code>Callable[[R, T], R | Awaitable[R]]</code> <p>The callable applied by this operation.</p> required <code>initializer</code> <code>Callable[[], R | Awaitable[R]]</code> <p>A zero-argument callable that creates fresh mutable state.</p> required <p>Returns:</p> Type Description <code>dict[Any, R]</code> <p>A dictionary containing the computed keys and values.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.frequencies","title":"frequencies  <code>async</code>","text":"<pre><code>frequencies(key: Selector | None = None) -&gt; dict[Any, int]\n</code></pre> <p>Count occurrences of values or selected keys.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>Selector | None</code> <p>The callable or selector used to derive a key.</p> <code>None</code> <p>Returns:</p> Type Description <code>dict[Any, int]</code> <p>A dictionary containing the computed keys and values.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.any","title":"any  <code>async</code>","text":"<pre><code>any(\n    predicate: Callable[[T], bool | Awaitable[bool]] = bool,\n) -&gt; bool\n</code></pre> <p>Return whether at least one item satisfies the predicate.</p> <p>Parameters:</p> Name Type Description Default <code>predicate</code> <code>Callable[[T], bool | Awaitable[bool]]</code> <p>A callable that decides whether an item matches.</p> <code>bool</code> <p>Returns:</p> Type Description <code>bool</code> <p>Whether the condition described above is true.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.all","title":"all  <code>async</code>","text":"<pre><code>all(\n    predicate: Callable[[T], bool | Awaitable[bool]] = bool,\n) -&gt; bool\n</code></pre> <p>Return whether every item satisfies the predicate.</p> <p>Parameters:</p> Name Type Description Default <code>predicate</code> <code>Callable[[T], bool | Awaitable[bool]]</code> <p>A callable that decides whether an item matches.</p> <code>bool</code> <p>Returns:</p> Type Description <code>bool</code> <p>Whether the condition described above is true.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.none","title":"none  <code>async</code>","text":"<pre><code>none(\n    predicate: Callable[[T], bool | Awaitable[bool]] = bool,\n) -&gt; bool\n</code></pre> <p>Return whether no item satisfies predicate.</p> <p>Parameters:</p> Name Type Description Default <code>predicate</code> <code>Callable[[T], bool | Awaitable[bool]]</code> <p>A callable that decides whether an item matches.</p> <code>bool</code> <p>Returns:</p> Type Description <code>bool</code> <p>Whether the condition described above is true.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.for_each","title":"for_each  <code>async</code>","text":"<pre><code>for_each(action: Callable[[T], Any]) -&gt; None\n</code></pre> <p>Run an action for every item and return after completion.</p> <p>Parameters:</p> Name Type Description Default <code>action</code> <code>Callable[[T], Any]</code> <p>The side-effecting callable invoked for each matching item.</p> required"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.of","title":"of  <code>staticmethod</code>","text":"<pre><code>of(*items: R) -&gt; AsyncFlow[R]\n</code></pre> <p>Create an async flow from positional items.</p> <p>Parameters:</p> Name Type Description Default <code>*items</code> <code>R</code> <p>Items emitted by the new pipeline.</p> <code>()</code> <p>Returns:</p> Type Description <code>AsyncFlow[R]</code> <p>A new lazy <code>AsyncFlow</code>.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.from_iterable","title":"from_iterable  <code>staticmethod</code>","text":"<pre><code>from_iterable(source: Iterable[R]) -&gt; AsyncFlow[R]\n</code></pre> <p>Create an async flow over a synchronous iterable.</p> <p>Parameters:</p> Name Type Description Default <code>source</code> <code>Iterable[R]</code> <p>The iterable, async iterable, or data source to read lazily.</p> required <p>Returns:</p> Type Description <code>AsyncFlow[R]</code> <p>A new lazy <code>AsyncFlow</code>.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.from_aiterable","title":"from_aiterable  <code>staticmethod</code>","text":"<pre><code>from_aiterable(source: AsyncIterable[R]) -&gt; AsyncFlow[R]\n</code></pre> <p>Create an async flow over an asynchronous iterable.</p> <p>Parameters:</p> Name Type Description Default <code>source</code> <code>AsyncIterable[R]</code> <p>The iterable, async iterable, or data source to read lazily.</p> required <p>Returns:</p> Type Description <code>AsyncFlow[R]</code> <p>A new lazy <code>AsyncFlow</code>.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.from_file","title":"from_file  <code>staticmethod</code>","text":"<pre><code>from_file(\n    path: str | PathLike[str], *, encoding: str = \"utf-8\"\n) -&gt; AsyncFlow[str]\n</code></pre> <p>Read a text file asynchronously and emit lines without trailing newlines.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str | PathLike[str]</code> <p>The filesystem path to read from or write to.</p> required <code>encoding</code> <code>str</code> <p>The text encoding used to open the file.</p> <code>'utf-8'</code> <p>Returns:</p> Type Description <code>AsyncFlow[str]</code> <p>A new lazy <code>AsyncFlow</code>.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.interval","title":"interval  <code>staticmethod</code>","text":"<pre><code>interval(seconds: float) -&gt; AsyncFlow[int]\n</code></pre> <p>Emit increasing integers separated by seconds.</p> <p>Parameters:</p> Name Type Description Default <code>seconds</code> <code>float</code> <p>The duration in seconds used by the timing operation.</p> required <p>Returns:</p> Type Description <code>AsyncFlow[int]</code> <p>A new lazy <code>AsyncFlow</code>.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.paginate","title":"paginate  <code>staticmethod</code>","text":"<pre><code>paginate(\n    fetch_page: Callable[\n        [C | None],\n        tuple[AsyncIterable[R] | Iterable[R], C | None]\n        | Awaitable[\n            tuple[AsyncIterable[R] | Iterable[R], C | None]\n        ],\n    ],\n    *,\n    start: C | None = None,\n) -&gt; AsyncFlow[R]\n</code></pre> <p>Fetch pages lazily until the returned cursor is None.</p> <p>Parameters:</p> Name Type Description Default <code>fetch_page</code> <code>Callable[[C | None], tuple[AsyncIterable[R] | Iterable[R], C | None] | Awaitable[tuple[AsyncIterable[R] | Iterable[R], C | None]]]</code> <p>An async callable that returns page items and the next cursor.</p> required <code>start</code> <code>C | None</code> <p>The first index, numeric value, or additive identity to use.</p> <code>None</code> <p>Returns:</p> Type Description <code>AsyncFlow[R]</code> <p>A new lazy <code>AsyncFlow</code>.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.map_async","title":"map_async","text":"<pre><code>map_async(\n    function: Callable[[T], R | Awaitable[R]],\n    *,\n    concurrency: int = 8,\n    ordered: bool = True,\n    timeout: float | None = None,\n) -&gt; AsyncFlow[R]\n</code></pre> <p>Map items with bounded concurrency and optional ordering and timeout.</p> <p>Concurrency is bounded. Ordered mode delays later completed results until earlier inputs finish.</p> <p>Parameters:</p> Name Type Description Default <code>function</code> <code>Callable[[T], R | Awaitable[R]]</code> <p>The callable applied by this operation.</p> required <code>concurrency</code> <code>int</code> <p>The maximum number of operations or inner sources active at once.</p> <code>8</code> <code>ordered</code> <code>bool</code> <p>Whether results must preserve source encounter order.</p> <code>True</code> <code>timeout</code> <code>float | None</code> <p>The optional maximum duration in seconds before the operation fails.</p> <code>None</code> <p>Returns:</p> Type Description <code>AsyncFlow[R]</code> <p>A new lazy <code>AsyncFlow</code> representing this operation.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If concurrency is less than one.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.map","title":"map","text":"<pre><code>map(\n    function: Callable[[T], R | Awaitable[R]],\n) -&gt; AsyncFlow[R]\n</code></pre> <p>Apply a synchronous or asynchronous function to each item in order.</p> <p><code>map</code> preserves encounter order and awaits awaitable results; work starts only when the flow is consumed.</p> <p>Parameters:</p> Name Type Description Default <code>function</code> <code>Callable[[T], R | Awaitable[R]]</code> <p>The callable applied by this operation.</p> required <p>Returns:</p> Type Description <code>AsyncFlow[R]</code> <p>A new lazy <code>AsyncFlow</code> representing this operation.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.filter","title":"filter","text":"<pre><code>filter(\n    predicate: Callable[[T], bool | Awaitable[bool]],\n) -&gt; AsyncFlow[T]\n</code></pre> <p>Keep items for which the sync or async predicate is truthy.</p> <p>The predicate may be synchronous or asynchronous and is evaluated in encounter order.</p> <p>Parameters:</p> Name Type Description Default <code>predicate</code> <code>Callable[[T], bool | Awaitable[bool]]</code> <p>A callable that decides whether an item matches.</p> required <p>Returns:</p> Type Description <code>AsyncFlow[T]</code> <p>A new lazy <code>AsyncFlow</code> representing this operation.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.where","title":"where","text":"<pre><code>where(\n    predicate: Callable[[T], bool | Awaitable[bool]],\n) -&gt; AsyncFlow[T]\n</code></pre> <p>Keep items for which predicate returns a truthy value.</p> <p>This is an alias-style filtering operation for a synchronous or asynchronous predicate.</p> <p>Parameters:</p> Name Type Description Default <code>predicate</code> <code>Callable[[T], bool | Awaitable[bool]]</code> <p>A callable that decides whether an item matches.</p> required <p>Returns:</p> Type Description <code>AsyncFlow[T]</code> <p>A new lazy <code>AsyncFlow</code> representing this operation.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.reject","title":"reject","text":"<pre><code>reject(\n    predicate: Callable[[T], bool | Awaitable[bool]],\n) -&gt; AsyncFlow[T]\n</code></pre> <p>Drop items for which the sync or async predicate is truthy.</p> <p>Parameters:</p> Name Type Description Default <code>predicate</code> <code>Callable[[T], bool | Awaitable[bool]]</code> <p>A callable that decides whether an item matches.</p> required <p>Returns:</p> Type Description <code>AsyncFlow[T]</code> <p>A new lazy <code>AsyncFlow</code> representing this operation.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.tap","title":"tap","text":"<pre><code>tap(action: Callable[[T], Any]) -&gt; AsyncFlow[T]\n</code></pre> <p>Run a sync or async side effect while passing each item through.</p> <p>Parameters:</p> Name Type Description Default <code>action</code> <code>Callable[[T], Any]</code> <p>The side-effecting callable invoked for each matching item.</p> required <p>Returns:</p> Type Description <code>AsyncFlow[T]</code> <p>A new lazy <code>AsyncFlow</code> representing this operation.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.flat_map","title":"flat_map","text":"<pre><code>flat_map(\n    function: Callable[\n        [T],\n        AsyncIterable[R]\n        | Iterable[R]\n        | Awaitable[AsyncIterable[R] | Iterable[R]],\n    ],\n) -&gt; AsyncFlow[R]\n</code></pre> <p>Map each item to an iterable and emit its contents.</p> <p>The mapper may be synchronous or asynchronous; each returned iterable is flattened in encounter order.</p> <p>Parameters:</p> Name Type Description Default <code>function</code> <code>Callable[[T], AsyncIterable[R] | Iterable[R] | Awaitable[AsyncIterable[R] | Iterable[R]]]</code> <p>The callable applied by this operation.</p> required <p>Returns:</p> Type Description <code>AsyncFlow[R]</code> <p>A new lazy <code>AsyncFlow</code> representing this operation.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.merge","title":"merge","text":"<pre><code>merge(\n    *others: AsyncIterable[T] | Iterable[T],\n) -&gt; AsyncFlow[T]\n</code></pre> <p>Merge this flow with other sources in completion order.</p> <p>Parameters:</p> Name Type Description Default <code>*others</code> <code>AsyncIterable[T] | Iterable[T]</code> <p>Additional sources combined with this pipeline.</p> <code>()</code> <p>Returns:</p> Type Description <code>AsyncFlow[T]</code> <p>A new lazy <code>AsyncFlow</code> representing this operation.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.combine_latest","title":"combine_latest","text":"<pre><code>combine_latest(\n    *others: AsyncIterable[Any] | Iterable[Any],\n) -&gt; AsyncFlow[tuple[Any, ...]]\n</code></pre> <p>Emit the latest value from every source after all have produced once.</p> <p>Parameters:</p> Name Type Description Default <code>*others</code> <code>AsyncIterable[Any] | Iterable[Any]</code> <p>Additional sources combined with this pipeline.</p> <code>()</code> <p>Returns:</p> Type Description <code>AsyncFlow[tuple[Any, ...]]</code> <p>A new lazy <code>AsyncFlow</code> representing this operation.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.merge_map","title":"merge_map","text":"<pre><code>merge_map(\n    function: Callable[\n        [T],\n        AsyncIterable[R]\n        | Iterable[R]\n        | Awaitable[AsyncIterable[R] | Iterable[R]],\n    ],\n    *,\n    concurrency: int = 8,\n) -&gt; AsyncFlow[R]\n</code></pre> <p>Map items to inner sources and merge them with bounded concurrency.</p> <p>Parameters:</p> Name Type Description Default <code>function</code> <code>Callable[[T], AsyncIterable[R] | Iterable[R] | Awaitable[AsyncIterable[R] | Iterable[R]]]</code> <p>The callable applied by this operation.</p> required <code>concurrency</code> <code>int</code> <p>The maximum number of operations or inner sources active at once.</p> <code>8</code> <p>Returns:</p> Type Description <code>AsyncFlow[R]</code> <p>A new lazy <code>AsyncFlow</code> representing this operation.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.switch_map","title":"switch_map","text":"<pre><code>switch_map(\n    function: Callable[\n        [T],\n        AsyncIterable[R]\n        | Iterable[R]\n        | Awaitable[AsyncIterable[R] | Iterable[R]],\n    ],\n) -&gt; AsyncFlow[R]\n</code></pre> <p>Map each item to a source and emit only from the latest source.</p> <p>When a new outer item arrives, the previous mapper or inner source is cancelled and closed. After the outer source completes, the latest inner source may finish normally.</p> <p>Parameters:</p> Name Type Description Default <code>function</code> <code>Callable[[T], AsyncIterable[R] | Iterable[R] | Awaitable[AsyncIterable[R] | Iterable[R]]]</code> <p>A sync or async callable returning a sync or async iterable.</p> required <p>Returns:</p> Type Description <code>AsyncFlow[R]</code> <p>A new lazy <code>AsyncFlow</code> that emits items from the most recent inner source.</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If function is not callable.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.delay","title":"delay","text":"<pre><code>delay(seconds: float) -&gt; AsyncFlow[T]\n</code></pre> <p>Wait before requesting the first item from this flow.</p> <p>The delay is applied once per evaluation. No upstream item is requested while waiting, and cancellation during the delay still closes the source.</p> <p>Parameters:</p> Name Type Description Default <code>seconds</code> <code>float</code> <p>The positive delay in seconds before the first upstream request.</p> required <p>Returns:</p> Type Description <code>AsyncFlow[T]</code> <p>A new lazy <code>AsyncFlow</code> with delayed consumption.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If seconds is not positive.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.throttle","title":"throttle","text":"<pre><code>throttle(max_count: int, *, per: float) -&gt; AsyncFlow[T]\n</code></pre> <p>Limit emissions within a sliding time window.</p> <p>Up to max_count items may be emitted immediately. Later items wait until the oldest emission leaves the monotonic window; encounter order and pull-based backpressure are preserved.</p> <p>Parameters:</p> Name Type Description Default <code>max_count</code> <code>int</code> <p>The maximum number of emissions allowed in one window.</p> required <code>per</code> <code>float</code> <p>The positive sliding-window duration in seconds.</p> required <p>Returns:</p> Type Description <code>AsyncFlow[T]</code> <p>A new lazy <code>AsyncFlow</code> with bounded emission rate.</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If max_count is not an integer.</p> <code>ValueError</code> <p>If max_count is below one or per is not positive.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.spaceout","title":"spaceout","text":"<pre><code>spaceout(seconds: float) -&gt; AsyncFlow[T]\n</code></pre> <p>Separate consecutive emissions by at least seconds.</p> <p>The first item is emitted immediately. Each later item waits only for the remainder of the requested interval, using the event loop's monotonic clock.</p> <p>Parameters:</p> Name Type Description Default <code>seconds</code> <code>float</code> <p>The positive minimum interval between emissions.</p> required <p>Returns:</p> Type Description <code>AsyncFlow[T]</code> <p>A new lazy <code>AsyncFlow</code> with evenly spaced emissions.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If seconds is not positive.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.timeout","title":"timeout","text":"<pre><code>timeout(seconds: float) -&gt; AsyncFlow[T]\n</code></pre> <p>Fail when the next item takes longer than seconds.</p> <p>Parameters:</p> Name Type Description Default <code>seconds</code> <code>float</code> <p>The duration in seconds used by the timing operation.</p> required <p>Returns:</p> Type Description <code>AsyncFlow[T]</code> <p>A new lazy <code>AsyncFlow</code> representing this operation.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.debounce","title":"debounce","text":"<pre><code>debounce(seconds: float) -&gt; AsyncFlow[T]\n</code></pre> <p>Emit an item only after the source stays quiet for seconds.</p> <p>Parameters:</p> Name Type Description Default <code>seconds</code> <code>float</code> <p>The duration in seconds used by the timing operation.</p> required <p>Returns:</p> Type Description <code>AsyncFlow[T]</code> <p>A new lazy <code>AsyncFlow</code> representing this operation.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.buffer_timeout","title":"buffer_timeout","text":"<pre><code>buffer_timeout(\n    max_count: int, seconds: float\n) -&gt; AsyncFlow[tuple[T, ...]]\n</code></pre> <p>Flush a tuple when it reaches max_count or seconds elapse.</p> <p>Parameters:</p> Name Type Description Default <code>max_count</code> <code>int</code> <p>The maximum number of items emitted in one batch.</p> required <code>seconds</code> <code>float</code> <p>The duration in seconds used by the timing operation.</p> required <p>Returns:</p> Type Description <code>AsyncFlow[tuple[T, ...]]</code> <p>A new lazy <code>AsyncFlow</code> representing this operation.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.filter_map","title":"filter_map","text":"<pre><code>filter_map(\n    function: Callable[[T], R | Awaitable[R | None] | None],\n) -&gt; AsyncFlow[R]\n</code></pre> <p>Map items and discard results equal to None.</p> <p>Parameters:</p> Name Type Description Default <code>function</code> <code>Callable[[T], R | Awaitable[R | None] | None]</code> <p>The callable applied by this operation.</p> required <p>Returns:</p> Type Description <code>AsyncFlow[R]</code> <p>A new lazy <code>AsyncFlow</code> representing this operation.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.pluck","title":"pluck","text":"<pre><code>pluck(selector: Selector) -&gt; AsyncFlow[Any]\n</code></pre> <p>Select one field, index, attribute, or nested path from each item.</p> <p>Parameters:</p> Name Type Description Default <code>selector</code> <code>Selector</code> <p>A callable, field name, index, path, or expression used to select a value.</p> required <p>Returns:</p> Type Description <code>AsyncFlow[Any]</code> <p>A new lazy <code>AsyncFlow</code> representing this operation.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.compact","title":"compact","text":"<pre><code>compact(selector: Selector | None = None) -&gt; AsyncFlow[T]\n</code></pre> <p>Drop None values, optionally selected from each item.</p> <p>Parameters:</p> Name Type Description Default <code>selector</code> <code>Selector | None</code> <p>A callable, field name, index, path, or expression used to select a value.</p> <code>None</code> <p>Returns:</p> Type Description <code>AsyncFlow[T]</code> <p>A new lazy <code>AsyncFlow</code> representing this operation.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.filter_none","title":"filter_none","text":"<pre><code>filter_none() -&gt; AsyncFlow[T]\n</code></pre> <p>Drop None values, optionally selected from each item.</p> <p>Returns:</p> Type Description <code>AsyncFlow[T]</code> <p>A new lazy <code>AsyncFlow</code> representing this operation.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.unique","title":"unique","text":"<pre><code>unique() -&gt; AsyncFlow[T]\n</code></pre> <p>Keep the first occurrence of each hashable value.</p> <p>Returns:</p> Type Description <code>AsyncFlow[T]</code> <p>A new lazy <code>AsyncFlow</code> representing this operation.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.unique_by","title":"unique_by","text":"<pre><code>unique_by(selector: Selector) -&gt; AsyncFlow[T]\n</code></pre> <p>Keep the first item for each selected key.</p> <p>Parameters:</p> Name Type Description Default <code>selector</code> <code>Selector</code> <p>A callable, field name, index, path, or expression used to select a value.</p> required <p>Returns:</p> Type Description <code>AsyncFlow[T]</code> <p>A new lazy <code>AsyncFlow</code> representing this operation.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.attempt","title":"attempt","text":"<pre><code>attempt(\n    function: Callable[[T], R | Awaitable[R]],\n) -&gt; AsyncFlow[Result[R]]\n</code></pre> <p>Map each item and wrap success or failure in a Result.</p> <p>Parameters:</p> Name Type Description Default <code>function</code> <code>Callable[[T], R | Awaitable[R]]</code> <p>The callable applied by this operation.</p> required <p>Returns:</p> Type Description <code>AsyncFlow[Result[R]]</code> <p>A new lazy <code>AsyncFlow</code> representing this operation.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.take","title":"take","text":"<pre><code>take(count: int) -&gt; AsyncFlow[T]\n</code></pre> <p>Emit at most count items and cancel pending upstream work.</p> <p>Parameters:</p> Name Type Description Default <code>count</code> <code>int</code> <p>The requested number of items.</p> required <p>Returns:</p> Type Description <code>AsyncFlow[T]</code> <p>A new lazy <code>AsyncFlow</code> representing this operation.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If count is negative.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.drop","title":"drop","text":"<pre><code>drop(count: int) -&gt; AsyncFlow[T]\n</code></pre> <p>Skip count items before yielding the remainder.</p> <p>Parameters:</p> Name Type Description Default <code>count</code> <code>int</code> <p>The requested number of items.</p> required <p>Returns:</p> Type Description <code>AsyncFlow[T]</code> <p>A new lazy <code>AsyncFlow</code> representing this operation.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If count is negative.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.take_while","title":"take_while","text":"<pre><code>take_while(\n    predicate: Callable[[T], bool | Awaitable[bool]],\n) -&gt; AsyncFlow[T]\n</code></pre> <p>Emit the longest prefix that satisfies predicate.</p> <p>Parameters:</p> Name Type Description Default <code>predicate</code> <code>Callable[[T], bool | Awaitable[bool]]</code> <p>A callable that decides whether an item matches.</p> required <p>Returns:</p> Type Description <code>AsyncFlow[T]</code> <p>A new lazy <code>AsyncFlow</code> representing this operation.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.take_while_inclusive","title":"take_while_inclusive","text":"<pre><code>take_while_inclusive(\n    predicate: Callable[[T], bool | Awaitable[bool]],\n) -&gt; AsyncFlow[T]\n</code></pre> <p>Emit through the first item that fails predicate.</p> <p>Parameters:</p> Name Type Description Default <code>predicate</code> <code>Callable[[T], bool | Awaitable[bool]]</code> <p>A callable that decides whether an item matches.</p> required <p>Returns:</p> Type Description <code>AsyncFlow[T]</code> <p>A new lazy <code>AsyncFlow</code> representing this operation.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.drop_while","title":"drop_while","text":"<pre><code>drop_while(\n    predicate: Callable[[T], bool | Awaitable[bool]],\n) -&gt; AsyncFlow[T]\n</code></pre> <p>Skip the longest prefix that satisfies predicate.</p> <p>Parameters:</p> Name Type Description Default <code>predicate</code> <code>Callable[[T], bool | Awaitable[bool]]</code> <p>A callable that decides whether an item matches.</p> required <p>Returns:</p> Type Description <code>AsyncFlow[T]</code> <p>A new lazy <code>AsyncFlow</code> representing this operation.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.chunk","title":"chunk","text":"<pre><code>chunk(size: int) -&gt; AsyncFlow[tuple[T, ...]]\n</code></pre> <p>Group consecutive items into fixed-size tuples.</p> <p>Parameters:</p> Name Type Description Default <code>size</code> <code>int</code> <p>The requested window, chunk, or batch size.</p> required <p>Returns:</p> Type Description <code>AsyncFlow[tuple[T, ...]]</code> <p>A new lazy <code>AsyncFlow</code> representing this operation.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If size is less than one.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.window","title":"window","text":"<pre><code>window(\n    size: int, *, step: int = 1\n) -&gt; AsyncFlow[tuple[T, ...]]\n</code></pre> <p>Emit sliding tuples of size with the requested step.</p> <p>Parameters:</p> Name Type Description Default <code>size</code> <code>int</code> <p>The requested window, chunk, or batch size.</p> required <code>step</code> <code>int</code> <p>The distance between consecutive windows or numeric increments.</p> <code>1</code> <p>Returns:</p> Type Description <code>AsyncFlow[tuple[T, ...]]</code> <p>A new lazy <code>AsyncFlow</code> representing this operation.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If size or step is less than one.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.pairwise","title":"pairwise","text":"<pre><code>pairwise() -&gt; AsyncFlow[tuple[T, T]]\n</code></pre> <p>Emit each adjacent pair of items.</p> <p>Returns:</p> Type Description <code>AsyncFlow[tuple[T, T]]</code> <p>A new lazy <code>AsyncFlow</code> representing this operation.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.pair_map","title":"pair_map","text":"<pre><code>pair_map(\n    function: Callable[[T, T], R | Awaitable[R]],\n) -&gt; AsyncFlow[R]\n</code></pre> <p>Apply a two-argument function to each adjacent pair.</p> <p>Parameters:</p> Name Type Description Default <code>function</code> <code>Callable[[T, T], R | Awaitable[R]]</code> <p>The callable applied by this operation.</p> required <p>Returns:</p> Type Description <code>AsyncFlow[R]</code> <p>A new lazy <code>AsyncFlow</code> representing this operation.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.group_runs","title":"group_runs","text":"<pre><code>group_runs(\n    key: Selector | None = None,\n) -&gt; AsyncFlow[tuple[T, ...]]\n</code></pre> <p>Group consecutive items that share the same key.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>Selector | None</code> <p>The callable or selector used to derive a key.</p> <code>None</code> <p>Returns:</p> Type Description <code>AsyncFlow[tuple[T, ...]]</code> <p>A new lazy <code>AsyncFlow</code> representing this operation.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.enumerate","title":"enumerate","text":"<pre><code>enumerate(start: int = 0) -&gt; AsyncFlow[tuple[int, T]]\n</code></pre> <p>Pair each item with a consecutive index starting at start.</p> <p>Parameters:</p> Name Type Description Default <code>start</code> <code>int</code> <p>The first index, numeric value, or additive identity to use.</p> <code>0</code> <p>Returns:</p> Type Description <code>AsyncFlow[tuple[int, T]]</code> <p>A new lazy <code>AsyncFlow</code> representing this operation.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.zip","title":"zip","text":"<pre><code>zip(\n    other: AsyncIterable[U] | Iterable[U],\n    *,\n    strict: bool = False,\n) -&gt; AsyncFlow[tuple[T, U]]\n</code></pre> <p>Pair items with another source until one side ends.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>AsyncIterable[U] | Iterable[U]</code> <p>The other iterable, flow, value, or fallback used by the operation.</p> required <code>strict</code> <code>bool</code> <p>Whether invalid or empty input should raise instead of returning a fallback.</p> <code>False</code> <p>Returns:</p> Type Description <code>AsyncFlow[tuple[T, U]]</code> <p>A new lazy <code>AsyncFlow</code> representing this operation.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.zip_longest","title":"zip_longest","text":"<pre><code>zip_longest(\n    other: AsyncIterable[U] | Iterable[U],\n    *,\n    fillvalue: Any = None,\n) -&gt; AsyncFlow[tuple[T | Any, U | Any]]\n</code></pre> <p>Pair with another source until both sides end, filling missing values.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>AsyncIterable[U] | Iterable[U]</code> <p>The other iterable, flow, value, or fallback used by the operation.</p> required <code>fillvalue</code> <code>Any</code> <p>The value used when one side of a longest zip is exhausted.</p> <code>None</code> <p>Returns:</p> Type Description <code>AsyncFlow[tuple[T | Any, U | Any]]</code> <p>A new lazy <code>AsyncFlow</code> representing this operation.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.intersperse","title":"intersperse","text":"<pre><code>intersperse(separator: T) -&gt; AsyncFlow[T]\n</code></pre> <p>Insert separator between consecutive items.</p> <p>Parameters:</p> Name Type Description Default <code>separator</code> <code>T</code> <p>The string inserted between adjacent string representations.</p> required <p>Returns:</p> Type Description <code>AsyncFlow[T]</code> <p>A new lazy <code>AsyncFlow</code> representing this operation.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.concat","title":"concat","text":"<pre><code>concat(\n    *others: AsyncIterable[T] | Iterable[T],\n) -&gt; AsyncFlow[T]\n</code></pre> <p>Emit this flow followed by each supplied source.</p> <p>Parameters:</p> Name Type Description Default <code>*others</code> <code>AsyncIterable[T] | Iterable[T]</code> <p>Additional sources combined with this pipeline.</p> <code>()</code> <p>Returns:</p> Type Description <code>AsyncFlow[T]</code> <p>A new lazy <code>AsyncFlow</code> representing this operation.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.cross","title":"cross","text":"<pre><code>cross(\n    other: AsyncIterable[U] | Iterable[U],\n    *,\n    max_right: int | None = None,\n) -&gt; AsyncFlow[tuple[T, U]]\n</code></pre> <p>Emit the Cartesian product with a bounded or reiterable right side.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>AsyncIterable[U] | Iterable[U]</code> <p>The other iterable, flow, value, or fallback used by the operation.</p> required <code>max_right</code> <code>int | None</code> <p>The maximum right-side size allowed when buffering is required.</p> <code>None</code> <p>Returns:</p> Type Description <code>AsyncFlow[tuple[T, U]]</code> <p>A new lazy <code>AsyncFlow</code> representing this operation.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.scan","title":"scan","text":"<pre><code>scan(\n    initial: R, function: Callable[[R, T], R | Awaitable[R]]\n) -&gt; AsyncFlow[R]\n</code></pre> <p>Emit each left-to-right accumulator state.</p> <p>Unlike <code>reduce</code>, <code>scan</code> emits every intermediate accumulator state.</p> <p>Parameters:</p> Name Type Description Default <code>initial</code> <code>R</code> <p>The initial accumulator value. When omitted, the first item is used where supported.</p> required <code>function</code> <code>Callable[[R, T], R | Awaitable[R]]</code> <p>The callable applied by this operation.</p> required <p>Returns:</p> Type Description <code>AsyncFlow[R]</code> <p>A new lazy <code>AsyncFlow</code> representing this operation.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.scan_right","title":"scan_right","text":"<pre><code>scan_right(\n    initial: R,\n    function: Callable[[T, R], R | Awaitable[R]],\n    *,\n    max_items: int | None = None,\n) -&gt; AsyncFlow[R]\n</code></pre> <p>Emit accumulator states while combining items from right to left.</p> <p>Parameters:</p> Name Type Description Default <code>initial</code> <code>R</code> <p>The initial accumulator value. When omitted, the first item is used where supported.</p> required <code>function</code> <code>Callable[[T, R], R | Awaitable[R]]</code> <p>The callable applied by this operation.</p> required <code>max_items</code> <code>int | None</code> <p>The maximum number of source items allowed in the right-side buffer.</p> <code>None</code> <p>Returns:</p> Type Description <code>AsyncFlow[R]</code> <p>A new lazy <code>AsyncFlow</code> representing this operation.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.prepend","title":"prepend","text":"<pre><code>prepend(*values: T) -&gt; AsyncFlow[T]\n</code></pre> <p>Emit values before the items from this flow.</p> <p>Parameters:</p> Name Type Description Default <code>*values</code> <code>T</code> <p>Values supplied to this operation in encounter order.</p> <code>()</code> <p>Returns:</p> Type Description <code>AsyncFlow[T]</code> <p>A new lazy <code>AsyncFlow</code> representing this operation.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.append","title":"append","text":"<pre><code>append(*values: T) -&gt; AsyncFlow[T]\n</code></pre> <p>Emit values after the items from this flow.</p> <p>Parameters:</p> Name Type Description Default <code>*values</code> <code>T</code> <p>Values supplied to this operation in encounter order.</p> <code>()</code> <p>Returns:</p> Type Description <code>AsyncFlow[T]</code> <p>A new lazy <code>AsyncFlow</code> representing this operation.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.map_first","title":"map_first","text":"<pre><code>map_first(\n    function: Callable[[T], T | Awaitable[T]],\n) -&gt; AsyncFlow[T]\n</code></pre> <p>Transform only the first item, if one exists.</p> <p>Parameters:</p> Name Type Description Default <code>function</code> <code>Callable[[T], T | Awaitable[T]]</code> <p>The callable applied by this operation.</p> required <p>Returns:</p> Type Description <code>AsyncFlow[T]</code> <p>A new lazy <code>AsyncFlow</code> representing this operation.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.map_last","title":"map_last","text":"<pre><code>map_last(\n    function: Callable[[T], T | Awaitable[T]],\n) -&gt; AsyncFlow[T]\n</code></pre> <p>Transform only the last item, if one exists.</p> <p>Parameters:</p> Name Type Description Default <code>function</code> <code>Callable[[T], T | Awaitable[T]]</code> <p>The callable applied by this operation.</p> required <p>Returns:</p> Type Description <code>AsyncFlow[T]</code> <p>A new lazy <code>AsyncFlow</code> representing this operation.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.collapse","title":"collapse","text":"<pre><code>collapse(\n    collapsible: Callable[[T, T], bool | Awaitable[bool]],\n    merger: Callable[[T, T], T | Awaitable[T]],\n) -&gt; AsyncFlow[T]\n</code></pre> <p>Merge adjacent items while collapsible returns true.</p> <p>Parameters:</p> Name Type Description Default <code>collapsible</code> <code>Callable[[T, T], bool | Awaitable[bool]]</code> <p>A callable deciding whether two adjacent items should be combined.</p> required <code>merger</code> <code>Callable[[T, T], T | Awaitable[T]]</code> <p>A callable that merges two downstream results.</p> required <p>Returns:</p> Type Description <code>AsyncFlow[T]</code> <p>A new lazy <code>AsyncFlow</code> representing this operation.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.fold","title":"fold","text":"<pre><code>fold(\n    initializer: Callable[[], R | Awaitable[R]],\n    function: Callable[[R, T], R | Awaitable[R]],\n) -&gt; AsyncFlow[R]\n</code></pre> <p>Apply a Gatherer-compatible stateful fold.</p> <p>Parameters:</p> Name Type Description Default <code>initializer</code> <code>Callable[[], R | Awaitable[R]]</code> <p>A zero-argument callable that creates fresh mutable state.</p> required <code>function</code> <code>Callable[[R, T], R | Awaitable[R]]</code> <p>The callable applied by this operation.</p> required <p>Returns:</p> Type Description <code>AsyncFlow[R]</code> <p>A new lazy <code>AsyncFlow</code> representing this operation.</p>"},{"location":"reference/async_flow/#fpstreams.AsyncFlow.batch_by_size","title":"batch_by_size","text":"<pre><code>batch_by_size(\n    max_size: int,\n    *,\n    max_count: int | None = None,\n    get_size: Callable[\n        [T], int | Awaitable[int]\n    ] = _default_item_size,\n    strict: bool = True,\n) -&gt; AsyncFlow[tuple[T, ...]]\n</code></pre> <p>Build batches constrained by count and measured size.</p> <p>Parameters:</p> Name Type Description Default <code>max_size</code> <code>int</code> <p>The maximum measured size allowed in one batch.</p> required <code>max_count</code> <code>int | None</code> <p>The maximum number of items emitted in one batch.</p> <code>None</code> <code>get_size</code> <code>Callable[[T], int | Awaitable[int]]</code> <p>A callable that returns the measured size of one item.</p> <code>_default_item_size</code> <code>strict</code> <code>bool</code> <p>Whether invalid or empty input should raise instead of returning a fallback.</p> <code>True</code> <p>Returns:</p> Type Description <code>AsyncFlow[tuple[T, ...]]</code> <p>A new lazy <code>AsyncFlow</code> representing this operation.</p>"},{"location":"reference/collecting/","title":"Collecting and aggregation","text":"<p>Collectors reduce a flow into a result. Aggregators are composable state machines used by named and grouped aggregation.</p>"},{"location":"reference/collecting/#collector","title":"Collector","text":""},{"location":"reference/collecting/#fpstreams.Collector","title":"fpstreams.Collector  <code>dataclass</code>","text":"<p>               Bases: <code>Generic[T, S, R]</code></p> <p>An immutable, composable description of a streaming reduction.</p>"},{"location":"reference/collecting/#built-in-collectors","title":"Built-in collectors","text":""},{"location":"reference/collecting/#fpstreams.Collectors","title":"fpstreams.Collectors","text":"<p>               Bases: <code>Generic[T]</code></p> <p>Factories for composable streaming collectors.</p>"},{"location":"reference/collecting/#fpstreams.Collectors.to_list","title":"to_list  <code>staticmethod</code>","text":"<pre><code>to_list() -&gt; Collector[T, list[T], list[T]]\n</code></pre> <p>Collect all items into a list.</p> <p>Returns:</p> Type Description <code>Collector[T, list[T], list[T]]</code> <p>A reusable <code>Collector</code> implementing the described reduction.</p>"},{"location":"reference/collecting/#fpstreams.Collectors.to_set","title":"to_set  <code>staticmethod</code>","text":"<pre><code>to_set() -&gt; Collector[T, set[T], set[T]]\n</code></pre> <p>Collect distinct hashable items into a set.</p> <p>Returns:</p> Type Description <code>Collector[T, set[T], set[T]]</code> <p>A reusable <code>Collector</code> implementing the described reduction.</p>"},{"location":"reference/collecting/#fpstreams.Collectors.to_tuple","title":"to_tuple  <code>staticmethod</code>","text":"<pre><code>to_tuple() -&gt; Collector[T, list[T], tuple[T, ...]]\n</code></pre> <p>Collect all items into a tuple.</p> <p>Returns:</p> Type Description <code>Collector[T, list[T], tuple[T, ...]]</code> <p>A reusable <code>Collector</code> implementing the described reduction.</p>"},{"location":"reference/collecting/#fpstreams.Collectors.joining","title":"joining  <code>staticmethod</code>","text":"<pre><code>joining(\n    delimiter: str = \"\",\n) -&gt; Collector[Any, list[str], str]\n</code></pre> <p>Convert items to strings and join them with delimiter.</p> <p>Parameters:</p> Name Type Description Default <code>delimiter</code> <code>str</code> <p>The string inserted between collected values.</p> <code>''</code> <p>Returns:</p> Type Description <code>Collector[Any, list[str], str]</code> <p>A reusable <code>Collector</code> implementing the described reduction.</p>"},{"location":"reference/collecting/#fpstreams.Collectors.grouping_by","title":"grouping_by  <code>staticmethod</code>","text":"<pre><code>grouping_by(\n    classifier: Selector,\n    downstream: Collector[T, Any, R]\n    | Callable[[Iterable[T]], R]\n    | None = None,\n) -&gt; Collector[T, dict[Any, Any], dict[Any, R | list[T]]]\n</code></pre> <p>Group items by classifier and reduce each group.</p> <p>Parameters:</p> Name Type Description Default <code>classifier</code> <code>Selector</code> <p>A callable that selects the group for each input item.</p> required <code>downstream</code> <code>Collector[T, Any, R] | Callable[[Iterable[T]], R] | None</code> <p>The collector or gatherer that receives transformed items.</p> <code>None</code> <p>Returns:</p> Type Description <code>Collector[T, dict[Any, Any], dict[Any, R | list[T]]]</code> <p>A reusable <code>Collector</code> implementing the described reduction.</p>"},{"location":"reference/collecting/#fpstreams.Collectors.partitioning_by","title":"partitioning_by  <code>staticmethod</code>","text":"<pre><code>partitioning_by(\n    predicate: Selector,\n    downstream: Collector[T, Any, R]\n    | Callable[[Iterable[T]], R]\n    | None = None,\n) -&gt; Collector[T, dict[bool, Any], dict[bool, R | list[T]]]\n</code></pre> <p>Split items by predicate and reduce both partitions.</p> <p>Parameters:</p> Name Type Description Default <code>predicate</code> <code>Selector</code> <p>A callable that decides whether an item matches.</p> required <code>downstream</code> <code>Collector[T, Any, R] | Callable[[Iterable[T]], R] | None</code> <p>The collector or gatherer that receives transformed items.</p> <code>None</code> <p>Returns:</p> Type Description <code>Collector[T, dict[bool, Any], dict[bool, R | list[T]]]</code> <p>A reusable <code>Collector</code> implementing the described reduction.</p>"},{"location":"reference/collecting/#fpstreams.Collectors.mapping","title":"mapping  <code>staticmethod</code>","text":"<pre><code>mapping(\n    mapper: Selector,\n    downstream: Collector[U, Any, R]\n    | Callable[[Iterable[U]], R],\n) -&gt; Collector[T, Any, R]\n</code></pre> <p>Transform each item before passing it to downstream.</p> <p>Parameters:</p> Name Type Description Default <code>mapper</code> <code>Selector</code> <p>The callable used to transform each selected value.</p> required <code>downstream</code> <code>Collector[U, Any, R] | Callable[[Iterable[U]], R]</code> <p>The collector or gatherer that receives transformed items.</p> required <p>Returns:</p> Type Description <code>Collector[T, Any, R]</code> <p>A reusable <code>Collector</code> implementing the described reduction.</p>"},{"location":"reference/collecting/#fpstreams.Collectors.filtering","title":"filtering  <code>staticmethod</code>","text":"<pre><code>filtering(\n    predicate: Selector,\n    downstream: Collector[T, Any, R]\n    | Callable[[Iterable[T]], R],\n) -&gt; Collector[T, Any, R]\n</code></pre> <p>Pass only matching items to downstream.</p> <p>Parameters:</p> Name Type Description Default <code>predicate</code> <code>Selector</code> <p>A callable that decides whether an item matches.</p> required <code>downstream</code> <code>Collector[T, Any, R] | Callable[[Iterable[T]], R]</code> <p>The collector or gatherer that receives transformed items.</p> required <p>Returns:</p> Type Description <code>Collector[T, Any, R]</code> <p>A reusable <code>Collector</code> implementing the described reduction.</p>"},{"location":"reference/collecting/#fpstreams.Collectors.flat_mapping","title":"flat_mapping  <code>staticmethod</code>","text":"<pre><code>flat_mapping(\n    mapper: Selector,\n    downstream: Collector[U, Any, R]\n    | Callable[[Iterable[U]], R],\n) -&gt; Collector[T, Any, R]\n</code></pre> <p>Expand each item and pass every nested item to downstream.</p> <p>Parameters:</p> Name Type Description Default <code>mapper</code> <code>Selector</code> <p>The callable used to transform each selected value.</p> required <code>downstream</code> <code>Collector[U, Any, R] | Callable[[Iterable[U]], R]</code> <p>The collector or gatherer that receives transformed items.</p> required <p>Returns:</p> Type Description <code>Collector[T, Any, R]</code> <p>A reusable <code>Collector</code> implementing the described reduction.</p>"},{"location":"reference/collecting/#fpstreams.Collectors.collecting_and_then","title":"collecting_and_then  <code>staticmethod</code>","text":"<pre><code>collecting_and_then(\n    downstream: Collector[T, Any, R]\n    | Callable[[Iterable[T]], R],\n    finisher: Callable[[R], U],\n) -&gt; Collector[T, Any, U]\n</code></pre> <p>Apply finisher to the result produced by downstream.</p> <p>Parameters:</p> Name Type Description Default <code>downstream</code> <code>Collector[T, Any, R] | Callable[[Iterable[T]], R]</code> <p>The collector or gatherer that receives transformed items.</p> required <code>finisher</code> <code>Callable[[R], U]</code> <p>A callable that converts accumulated state into the final result.</p> required <p>Returns:</p> Type Description <code>Collector[T, Any, U]</code> <p>A reusable <code>Collector</code> implementing the described reduction.</p>"},{"location":"reference/collecting/#fpstreams.Collectors.teeing","title":"teeing  <code>staticmethod</code>","text":"<pre><code>teeing(\n    left: Collector[T, Any, R] | Callable[[Iterable[T]], R],\n    right: Collector[T, Any, U]\n    | Callable[[Iterable[T]], U],\n    merger: Callable[[R, U], V],\n) -&gt; Collector[T, _TeeState, V]\n</code></pre> <p>Run two collectors in one traversal and merge their results.</p> <p>Parameters:</p> Name Type Description Default <code>left</code> <code>Collector[T, Any, R] | Callable[[Iterable[T]], R]</code> <p>The first collector, result, or value to combine.</p> required <code>right</code> <code>Collector[T, Any, U] | Callable[[Iterable[T]], U]</code> <p>The second collector, result, or value to combine.</p> required <code>merger</code> <code>Callable[[R, U], V]</code> <p>A callable that merges two downstream results.</p> required <p>Returns:</p> Type Description <code>Collector[T, _TeeState, V]</code> <p>A reusable <code>Collector</code> implementing the described reduction.</p>"},{"location":"reference/collecting/#fpstreams.Collectors.counting","title":"counting  <code>staticmethod</code>","text":"<pre><code>counting() -&gt; Collector[T, int, int]\n</code></pre> <p>Count all input items.</p> <p>Returns:</p> Type Description <code>Collector[T, int, int]</code> <p>A reusable <code>Collector</code> implementing the described reduction.</p>"},{"location":"reference/collecting/#fpstreams.Collectors.summing","title":"summing  <code>staticmethod</code>","text":"<pre><code>summing(\n    selector: Selector | None = None,\n) -&gt; Collector[T, Any, Any]\n</code></pre> <p>Sum input items or selected values.</p> <p>Parameters:</p> Name Type Description Default <code>selector</code> <code>Selector | None</code> <p>A callable, field name, index, path, or expression used to select a value.</p> <code>None</code> <p>Returns:</p> Type Description <code>Collector[T, Any, Any]</code> <p>A reusable <code>Collector</code> implementing the described reduction.</p>"},{"location":"reference/collecting/#fpstreams.Collectors.averaging","title":"averaging  <code>staticmethod</code>","text":"<pre><code>averaging(\n    selector: Selector | None = None,\n) -&gt; Collector[T, tuple[Any, int], float]\n</code></pre> <p>Return the arithmetic mean of items or selected values.</p> <p>Parameters:</p> Name Type Description Default <code>selector</code> <code>Selector | None</code> <p>A callable, field name, index, path, or expression used to select a value.</p> <code>None</code> <p>Returns:</p> Type Description <code>Collector[T, tuple[Any, int], float]</code> <p>A reusable <code>Collector</code> implementing the described reduction.</p>"},{"location":"reference/collecting/#fpstreams.Collectors.summarizing","title":"summarizing  <code>staticmethod</code>","text":"<pre><code>summarizing(\n    selector: Selector | None = None,\n) -&gt; Collector[T, SummaryStatistics, SummaryStatistics]\n</code></pre> <p>Return count, sum, minimum, maximum, and average in one traversal.</p> <p>Parameters:</p> Name Type Description Default <code>selector</code> <code>Selector | None</code> <p>A callable, field name, index, path, or expression used to select a value.</p> <code>None</code> <p>Returns:</p> Type Description <code>Collector[T, SummaryStatistics, SummaryStatistics]</code> <p>A reusable <code>Collector</code> implementing the described reduction.</p>"},{"location":"reference/collecting/#fpstreams.Collectors.first","title":"first  <code>staticmethod</code>","text":"<pre><code>first() -&gt; Collector[T, Any, T | None]\n</code></pre> <p>Return the first item, or None when input is empty.</p> <p>Returns:</p> Type Description <code>Collector[T, Any, T | None]</code> <p>A reusable <code>Collector</code> implementing the described reduction.</p>"},{"location":"reference/collecting/#fpstreams.Collectors.last","title":"last  <code>staticmethod</code>","text":"<pre><code>last() -&gt; Collector[T, Any, T | None]\n</code></pre> <p>Return the last item, or None when input is empty.</p> <p>Returns:</p> Type Description <code>Collector[T, Any, T | None]</code> <p>A reusable <code>Collector</code> implementing the described reduction.</p>"},{"location":"reference/collecting/#fpstreams.Collectors.head","title":"head  <code>staticmethod</code>","text":"<pre><code>head(count: int) -&gt; Collector[T, list[T], list[T]]\n</code></pre> <p>Collect at most the first count items.</p> <p>Parameters:</p> Name Type Description Default <code>count</code> <code>int</code> <p>The requested number of items.</p> required <p>Returns:</p> Type Description <code>Collector[T, list[T], list[T]]</code> <p>A reusable <code>Collector</code> implementing the described reduction.</p>"},{"location":"reference/collecting/#fpstreams.Collectors.tail","title":"tail  <code>staticmethod</code>","text":"<pre><code>tail(count: int) -&gt; Collector[T, deque[T], list[T]]\n</code></pre> <p>Collect at most the last count items.</p> <p>Parameters:</p> Name Type Description Default <code>count</code> <code>int</code> <p>The requested number of items.</p> required <p>Returns:</p> Type Description <code>Collector[T, deque[T], list[T]]</code> <p>A reusable <code>Collector</code> implementing the described reduction.</p>"},{"location":"reference/collecting/#fpstreams.Collectors.only","title":"only  <code>staticmethod</code>","text":"<pre><code>only() -&gt; Collector[T, list[T], T | None]\n</code></pre> <p>Return the sole item, None for empty input, or raise for multiple items.</p> <p>Returns:</p> Type Description <code>Collector[T, list[T], T | None]</code> <p>A reusable <code>Collector</code> implementing the described reduction.</p> <p>Raises:</p> Type Description <code>OnlyElementError</code> <p>If the input contains more than one item.</p>"},{"location":"reference/collecting/#fpstreams.Collectors.to_dict","title":"to_dict  <code>staticmethod</code>","text":"<pre><code>to_dict(\n    key: Selector,\n    value: Selector,\n    *,\n    on_duplicate: Literal[\n        \"error\", \"first\", \"last\"\n    ] = \"error\",\n) -&gt; Collector[T, dict[K, V], dict[K, V]]\n</code></pre> <p>Collect selected keys and values into a dictionary.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>Selector</code> <p>The callable or selector used to derive a key.</p> required <code>value</code> <code>Selector</code> <p>The value consumed by this operation.</p> required <code>on_duplicate</code> <code>Literal['error', 'first', 'last']</code> <p>The policy used when the same key appears more than once.</p> <code>'error'</code> <p>Returns:</p> Type Description <code>Collector[T, dict[K, V], dict[K, V]]</code> <p>A reusable <code>Collector</code> implementing the described reduction.</p>"},{"location":"reference/collecting/#fpstreams.Collectors.to_columns","title":"to_columns  <code>staticmethod</code>","text":"<pre><code>to_columns() -&gt; Collector[\n    Mapping[str, Any], _ColumnsState, dict[str, list[Any]]\n]\n</code></pre> <p>Collect mapping rows into column-oriented lists.</p> <p>Returns:</p> Type Description <code>Collector[Mapping[str, Any], _ColumnsState, dict[str, list[Any]]]</code> <p>A reusable <code>Collector</code> implementing the described reduction.</p>"},{"location":"reference/collecting/#aggregators","title":"Aggregators","text":"<p><code>agg</code> creates named, single-pass aggregations:</p> Call Result <code>agg.count()</code> Number of input items <code>agg.count_where(predicate)</code> Number of matching items <code>agg.any(predicate)</code> / <code>agg.all(predicate)</code> Boolean checks <code>agg.sum(selector)</code> / <code>agg.mean(selector)</code> Sum or arithmetic mean <code>agg.variance(selector, ddof=1)</code> Variance <code>agg.std(selector, ddof=1)</code> Standard deviation <code>agg.count_distinct(selector)</code> Number of distinct values <code>agg.min(selector)</code> / <code>agg.max(selector)</code> Smallest or largest value <code>agg.first(selector)</code> / <code>agg.last(selector)</code> Boundary values <code>agg.collect(selector, into=list)</code> Values collected with a constructor <p>Omit <code>selector</code> to aggregate the items themselves. A string selector reads a record field; expression and callable selectors are also accepted.</p>"},{"location":"reference/collecting/#fpstreams.Aggregator","title":"fpstreams.Aggregator  <code>dataclass</code>","text":"<p>               Bases: <code>Collector[Any, Any, Any]</code></p> <p>A named Collector that may carry metadata for fused native terminals.</p>"},{"location":"reference/containers/","title":"Option and Result","text":"<p><code>Option[T]</code> represents a present or absent value. <code>Result[T]</code> contains either a successful value or an exception.</p>"},{"location":"reference/containers/#option","title":"Option","text":""},{"location":"reference/containers/#fpstreams.Option","title":"fpstreams.Option  <code>dataclass</code>","text":"<p>               Bases: <code>Generic[T]</code></p> <p>A compatibility container for a value that may be absent.</p>"},{"location":"reference/containers/#fpstreams.Option.of","title":"of  <code>classmethod</code>","text":"<pre><code>of(value: T) -&gt; Option[T]\n</code></pre> <p>Wrap a non-None value; use empty() or of_nullable() for absence.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>T</code> <p>The value consumed by this operation.</p> required <p>Returns:</p> Type Description <code>Option[T]</code> <p>An <code>Option</code> containing the resulting value, or an empty option.</p>"},{"location":"reference/containers/#fpstreams.Option.of_nullable","title":"of_nullable  <code>classmethod</code>","text":"<pre><code>of_nullable(value: T | None) -&gt; Option[T]\n</code></pre> <p>Wrap value in an Option; None produces an empty Option.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>T | None</code> <p>The value consumed by this operation.</p> required <p>Returns:</p> Type Description <code>Option[T]</code> <p>An <code>Option</code> containing the resulting value, or an empty option.</p>"},{"location":"reference/containers/#fpstreams.Option.empty","title":"empty  <code>classmethod</code>","text":"<pre><code>empty() -&gt; Option[T]\n</code></pre> <p>Return an Option with no value.</p> <p>Returns:</p> Type Description <code>Option[T]</code> <p>An <code>Option</code> containing the resulting value, or an empty option.</p>"},{"location":"reference/containers/#fpstreams.Option.is_present","title":"is_present","text":"<pre><code>is_present() -&gt; bool\n</code></pre> <p>Return whether this Option contains a value.</p> <p>Returns:</p> Type Description <code>bool</code> <p>Whether the condition described above is true.</p>"},{"location":"reference/containers/#fpstreams.Option.is_empty","title":"is_empty","text":"<pre><code>is_empty() -&gt; bool\n</code></pre> <p>Return whether this Option contains no value.</p> <p>Returns:</p> Type Description <code>bool</code> <p>Whether the condition described above is true.</p>"},{"location":"reference/containers/#fpstreams.Option.if_present","title":"if_present","text":"<pre><code>if_present(action: Callable[[T], None]) -&gt; None\n</code></pre> <p>Run action when a value is present.</p> <p>Parameters:</p> Name Type Description Default <code>action</code> <code>Callable[[T], None]</code> <p>The side-effecting callable invoked for each matching item.</p> required"},{"location":"reference/containers/#fpstreams.Option.filter","title":"filter","text":"<pre><code>filter(predicate: Callable[[T], bool]) -&gt; Option[T]\n</code></pre> <p>Keep the value only when predicate returns true.</p> <p>An empty option stays empty. A present value is retained only when the predicate is true.</p> <p>Parameters:</p> Name Type Description Default <code>predicate</code> <code>Callable[[T], bool]</code> <p>A callable that decides whether an item matches.</p> required <p>Returns:</p> Type Description <code>Option[T]</code> <p>An <code>Option</code> containing the resulting value, or an empty option.</p>"},{"location":"reference/containers/#fpstreams.Option.map","title":"map","text":"<pre><code>map(mapper: Callable[[T], R | None]) -&gt; Option[R]\n</code></pre> <p>Transform a present value; None becomes an empty Option.</p> <p>The callable runs only for a present value. A mapped <code>None</code> becomes an empty <code>Option</code>.</p> <p>Parameters:</p> Name Type Description Default <code>mapper</code> <code>Callable[[T], R | None]</code> <p>The callable used to transform each selected value.</p> required <p>Returns:</p> Type Description <code>Option[R]</code> <p>An <code>Option</code> containing the resulting value, or an empty option.</p>"},{"location":"reference/containers/#fpstreams.Option.flat_map","title":"flat_map","text":"<pre><code>flat_map(mapper: Callable[[T], Option[R]]) -&gt; Option[R]\n</code></pre> <p>Transform a present value with an Option-returning function.</p> <p>The callable runs only for a present value and must return another <code>Option</code>.</p> <p>Parameters:</p> Name Type Description Default <code>mapper</code> <code>Callable[[T], Option[R]]</code> <p>The callable used to transform each selected value.</p> required <p>Returns:</p> Type Description <code>Option[R]</code> <p>An <code>Option</code> containing the resulting value, or an empty option.</p>"},{"location":"reference/containers/#fpstreams.Option.unwrap","title":"unwrap","text":"<pre><code>unwrap() -&gt; T\n</code></pre> <p>Return the value or raise ValueError when empty.</p> <p>Returns:</p> Type Description <code>T</code> <p>The contained value.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If this option is empty.</p>"},{"location":"reference/containers/#fpstreams.Option.or_else","title":"or_else","text":"<pre><code>or_else(other: T) -&gt; T\n</code></pre> <p>Return the value when present, otherwise other.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>T</code> <p>The other iterable, flow, value, or fallback used by the operation.</p> required <p>Returns:</p> Type Description <code>T</code> <p>The contained value, or <code>other</code> when this option is empty.</p>"},{"location":"reference/containers/#fpstreams.Option.or_else_get","title":"or_else_get","text":"<pre><code>or_else_get(supplier: Callable[[], T]) -&gt; T\n</code></pre> <p>Return the value when present, otherwise call supplier.</p> <p>Parameters:</p> Name Type Description Default <code>supplier</code> <code>Callable[[], T]</code> <p>A zero-argument callable that supplies a value or iterable.</p> required <p>Returns:</p> Type Description <code>T</code> <p>The contained value, or the value supplied when this option is empty.</p>"},{"location":"reference/containers/#fpstreams.Option.or_else_throw","title":"or_else_throw","text":"<pre><code>or_else_throw(exception: Callable[[], Exception]) -&gt; T\n</code></pre> <p>Return the value when present, otherwise raise a supplied exception.</p> <p>Parameters:</p> Name Type Description Default <code>exception</code> <code>Callable[[], Exception]</code> <p>A zero-argument callable that creates the exception to raise.</p> required <p>Returns:</p> Type Description <code>T</code> <p>The contained value.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>The exception produced by <code>exception</code> when this option is empty.</p>"},{"location":"reference/containers/#result","title":"Result","text":""},{"location":"reference/containers/#fpstreams.Result","title":"fpstreams.Result","text":"<p>               Bases: <code>Generic[T]</code></p> <p>A value that is either :class:<code>Ok</code> or :class:<code>Err</code>.</p>"},{"location":"reference/containers/#fpstreams.Result.error","title":"error  <code>property</code>","text":"<pre><code>error: Exception | None\n</code></pre> <p>Return the failure exception, or None for success.</p> <p>Returns:</p> Type Description <code>Exception | None</code> <p>The matching or computed value, or <code>None</code> when unavailable.</p>"},{"location":"reference/containers/#fpstreams.Result.success","title":"success  <code>classmethod</code>","text":"<pre><code>success(value: T) -&gt; Result[T]\n</code></pre> <p>Create a successful Result containing value.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>T</code> <p>The value consumed by this operation.</p> required <p>Returns:</p> Type Description <code>Result[T]</code> <p>A <code>Result</code> containing the transformed success or preserved failure.</p>"},{"location":"reference/containers/#fpstreams.Result.failure","title":"failure  <code>classmethod</code>","text":"<pre><code>failure(error: Exception) -&gt; Result[T]\n</code></pre> <p>Create a failed Result containing error.</p> <p>Parameters:</p> Name Type Description Default <code>error</code> <code>Exception</code> <p>The exception stored in a failed result.</p> required <p>Returns:</p> Type Description <code>Result[T]</code> <p>A <code>Result</code> containing the transformed success or preserved failure.</p>"},{"location":"reference/containers/#fpstreams.Result.of","title":"of  <code>classmethod</code>","text":"<pre><code>of(function: Callable[[], T]) -&gt; Result[T]\n</code></pre> <p>Call function and capture its return value or exception.</p> <p>Parameters:</p> Name Type Description Default <code>function</code> <code>Callable[[], T]</code> <p>The callable applied by this operation.</p> required <p>Returns:</p> Type Description <code>Result[T]</code> <p>A <code>Result</code> containing the transformed success or preserved failure.</p>"},{"location":"reference/containers/#fpstreams.Result.is_success","title":"is_success","text":"<pre><code>is_success() -&gt; bool\n</code></pre> <p>Return whether this Result contains a successful value.</p> <p>Returns:</p> Type Description <code>bool</code> <p>Whether the condition described above is true.</p>"},{"location":"reference/containers/#fpstreams.Result.is_failure","title":"is_failure","text":"<pre><code>is_failure() -&gt; bool\n</code></pre> <p>Return whether this Result contains an exception.</p> <p>Returns:</p> Type Description <code>bool</code> <p>Whether the condition described above is true.</p>"},{"location":"reference/containers/#fpstreams.Result.map","title":"map","text":"<pre><code>map(mapper: Callable[[T], R]) -&gt; Result[R]\n</code></pre> <p>Transform a successful value while preserving failure.</p> <p>The callable runs only for success. Existing failures pass through unchanged, and raised exceptions become failures.</p> <p>Parameters:</p> Name Type Description Default <code>mapper</code> <code>Callable[[T], R]</code> <p>The callable used to transform each selected value.</p> required <p>Returns:</p> Type Description <code>Result[R]</code> <p>A <code>Result</code> containing the transformed success or preserved failure.</p>"},{"location":"reference/containers/#fpstreams.Result.and_then","title":"and_then","text":"<pre><code>and_then(mapper: Callable[[T], Result[R]]) -&gt; Result[R]\n</code></pre> <p>Chain an operation that returns another Result.</p> <p>Parameters:</p> Name Type Description Default <code>mapper</code> <code>Callable[[T], Result[R]]</code> <p>The callable used to transform each selected value.</p> required <p>Returns:</p> Type Description <code>Result[R]</code> <p>A <code>Result</code> containing the transformed success or preserved failure.</p>"},{"location":"reference/containers/#fpstreams.Result.flat_map","title":"flat_map","text":"<pre><code>flat_map(mapper: Callable[[T], Result[R]]) -&gt; Result[R]\n</code></pre> <p>Chain an operation that returns another Result.</p> <p>The callable runs only for success and must return another <code>Result</code>; failures pass through unchanged.</p> <p>Parameters:</p> Name Type Description Default <code>mapper</code> <code>Callable[[T], Result[R]]</code> <p>The callable used to transform each selected value.</p> required <p>Returns:</p> Type Description <code>Result[R]</code> <p>A <code>Result</code> containing the transformed success or preserved failure.</p>"},{"location":"reference/containers/#fpstreams.Result.map_err","title":"map_err","text":"<pre><code>map_err(\n    mapper: Callable[[Exception], Exception],\n) -&gt; Result[T]\n</code></pre> <p>Transform the exception while preserving success.</p> <p>Parameters:</p> Name Type Description Default <code>mapper</code> <code>Callable[[Exception], Exception]</code> <p>The callable used to transform each selected value.</p> required <p>Returns:</p> Type Description <code>Result[T]</code> <p>A <code>Result</code> containing the transformed success or preserved failure.</p>"},{"location":"reference/containers/#fpstreams.Result.map_error","title":"map_error","text":"<pre><code>map_error(\n    mapper: Callable[[Exception], Exception],\n) -&gt; Result[T]\n</code></pre> <p>Transform the exception while preserving success.</p> <p>Parameters:</p> Name Type Description Default <code>mapper</code> <code>Callable[[Exception], Exception]</code> <p>The callable used to transform each selected value.</p> required <p>Returns:</p> Type Description <code>Result[T]</code> <p>A <code>Result</code> containing the transformed success or preserved failure.</p>"},{"location":"reference/containers/#fpstreams.Result.on_success","title":"on_success","text":"<pre><code>on_success(action: Callable[[T], None]) -&gt; Result[T]\n</code></pre> <p>Run action for a successful value and return this Result.</p> <p>Parameters:</p> Name Type Description Default <code>action</code> <code>Callable[[T], None]</code> <p>The side-effecting callable invoked for each matching item.</p> required <p>Returns:</p> Type Description <code>Result[T]</code> <p>A <code>Result</code> containing the transformed success or preserved failure.</p>"},{"location":"reference/containers/#fpstreams.Result.on_failure","title":"on_failure","text":"<pre><code>on_failure(\n    action: Callable[[Exception], None],\n) -&gt; Result[T]\n</code></pre> <p>Run action for a failure exception and return this Result.</p> <p>Parameters:</p> Name Type Description Default <code>action</code> <code>Callable[[Exception], None]</code> <p>The side-effecting callable invoked for each matching item.</p> required <p>Returns:</p> Type Description <code>Result[T]</code> <p>A <code>Result</code> containing the transformed success or preserved failure.</p>"},{"location":"reference/containers/#fpstreams.Result.unwrap","title":"unwrap","text":"<pre><code>unwrap() -&gt; T\n</code></pre> <p>Return the successful value or raise the failure exception.</p> <p>Returns:</p> Type Description <code>T</code> <p>The successful value.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>The stored failure exception when this result is unsuccessful.</p>"},{"location":"reference/containers/#fpstreams.Result.get_or_throw","title":"get_or_throw","text":"<pre><code>get_or_throw() -&gt; T\n</code></pre> <p>Return the successful value or raise the failure exception.</p> <p>Returns:</p> Type Description <code>T</code> <p>The successful value.</p>"},{"location":"reference/containers/#fpstreams.Result.get_or_else","title":"get_or_else","text":"<pre><code>get_or_else(default: T) -&gt; T\n</code></pre> <p>Return the successful value, otherwise default.</p> <p>Parameters:</p> Name Type Description Default <code>default</code> <code>T</code> <p>The value returned when no matching item is available.</p> required <p>Returns:</p> Type Description <code>T</code> <p>The successful value, or <code>default</code> for a failure.</p>"},{"location":"reference/expressions/","title":"Expressions","text":"<p><code>item</code> and <code>fitem</code> build scalar placeholder expressions for flows. <code>col()</code>, <code>lit()</code>, <code>when()</code>, and <code>coalesce()</code> build record expressions for <code>Rows</code>.</p> <pre><code>from fpstreams import col, flow, item, rows\n\nnumbers = flow(range(5)).filter(item % 2 == 0).map(item * 10)\nactive = rows([{\"status\": \"active\"}]).where(col(\"status\") == \"active\")\n</code></pre>"},{"location":"reference/expressions/#building-row-expressions","title":"Building row expressions","text":"Call Meaning <code>col(selector)</code> Read a field, index, attribute, nested path, or callable selector <code>lit(value)</code> Return the same value for every row <code>when(condition, then, otherwise)</code> Choose a value from a condition <code>coalesce(*values)</code> Return the first value that is not <code>None</code> <p>Row-expression methods such as <code>cast()</code>, <code>isin()</code>, <code>fill_null()</code>, <code>lower()</code>, and <code>contains()</code> return new expressions; they do not read rows immediately.</p>"},{"location":"reference/expressions/#scalar-expressions","title":"Scalar expressions","text":""},{"location":"reference/expressions/#fpstreams.Expr","title":"fpstreams.Expr  <code>dataclass</code>","text":"<p>A callable expression that can also be compiled by a native executor.</p>"},{"location":"reference/expressions/#fpstreams.Expr.constant","title":"constant  <code>staticmethod</code>","text":"<pre><code>constant(value: int) -&gt; Expr\n</code></pre> <p>Build an integer expression that always returns value.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>int</code> <p>The value consumed by this operation.</p> required <p>Returns:</p> Type Description <code>Expr</code> <p>A composable scalar expression.</p>"},{"location":"reference/expressions/#fpstreams.Expr.native_instructions","title":"native_instructions","text":"<pre><code>native_instructions() -&gt; tuple[tuple[int, int], ...]\n</code></pre> <p>Compile this expression into native executor instructions.</p> <p>Returns:</p> Type Description <code>tuple[tuple[int, int], ...]</code> <p>A tuple containing the resulting values.</p>"},{"location":"reference/expressions/#row-expressions","title":"Row expressions","text":""},{"location":"reference/expressions/#fpstreams.RowExpr","title":"fpstreams.RowExpr  <code>dataclass</code>","text":"<p>A callable expression evaluated against one row.</p>"},{"location":"reference/expressions/#fpstreams.RowExpr.map","title":"map","text":"<pre><code>map(function: Callable[[Any], Any]) -&gt; RowExpr\n</code></pre> <p>Transform the value produced by this row expression.</p> <p>The callable runs when the expression is evaluated for a row, not when the expression is created.</p> <p>Parameters:</p> Name Type Description Default <code>function</code> <code>Callable[[Any], Any]</code> <p>The callable applied by this operation.</p> required <p>Returns:</p> Type Description <code>RowExpr</code> <p>A new row expression that applies the callable when evaluated.</p>"},{"location":"reference/expressions/#fpstreams.RowExpr.cast","title":"cast","text":"<pre><code>cast(target: Callable[[Any], Any]) -&gt; RowExpr\n</code></pre> <p>Convert the produced value with target.</p> <p>Parameters:</p> Name Type Description Default <code>target</code> <code>Callable[[Any], Any]</code> <p>The callable or type used to convert a value.</p> required <p>Returns:</p> Type Description <code>RowExpr</code> <p>A new row expression that converts the selected value.</p>"},{"location":"reference/expressions/#fpstreams.RowExpr.isin","title":"isin","text":"<pre><code>isin(values: Iterable[Any]) -&gt; RowExpr\n</code></pre> <p>Test whether the produced value belongs to values.</p> <p>Parameters:</p> Name Type Description Default <code>values</code> <code>Iterable[Any]</code> <p>The values consumed by this operation.</p> required <p>Returns:</p> Type Description <code>RowExpr</code> <p>A boolean row expression testing membership.</p>"},{"location":"reference/expressions/#fpstreams.RowExpr.is_null","title":"is_null","text":"<pre><code>is_null() -&gt; RowExpr\n</code></pre> <p>Test whether the produced value is None.</p> <p>Returns:</p> Type Description <code>RowExpr</code> <p>A boolean row expression testing for <code>None</code>.</p>"},{"location":"reference/expressions/#fpstreams.RowExpr.is_not_null","title":"is_not_null","text":"<pre><code>is_not_null() -&gt; RowExpr\n</code></pre> <p>Test whether the produced value is not None.</p> <p>Returns:</p> Type Description <code>RowExpr</code> <p>A boolean row expression testing for a non-<code>None</code> value.</p>"},{"location":"reference/expressions/#fpstreams.RowExpr.fill_null","title":"fill_null","text":"<pre><code>fill_null(value: object) -&gt; RowExpr\n</code></pre> <p>Replace a None result with value.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>object</code> <p>The value consumed by this operation.</p> required <p>Returns:</p> Type Description <code>RowExpr</code> <p>A new row expression with the fallback value.</p>"},{"location":"reference/expressions/#fpstreams.RowExpr.coalesce","title":"coalesce","text":"<pre><code>coalesce(*fallbacks: object) -&gt; RowExpr\n</code></pre> <p>Return the first non-None result from this expression and fallbacks.</p> <p>Parameters:</p> Name Type Description Default <code>*fallbacks</code> <code>object</code> <p>Fallback expressions checked from left to right.</p> <code>()</code> <p>Returns:</p> Type Description <code>RowExpr</code> <p>A new row expression returning the first non-<code>None</code> value.</p>"},{"location":"reference/expressions/#fpstreams.RowExpr.lower","title":"lower","text":"<pre><code>lower() -&gt; RowExpr\n</code></pre> <p>Convert the produced string to lowercase.</p> <p>Returns:</p> Type Description <code>RowExpr</code> <p>A new row expression producing lowercase strings.</p>"},{"location":"reference/expressions/#fpstreams.RowExpr.upper","title":"upper","text":"<pre><code>upper() -&gt; RowExpr\n</code></pre> <p>Convert the produced string to uppercase.</p> <p>Returns:</p> Type Description <code>RowExpr</code> <p>A new row expression producing uppercase strings.</p>"},{"location":"reference/expressions/#fpstreams.RowExpr.strip","title":"strip","text":"<pre><code>strip() -&gt; RowExpr\n</code></pre> <p>Remove leading and trailing whitespace from the produced string.</p> <p>Returns:</p> Type Description <code>RowExpr</code> <p>A new row expression producing stripped strings.</p>"},{"location":"reference/expressions/#fpstreams.RowExpr.contains","title":"contains","text":"<pre><code>contains(value: object) -&gt; RowExpr\n</code></pre> <p>Test whether value occurs in the produced container.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>object</code> <p>The value consumed by this operation.</p> required <p>Returns:</p> Type Description <code>RowExpr</code> <p>A boolean row expression testing containment.</p>"},{"location":"reference/flow/","title":"Flow","text":"<p><code>Flow[T]</code> is the synchronous lazy pipeline. Transformations return a new flow; terminal methods execute its plan.</p> <p>Use <code>flow(source)</code> for an existing iterable and <code>flow.defer(factory)</code> when every execution must create a fresh iterable.</p>"},{"location":"reference/flow/#explaining-terminal-execution","title":"Explaining terminal execution","text":"<p><code>explain()</code> defaults to ordinary iteration. Pass a terminal name when you want to inspect <code>to_list()</code>, <code>count()</code>, <code>sum()</code>, statistics, aggregation, or a short-circuiting terminal. The same planner is used by the explanation and the terminal itself.</p> <pre><code>from fpstreams import flow\n\nflow([1, 2, 3]).explain(terminal=\"count\").to_dict()\n</code></pre> <p>The actual result is:</p> <pre><code>{\n    \"terminal\": \"count\",\n    \"source\": {\"reiterable\": True, \"exact_size\": 3, \"ordered\": True},\n    \"requested_engine\": \"auto\",\n    \"selected_engine\": \"python\",\n    \"streaming_engine\": \"python\",\n    \"materializing_engine\": \"python\",\n    \"selection_reason\": \"identity list/tuple stays in Python to avoid a type scan and Rust copy\",\n    \"data_movement\": {\n        \"scans_source\": False,\n        \"copies_source\": False,\n        \"materializes\": False,\n    },\n    \"complexity\": \"O(1)\",\n    \"operations\": [],\n    \"stages\": [],\n}\n</code></pre> <p>An identity list or tuple remains in Python under <code>auto</code> when a terminal would otherwise scan and copy it. An identity range can still use native numeric reduction. Exact-size <code>count()</code> is O(1) only for an unchanged, safely reiterable source; operations and one-shot inputs are consumed normally.</p>"},{"location":"reference/flow/#csv-safety","title":"CSV safety","text":"<p><code>to_csv(..., spreadsheet_safe=False)</code> preserves raw values for machine interchange. Set it to <code>True</code> when untrusted strings will be opened in Excel, Sheets, or similar software. Suspect formula prefixes are neutralized with a leading single quote. The method writes the file and returns <code>None</code>.</p>"},{"location":"reference/flow/#creating-a-flow","title":"Creating a flow","text":"Call Behavior <code>flow(source)</code> Wrap an iterable without reading it immediately <code>flow.defer(factory)</code> Call the factory for each execution <code>flow.empty()</code> Create a flow with no items <code>flow.of_nullable(value)</code> Emit one value, or nothing when it is <code>None</code> <code>flow.iterate(seed, function)</code> Repeatedly derive the next value from the previous one <code>flow.generate(supplier)</code> Call a supplier for every emitted value <code>flow.concat(*sources)</code> Read several sources in order"},{"location":"reference/flow/#methods","title":"Methods","text":""},{"location":"reference/flow/#fpstreams.Flow","title":"fpstreams.Flow","text":"<p>               Bases: <code>FlowTerminalsMixin[T]</code>, <code>Generic[T]</code></p> <p>A lazy description of work over an iterable source.</p>"},{"location":"reference/flow/#fpstreams.Flow.to_list","title":"to_list","text":"<pre><code>to_list() -&gt; list[T]\n</code></pre> <p>Execute the pipeline and collect its items in a list.</p> <p>Returns:</p> Type Description <code>list[T]</code> <p>A list containing the consumed results in encounter order.</p>"},{"location":"reference/flow/#fpstreams.Flow.to_tuple","title":"to_tuple","text":"<pre><code>to_tuple() -&gt; tuple[T, ...]\n</code></pre> <p>Execute the pipeline and collect its items in a tuple.</p> <p>Returns:</p> Type Description <code>tuple[T, ...]</code> <p>A tuple containing the resulting values.</p>"},{"location":"reference/flow/#fpstreams.Flow.to_set","title":"to_set","text":"<pre><code>to_set() -&gt; set[T]\n</code></pre> <p>Execute the pipeline and collect distinct hashable items.</p> <p>Returns:</p> Type Description <code>set[T]</code> <p>A set containing the distinct resulting values.</p>"},{"location":"reference/flow/#fpstreams.Flow.to_pandas","title":"to_pandas","text":"<pre><code>to_pandas(columns: Iterable[str] | None = None) -&gt; Any\n</code></pre> <p>Execute the pipeline and build a pandas DataFrame.</p> <p>Parameters:</p> Name Type Description Default <code>columns</code> <code>Iterable[str] | None</code> <p>The columns or column mapping used by the operation.</p> <code>None</code> <p>Returns:</p> Type Description <code>Any</code> <p>A pandas <code>DataFrame</code> containing the emitted items.</p>"},{"location":"reference/flow/#fpstreams.Flow.to_numpy","title":"to_numpy","text":"<pre><code>to_numpy(dtype: Any = None) -&gt; Any\n</code></pre> <p>Execute the pipeline and build a NumPy array.</p> <p>Parameters:</p> Name Type Description Default <code>dtype</code> <code>Any</code> <p>The optional NumPy data type used for the resulting array.</p> <code>None</code> <p>Returns:</p> Type Description <code>Any</code> <p>A NumPy array containing the emitted items.</p>"},{"location":"reference/flow/#fpstreams.Flow.to_csv","title":"to_csv","text":"<pre><code>to_csv(\n    path: str | PathLike[str],\n    *,\n    header: Iterable[str] | None = None,\n    encoding: str = \"utf-8\",\n    spreadsheet_safe: bool = False,\n) -&gt; None\n</code></pre> <p>Execute the pipeline and stream its items to a CSV file.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str | PathLike[str]</code> <p>The filesystem path to read from or write to.</p> required <code>header</code> <code>Iterable[str] | None</code> <p>The CSV header policy or explicit field names.</p> <code>None</code> <code>encoding</code> <code>str</code> <p>The text encoding used to open the file.</p> <code>'utf-8'</code> <code>spreadsheet_safe</code> <code>bool</code> <p>Whether to neutralize cells that spreadsheet programs may execute.</p> <code>False</code>"},{"location":"reference/flow/#fpstreams.Flow.to_json","title":"to_json","text":"<pre><code>to_json(\n    path: str | PathLike[str],\n    *,\n    encoding: str = \"utf-8\",\n    ensure_ascii: bool = False,\n    default: Callable[[Any], Any] | None = None,\n) -&gt; None\n</code></pre> <p>Execute the pipeline and stream its items to one JSON array.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str | PathLike[str]</code> <p>The filesystem path to read from or write to.</p> required <code>encoding</code> <code>str</code> <p>The text encoding used to open the file.</p> <code>'utf-8'</code> <code>ensure_ascii</code> <code>bool</code> <p>Whether JSON output escapes non-ASCII characters.</p> <code>False</code> <code>default</code> <code>Callable[[Any], Any] | None</code> <p>The value returned when no matching item is available.</p> <code>None</code>"},{"location":"reference/flow/#fpstreams.Flow.describe","title":"describe","text":"<pre><code>describe() -&gt; dict[str, int | float]\n</code></pre> <p>Return count and one-pass summary statistics for numeric items.</p> <p>Returns:</p> Type Description <code>dict[str, int | float]</code> <p>A dictionary containing the computed keys and values.</p>"},{"location":"reference/flow/#fpstreams.Flow.aggregate","title":"aggregate","text":"<pre><code>aggregate(**aggregations: Aggregator) -&gt; dict[str, Any]\n</code></pre> <p>Compute several named aggregations while traversing the flow once.</p> <p>All named aggregators are updated during the same source traversal.</p> <p>Parameters:</p> Name Type Description Default <code>**aggregations</code> <code>Aggregator</code> <p>Named aggregators evaluated during the same traversal.</p> <code>{}</code> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>A dictionary containing the computed keys and values.</p>"},{"location":"reference/flow/#fpstreams.Flow.collect","title":"collect","text":"<pre><code>collect(\n    collector: Callable[[Iterable[T]], C]\n    | Collector[T, Any, C]\n    | None = None,\n    /,\n    **collectors: Collector[T, Any, Any],\n) -&gt; C | dict[str, Any]\n</code></pre> <p>Reduce the pipeline with one Collector or named Collectors.</p> <p>One collector returns its finished value. Named collectors share one source traversal and return a dictionary.</p> <p>Parameters:</p> Name Type Description Default <code>collector</code> <code>Callable[[Iterable[T]], C] | Collector[T, Any, C] | None</code> <p>The collector used to reduce input items.</p> <code>None</code> <code>**collectors</code> <code>Collector[T, Any, Any]</code> <p>Named collectors evaluated during the same traversal.</p> <code>{}</code> <p>Returns:</p> Type Description <code>C | dict[str, Any]</code> <p>The collector result, or a dictionary of results for named collectors.</p>"},{"location":"reference/flow/#fpstreams.Flow.join","title":"join","text":"<pre><code>join(separator: str = '') -&gt; str\n</code></pre> <p>Convert items to strings and join them with separator.</p> <p>This is a string terminal operation; it consumes the flow and does not perform a relational join.</p> <p>Parameters:</p> Name Type Description Default <code>separator</code> <code>str</code> <p>The string inserted between adjacent string representations.</p> <code>''</code> <p>Returns:</p> Type Description <code>str</code> <p>One string containing every item separated by <code>separator</code>.</p>"},{"location":"reference/flow/#fpstreams.Flow.for_each","title":"for_each","text":"<pre><code>for_each(action: Callable[[T], Any]) -&gt; None\n</code></pre> <p>Execute action once for every item.</p> <p>Parameters:</p> Name Type Description Default <code>action</code> <code>Callable[[T], Any]</code> <p>The side-effecting callable invoked for each matching item.</p> required"},{"location":"reference/flow/#fpstreams.Flow.partition","title":"partition","text":"<pre><code>partition(\n    predicate: Callable[[T], bool],\n) -&gt; tuple[list[T], list[T]]\n</code></pre> <p>Collect matching and non-matching items in separate lists.</p> <p>Parameters:</p> Name Type Description Default <code>predicate</code> <code>Callable[[T], bool]</code> <p>A callable that decides whether an item matches.</p> required <p>Returns:</p> Type Description <code>tuple[list[T], list[T]]</code> <p>A tuple containing the resulting values.</p>"},{"location":"reference/flow/#fpstreams.Flow.partition_results","title":"partition_results","text":"<pre><code>partition_results() -&gt; tuple[list[Any], list[Exception]]\n</code></pre> <p>Separate Result values into successes and failures.</p> <p>Returns:</p> Type Description <code>tuple[list[Any], list[Exception]]</code> <p>A tuple containing the resulting values.</p>"},{"location":"reference/flow/#fpstreams.Flow.to_async","title":"to_async","text":"<pre><code>to_async() -&gt; Any\n</code></pre> <p>View this synchronous pipeline as an AsyncFlow.</p> <p>Returns:</p> Type Description <code>Any</code> <p>An <code>AsyncFlow</code> that yields the same items.</p>"},{"location":"reference/flow/#fpstreams.Flow.first","title":"first","text":"<pre><code>first(default: Any = _MISSING) -&gt; T | Any\n</code></pre> <p>Return the first item without consuming an unnecessary tail.</p> <p>Parameters:</p> Name Type Description Default <code>default</code> <code>Any</code> <p>The value returned when no matching item is available.</p> <code>_MISSING</code> <p>Returns:</p> Type Description <code>T | Any</code> <p>The first item, or <code>default</code> when the flow is empty.</p> <p>Raises:</p> Type Description <code>EmptyFlowError</code> <p>If the flow is empty and no default is supplied.</p>"},{"location":"reference/flow/#fpstreams.Flow.last","title":"last","text":"<pre><code>last(default: Any = _MISSING) -&gt; T | Any\n</code></pre> <p>Return the last item, or default when the flow is empty.</p> <p>Parameters:</p> Name Type Description Default <code>default</code> <code>Any</code> <p>The value returned when no matching item is available.</p> <code>_MISSING</code> <p>Returns:</p> Type Description <code>T | Any</code> <p>The last item, or <code>default</code> when the flow is empty.</p>"},{"location":"reference/flow/#fpstreams.Flow.find","title":"find","text":"<pre><code>find(\n    predicate: Callable[[T], Any], default: Any = _MISSING\n) -&gt; T | Any\n</code></pre> <p>Return the first matching item, a default, or raise EmptyFlowError.</p> <p>Parameters:</p> Name Type Description Default <code>predicate</code> <code>Callable[[T], Any]</code> <p>A callable that decides whether an item matches.</p> required <code>default</code> <code>Any</code> <p>The value returned when no matching item is available.</p> <code>_MISSING</code> <p>Returns:</p> Type Description <code>T | Any</code> <p>The first matching item, or <code>default</code> when no item matches.</p>"},{"location":"reference/flow/#fpstreams.Flow.find_index","title":"find_index","text":"<pre><code>find_index(predicate: Callable[[T], Any]) -&gt; int | None\n</code></pre> <p>Return the index of the first matching item, or None.</p> <p>Parameters:</p> Name Type Description Default <code>predicate</code> <code>Callable[[T], Any]</code> <p>A callable that decides whether an item matches.</p> required <p>Returns:</p> Type Description <code>int | None</code> <p>The matching or computed value, or <code>None</code> when unavailable.</p>"},{"location":"reference/flow/#fpstreams.Flow.index_of","title":"index_of","text":"<pre><code>index_of(value: T) -&gt; int | None\n</code></pre> <p>Return the index of the first equal value, or None.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>T</code> <p>The value consumed by this operation.</p> required <p>Returns:</p> Type Description <code>int | None</code> <p>The matching or computed value, or <code>None</code> when unavailable.</p>"},{"location":"reference/flow/#fpstreams.Flow.nth","title":"nth","text":"<pre><code>nth(index: int, default: Any = _MISSING) -&gt; T | Any\n</code></pre> <p>Return the item at a positive or negative index.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>int</code> <p>The zero-based item or field position to select.</p> required <code>default</code> <code>Any</code> <p>The value returned when no matching item is available.</p> <code>_MISSING</code> <p>Returns:</p> Type Description <code>T | Any</code> <p>The selected item, or <code>default</code> when the index is out of range.</p>"},{"location":"reference/flow/#fpstreams.Flow.count","title":"count","text":"<pre><code>count() -&gt; int\n</code></pre> <p>Count all items produced by the pipeline.</p> <p>Returns:</p> Type Description <code>int</code> <p>The number of matching input items.</p>"},{"location":"reference/flow/#fpstreams.Flow.sum","title":"sum","text":"<pre><code>sum(start: Any = 0) -&gt; Any\n</code></pre> <p>Add all items, starting with start.</p> <p>Parameters:</p> Name Type Description Default <code>start</code> <code>Any</code> <p>The first index, numeric value, or additive identity to use.</p> <code>0</code> <p>Returns:</p> Type Description <code>Any</code> <p>The total of <code>start</code> and all emitted items.</p>"},{"location":"reference/flow/#fpstreams.Flow.mean","title":"mean","text":"<pre><code>mean() -&gt; float | None\n</code></pre> <p>Return the arithmetic mean, or None for an empty flow.</p> <p>Returns:</p> Type Description <code>float | None</code> <p>The matching or computed value, or <code>None</code> when unavailable.</p>"},{"location":"reference/flow/#fpstreams.Flow.variance","title":"variance","text":"<pre><code>variance(*, ddof: int = 1) -&gt; float | None\n</code></pre> <p>Return the variance, or None when too few values are available.</p> <p>Parameters:</p> Name Type Description Default <code>ddof</code> <code>int</code> <p>Delta degrees of freedom used in the variance divisor.</p> <code>1</code> <p>Returns:</p> Type Description <code>float | None</code> <p>The matching or computed value, or <code>None</code> when unavailable.</p>"},{"location":"reference/flow/#fpstreams.Flow.std","title":"std","text":"<pre><code>std(*, ddof: int = 1) -&gt; float | None\n</code></pre> <p>Return the standard deviation, or None when too few values are available.</p> <p>Parameters:</p> Name Type Description Default <code>ddof</code> <code>int</code> <p>Delta degrees of freedom used in the variance divisor.</p> <code>1</code> <p>Returns:</p> Type Description <code>float | None</code> <p>The matching or computed value, or <code>None</code> when unavailable.</p>"},{"location":"reference/flow/#fpstreams.Flow.min","title":"min","text":"<pre><code>min(*, key: Callable[[T], Any] | None = None) -&gt; T\n</code></pre> <p>Return the smallest item and raise on an empty flow.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>Callable[[T], Any] | None</code> <p>The callable or selector used to derive a key.</p> <code>None</code> <p>Returns:</p> Type Description <code>T</code> <p>The smallest item according to <code>key</code>.</p>"},{"location":"reference/flow/#fpstreams.Flow.max","title":"max","text":"<pre><code>max(*, key: Callable[[T], Any] | None = None) -&gt; T\n</code></pre> <p>Return the largest item and raise on an empty flow.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>Callable[[T], Any] | None</code> <p>The callable or selector used to derive a key.</p> <code>None</code> <p>Returns:</p> Type Description <code>T</code> <p>The largest item according to <code>key</code>.</p>"},{"location":"reference/flow/#fpstreams.Flow.top","title":"top","text":"<pre><code>top(count: int, *, key: Selector | None = None) -&gt; list[T]\n</code></pre> <p>Return up to count largest items without sorting the entire result.</p> <p>Parameters:</p> Name Type Description Default <code>count</code> <code>int</code> <p>The requested number of items.</p> required <code>key</code> <code>Selector | None</code> <p>The callable or selector used to derive a key.</p> <code>None</code> <p>Returns:</p> Type Description <code>list[T]</code> <p>A list containing the consumed results in encounter order.</p>"},{"location":"reference/flow/#fpstreams.Flow.bottom","title":"bottom","text":"<pre><code>bottom(\n    count: int, *, key: Selector | None = None\n) -&gt; list[T]\n</code></pre> <p>Return up to count smallest items without sorting the entire result.</p> <p>Parameters:</p> Name Type Description Default <code>count</code> <code>int</code> <p>The requested number of items.</p> required <code>key</code> <code>Selector | None</code> <p>The callable or selector used to derive a key.</p> <code>None</code> <p>Returns:</p> Type Description <code>list[T]</code> <p>A list containing the consumed results in encounter order.</p>"},{"location":"reference/flow/#fpstreams.Flow.minmax","title":"minmax","text":"<pre><code>minmax(*, key: Selector | None = None) -&gt; tuple[T, T]\n</code></pre> <p>Return the smallest and largest items in one traversal.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>Selector | None</code> <p>The callable or selector used to derive a key.</p> <code>None</code> <p>Returns:</p> Type Description <code>tuple[T, T]</code> <p>A tuple containing the resulting values.</p>"},{"location":"reference/flow/#fpstreams.Flow.any","title":"any","text":"<pre><code>any(predicate: Callable[[T], bool] = bool) -&gt; bool\n</code></pre> <p>Return whether at least one item satisfies predicate.</p> <p>Parameters:</p> Name Type Description Default <code>predicate</code> <code>Callable[[T], bool]</code> <p>A callable that decides whether an item matches.</p> <code>bool</code> <p>Returns:</p> Type Description <code>bool</code> <p>Whether the condition described above is true.</p>"},{"location":"reference/flow/#fpstreams.Flow.all","title":"all","text":"<pre><code>all(predicate: Callable[[T], bool] = bool) -&gt; bool\n</code></pre> <p>Return whether every item satisfies predicate.</p> <p>Parameters:</p> Name Type Description Default <code>predicate</code> <code>Callable[[T], bool]</code> <p>A callable that decides whether an item matches.</p> <code>bool</code> <p>Returns:</p> Type Description <code>bool</code> <p>Whether the condition described above is true.</p>"},{"location":"reference/flow/#fpstreams.Flow.none","title":"none","text":"<pre><code>none(predicate: Callable[[T], bool] = bool) -&gt; bool\n</code></pre> <p>Return whether no item satisfies predicate.</p> <p>Parameters:</p> Name Type Description Default <code>predicate</code> <code>Callable[[T], bool]</code> <p>A callable that decides whether an item matches.</p> <code>bool</code> <p>Returns:</p> Type Description <code>bool</code> <p>Whether the condition described above is true.</p>"},{"location":"reference/flow/#fpstreams.Flow.reduce","title":"reduce","text":"<pre><code>reduce(\n    function: Callable[[Any, T], Any],\n    initial: Any = _MISSING,\n) -&gt; Any\n</code></pre> <p>Combine items from left to right with an optional initial value.</p> <p>Parameters:</p> Name Type Description Default <code>function</code> <code>Callable[[Any, T], Any]</code> <p>The callable applied by this operation.</p> required <code>initial</code> <code>Any</code> <p>The initial accumulator value. When omitted, the first item is used where supported.</p> <code>_MISSING</code> <p>Returns:</p> Type Description <code>Any</code> <p>The final left-to-right accumulator.</p>"},{"location":"reference/flow/#fpstreams.Flow.reduce_right","title":"reduce_right","text":"<pre><code>reduce_right(\n    function: Callable[[T, Any], Any],\n    initial: Any = _MISSING,\n    *,\n    max_items: int | None = None,\n) -&gt; Any\n</code></pre> <p>Combine buffered items from right to left.</p> <p>Parameters:</p> Name Type Description Default <code>function</code> <code>Callable[[T, Any], Any]</code> <p>The callable applied by this operation.</p> required <code>initial</code> <code>Any</code> <p>The initial accumulator value. When omitted, the first item is used where supported.</p> <code>_MISSING</code> <code>max_items</code> <code>int | None</code> <p>The maximum number of source items allowed in the right-side buffer.</p> <code>None</code> <p>Returns:</p> Type Description <code>Any</code> <p>The final right-to-left accumulator.</p>"},{"location":"reference/flow/#fpstreams.Flow.reduce_by","title":"reduce_by","text":"<pre><code>reduce_by(\n    key: Selector,\n    function: Callable[[R, T], R],\n    *,\n    initializer: Callable[[], R],\n) -&gt; dict[Any, R]\n</code></pre> <p>Reduce items independently for each selected key.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>Selector</code> <p>The callable or selector used to derive a key.</p> required <code>function</code> <code>Callable[[R, T], R]</code> <p>The callable applied by this operation.</p> required <code>initializer</code> <code>Callable[[], R]</code> <p>A zero-argument callable that creates fresh mutable state.</p> required <p>Returns:</p> Type Description <code>dict[Any, R]</code> <p>A dictionary containing the computed keys and values.</p>"},{"location":"reference/flow/#fpstreams.Flow.frequencies","title":"frequencies","text":"<pre><code>frequencies(key: Selector | None = None) -&gt; dict[Any, int]\n</code></pre> <p>Count occurrences of values or selected keys.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>Selector | None</code> <p>The callable or selector used to derive a key.</p> <code>None</code> <p>Returns:</p> Type Description <code>dict[Any, int]</code> <p>A dictionary containing the computed keys and values.</p>"},{"location":"reference/flow/#fpstreams.Flow.of","title":"of  <code>staticmethod</code>","text":"<pre><code>of(*items: R) -&gt; Flow[R]\n</code></pre> <p>Create a flow from positional items.</p> <p>Parameters:</p> Name Type Description Default <code>*items</code> <code>R</code> <p>Items emitted by the new pipeline.</p> <code>()</code> <p>Returns:</p> Type Description <code>Flow[R]</code> <p>A new lazy <code>Flow</code> over the supplied source or values.</p>"},{"location":"reference/flow/#fpstreams.Flow.from_iterable","title":"from_iterable  <code>staticmethod</code>","text":"<pre><code>from_iterable(source: Iterable[R]) -&gt; Flow[R]\n</code></pre> <p>Create a flow over an iterable source.</p> <p>Parameters:</p> Name Type Description Default <code>source</code> <code>Iterable[R]</code> <p>The iterable, async iterable, or data source to read lazily.</p> required <p>Returns:</p> Type Description <code>Flow[R]</code> <p>A new lazy <code>Flow</code> over the supplied source or values.</p>"},{"location":"reference/flow/#fpstreams.Flow.empty","title":"empty  <code>staticmethod</code>","text":"<pre><code>empty() -&gt; Flow[Any]\n</code></pre> <p>Create a flow that emits no items.</p> <p>Returns:</p> Type Description <code>Flow[Any]</code> <p>A new lazy <code>Flow</code> over the supplied source or values.</p>"},{"location":"reference/flow/#fpstreams.Flow.of_nullable","title":"of_nullable  <code>staticmethod</code>","text":"<pre><code>of_nullable(value: R | None) -&gt; Flow[R]\n</code></pre> <p>Create an empty flow for None, otherwise emit the value once.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>R | None</code> <p>The value consumed by this operation.</p> required <p>Returns:</p> Type Description <code>Flow[R]</code> <p>A new lazy <code>Flow</code> over the supplied source or values.</p>"},{"location":"reference/flow/#fpstreams.Flow.iterate","title":"iterate  <code>staticmethod</code>","text":"<pre><code>iterate(seed: R, function: Callable[[R], R]) -&gt; Flow[R]\n</code></pre> <p>Emit seed, then repeatedly apply function to the previous value.</p> <p>Parameters:</p> Name Type Description Default <code>seed</code> <code>R</code> <p>The first value emitted or used to initialize the sequence.</p> required <code>function</code> <code>Callable[[R], R]</code> <p>The callable applied by this operation.</p> required <p>Returns:</p> Type Description <code>Flow[R]</code> <p>A new lazy <code>Flow</code> over the supplied source or values.</p>"},{"location":"reference/flow/#fpstreams.Flow.generate","title":"generate  <code>staticmethod</code>","text":"<pre><code>generate(supplier: Callable[[], R]) -&gt; Flow[R]\n</code></pre> <p>Create an infinite flow by calling supplier for each item.</p> <p>Parameters:</p> Name Type Description Default <code>supplier</code> <code>Callable[[], R]</code> <p>A zero-argument callable that supplies a value or iterable.</p> required <p>Returns:</p> Type Description <code>Flow[R]</code> <p>A new lazy <code>Flow</code> over the supplied source or values.</p>"},{"location":"reference/flow/#fpstreams.Flow.map","title":"map","text":"<pre><code>map(function: Callable[[T], R]) -&gt; Flow[R]\n</code></pre> <p>Apply function to each item lazily.</p> <p><code>map</code> is lazy: the callable runs only when a terminal operation or iteration consumes the flow.</p> <p>Parameters:</p> Name Type Description Default <code>function</code> <code>Callable[[T], R]</code> <p>The callable applied by this operation.</p> required <p>Returns:</p> Type Description <code>Flow[R]</code> <p>A new lazy <code>Flow</code> representing this operation.</p>"},{"location":"reference/flow/#fpstreams.Flow.map_parallel","title":"map_parallel","text":"<pre><code>map_parallel(\n    function: Callable[[T], R],\n    *,\n    workers: int | None = None,\n    backend: ParallelBackend = \"thread\",\n    ordered: bool = True,\n    buffer: int | None = None,\n) -&gt; Flow[R]\n</code></pre> <p>Map items in a bounded thread or process pool.</p> <p>Only a bounded number of tasks are submitted at once, preventing a slow consumer from creating unbounded work.</p> <p>Parameters:</p> Name Type Description Default <code>function</code> <code>Callable[[T], R]</code> <p>The callable applied by this operation.</p> required <code>workers</code> <code>int | None</code> <p>The number of worker threads or processes. None chooses a runtime default.</p> <code>None</code> <code>backend</code> <code>ParallelBackend</code> <p>The parallel backend: thread or process.</p> <code>'thread'</code> <code>ordered</code> <code>bool</code> <p>Whether results must preserve source encounter order.</p> <code>True</code> <code>buffer</code> <code>int | None</code> <p>The maximum number of submitted tasks awaiting consumption.</p> <code>None</code> <p>Returns:</p> Type Description <code>Flow[R]</code> <p>A new lazy <code>Flow</code> representing this operation.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If workers or buffer is less than one, or backend is unsupported.</p>"},{"location":"reference/flow/#fpstreams.Flow.parallel","title":"parallel","text":"<pre><code>parallel(\n    *,\n    workers: int | None = None,\n    backend: ParallelBackend = \"process\",\n    ordered: bool = True,\n    buffer: int | None = None,\n) -&gt; Flow[T]\n</code></pre> <p>Apply parallel settings to map operations added after this call.</p> <p>Parameters:</p> Name Type Description Default <code>workers</code> <code>int | None</code> <p>The number of worker threads or processes. None chooses a runtime default.</p> <code>None</code> <code>backend</code> <code>ParallelBackend</code> <p>The parallel backend: thread or process.</p> <code>'process'</code> <code>ordered</code> <code>bool</code> <p>Whether results must preserve source encounter order.</p> <code>True</code> <code>buffer</code> <code>int | None</code> <p>The maximum number of submitted tasks awaiting consumption.</p> <code>None</code> <p>Returns:</p> Type Description <code>Flow[T]</code> <p>A new lazy <code>Flow</code> representing this operation.</p>"},{"location":"reference/flow/#fpstreams.Flow.sequential","title":"sequential","text":"<pre><code>sequential() -&gt; Flow[T]\n</code></pre> <p>Return a flow whose following maps run sequentially.</p> <p>Returns:</p> Type Description <code>Flow[T]</code> <p>A new lazy <code>Flow</code> representing this operation.</p>"},{"location":"reference/flow/#fpstreams.Flow.tap","title":"tap","text":"<pre><code>tap(function: Callable[[T], None]) -&gt; Flow[T]\n</code></pre> <p>Run a side effect for each item while passing the item through.</p> <p>Parameters:</p> Name Type Description Default <code>function</code> <code>Callable[[T], None]</code> <p>The callable applied by this operation.</p> required <p>Returns:</p> Type Description <code>Flow[T]</code> <p>A new lazy <code>Flow</code> representing this operation.</p>"},{"location":"reference/flow/#fpstreams.Flow.filter","title":"filter","text":"<pre><code>filter(predicate: Callable[[T], Any]) -&gt; Flow[T]\n</code></pre> <p>Keep items for which predicate returns a truthy value.</p> <p><code>filter</code> is lazy and preserves encounter order; the predicate runs as items are requested.</p> <p>Parameters:</p> Name Type Description Default <code>predicate</code> <code>Callable[[T], Any]</code> <p>A callable that decides whether an item matches.</p> required <p>Returns:</p> Type Description <code>Flow[T]</code> <p>A new lazy <code>Flow</code> representing this operation.</p>"},{"location":"reference/flow/#fpstreams.Flow.reject","title":"reject","text":"<pre><code>reject(predicate: Callable[[T], Any]) -&gt; Flow[T]\n</code></pre> <p>Drop items for which predicate returns a truthy value.</p> <p>Parameters:</p> Name Type Description Default <code>predicate</code> <code>Callable[[T], Any]</code> <p>A callable that decides whether an item matches.</p> required <p>Returns:</p> Type Description <code>Flow[T]</code> <p>A new lazy <code>Flow</code> representing this operation.</p>"},{"location":"reference/flow/#fpstreams.Flow.compact","title":"compact","text":"<pre><code>compact(selector: Selector | None = None) -&gt; Flow[T]\n</code></pre> <p>Drop None values, optionally selected from each item.</p> <p>Parameters:</p> Name Type Description Default <code>selector</code> <code>Selector | None</code> <p>A callable, field name, index, path, or expression used to select a value.</p> <code>None</code> <p>Returns:</p> Type Description <code>Flow[T]</code> <p>A new lazy <code>Flow</code> representing this operation.</p>"},{"location":"reference/flow/#fpstreams.Flow.flat_map","title":"flat_map","text":"<pre><code>flat_map(function: Callable[[T], Iterable[R]]) -&gt; Flow[R]\n</code></pre> <p>Map each item to an iterable and emit the iterable contents.</p> <p>Each input may emit zero or more output items. Nested iterables are consumed lazily in encounter order.</p> <p>Parameters:</p> Name Type Description Default <code>function</code> <code>Callable[[T], Iterable[R]]</code> <p>The callable applied by this operation.</p> required <p>Returns:</p> Type Description <code>Flow[R]</code> <p>A new lazy <code>Flow</code> representing this operation.</p>"},{"location":"reference/flow/#fpstreams.Flow.filter_map","title":"filter_map","text":"<pre><code>filter_map(function: Callable[[T], R | None]) -&gt; Flow[R]\n</code></pre> <p>Map items and discard results equal to None.</p> <p>Parameters:</p> Name Type Description Default <code>function</code> <code>Callable[[T], R | None]</code> <p>The callable applied by this operation.</p> required <p>Returns:</p> Type Description <code>Flow[R]</code> <p>A new lazy <code>Flow</code> representing this operation.</p>"},{"location":"reference/flow/#fpstreams.Flow.pluck","title":"pluck","text":"<pre><code>pluck(selector: Selector) -&gt; Flow[Any]\n</code></pre> <p>Select one field, index, attribute, or nested path from each item.</p> <p>Parameters:</p> Name Type Description Default <code>selector</code> <code>Selector</code> <p>A callable, field name, index, path, or expression used to select a value.</p> required <p>Returns:</p> Type Description <code>Flow[Any]</code> <p>A new lazy <code>Flow</code> representing this operation.</p>"},{"location":"reference/flow/#fpstreams.Flow.unique","title":"unique","text":"<pre><code>unique() -&gt; Flow[T]\n</code></pre> <p>Keep the first occurrence of each value in encounter order.</p> <p>Returns:</p> Type Description <code>Flow[T]</code> <p>A new lazy <code>Flow</code> representing this operation.</p>"},{"location":"reference/flow/#fpstreams.Flow.distinct","title":"distinct","text":"<pre><code>distinct() -&gt; Flow[T]\n</code></pre> <p>Keep the first occurrence of each value in encounter order.</p> <p>Returns:</p> Type Description <code>Flow[T]</code> <p>A new lazy <code>Flow</code> representing this operation.</p>"},{"location":"reference/flow/#fpstreams.Flow.unique_by","title":"unique_by","text":"<pre><code>unique_by(selector: Selector) -&gt; Flow[T]\n</code></pre> <p>Keep the first item for each selected key.</p> <p>Parameters:</p> Name Type Description Default <code>selector</code> <code>Selector</code> <p>A callable, field name, index, path, or expression used to select a value.</p> required <p>Returns:</p> Type Description <code>Flow[T]</code> <p>A new lazy <code>Flow</code> representing this operation.</p>"},{"location":"reference/flow/#fpstreams.Flow.sort_by","title":"sort_by","text":"<pre><code>sort_by(\n    selector: Selector,\n    *,\n    reverse: bool = False,\n    buffer_size: int | None = None,\n    tempdir: str | PathLike[str] | None = None,\n) -&gt; Flow[T]\n</code></pre> <p>Sort items by a selector, optionally using bounded external storage.</p> <p>Parameters:</p> Name Type Description Default <code>selector</code> <code>Selector</code> <p>A callable, field name, index, path, or expression used to select a value.</p> required <code>reverse</code> <code>bool</code> <p>If true, produce values in descending order.</p> <code>False</code> <code>buffer_size</code> <code>int | None</code> <p>The maximum number of items held in an in-memory buffer or batch.</p> <code>None</code> <code>tempdir</code> <code>str | PathLike[str] | None</code> <p>The directory used for temporary spill files.</p> <code>None</code> <p>Returns:</p> Type Description <code>Flow[T]</code> <p>A new lazy <code>Flow</code> representing this operation.</p>"},{"location":"reference/flow/#fpstreams.Flow.sorted","title":"sorted","text":"<pre><code>sorted(\n    *,\n    key: Callable[[T], Any] | None = None,\n    reverse: bool = False,\n    buffer_size: int | None = None,\n    tempdir: str | PathLike[str] | None = None,\n) -&gt; Flow[T]\n</code></pre> <p>Sort the flow, optionally using bounded external runs.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>Callable[[T], Any] | None</code> <p>The callable or selector used to derive a key.</p> <code>None</code> <code>reverse</code> <code>bool</code> <p>If true, produce values in descending order.</p> <code>False</code> <code>buffer_size</code> <code>int | None</code> <p>The maximum number of items held in an in-memory buffer or batch.</p> <code>None</code> <code>tempdir</code> <code>str | PathLike[str] | None</code> <p>The directory used for temporary spill files.</p> <code>None</code> <p>Returns:</p> Type Description <code>Flow[T]</code> <p>A new lazy <code>Flow</code> representing this operation.</p>"},{"location":"reference/flow/#fpstreams.Flow.external_sort","title":"external_sort","text":"<pre><code>external_sort(\n    *,\n    key: Callable[[T], Any] | None = None,\n    reverse: bool = False,\n    buffer_size: int = 100000,\n    tempdir: str | PathLike[str] | None = None,\n) -&gt; Flow[T]\n</code></pre> <p>Sort with a bounded in-memory buffer and temporary files.</p> <p>Sorted runs are written to temporary files and merged lazily, keeping peak in-memory items bounded.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>Callable[[T], Any] | None</code> <p>The callable or selector used to derive a key.</p> <code>None</code> <code>reverse</code> <code>bool</code> <p>If true, produce values in descending order.</p> <code>False</code> <code>buffer_size</code> <code>int</code> <p>The maximum number of items held in an in-memory buffer or batch.</p> <code>100000</code> <code>tempdir</code> <code>str | PathLike[str] | None</code> <p>The directory used for temporary spill files.</p> <code>None</code> <p>Returns:</p> Type Description <code>Flow[T]</code> <p>A new lazy <code>Flow</code> representing this operation.</p>"},{"location":"reference/flow/#fpstreams.Flow.external_sort_by","title":"external_sort_by","text":"<pre><code>external_sort_by(\n    selector: Selector,\n    *,\n    reverse: bool = False,\n    buffer_size: int = 100000,\n    tempdir: str | PathLike[str] | None = None,\n) -&gt; Flow[T]\n</code></pre> <p>Sort by a selector using bounded memory and temporary files.</p> <p>Sorted runs are written to temporary files and merged lazily, keeping peak in-memory items bounded.</p> <p>Parameters:</p> Name Type Description Default <code>selector</code> <code>Selector</code> <p>A callable, field name, index, path, or expression used to select a value.</p> required <code>reverse</code> <code>bool</code> <p>If true, produce values in descending order.</p> <code>False</code> <code>buffer_size</code> <code>int</code> <p>The maximum number of items held in an in-memory buffer or batch.</p> <code>100000</code> <code>tempdir</code> <code>str | PathLike[str] | None</code> <p>The directory used for temporary spill files.</p> <code>None</code> <p>Returns:</p> Type Description <code>Flow[T]</code> <p>A new lazy <code>Flow</code> representing this operation.</p>"},{"location":"reference/flow/#fpstreams.Flow.chunk","title":"chunk","text":"<pre><code>chunk(size: int) -&gt; Flow[tuple[T, ...]]\n</code></pre> <p>Group consecutive items into fixed-size tuples.</p> <p>Parameters:</p> Name Type Description Default <code>size</code> <code>int</code> <p>The requested window, chunk, or batch size.</p> required <p>Returns:</p> Type Description <code>Flow[tuple[T, ...]]</code> <p>A new lazy <code>Flow</code> representing this operation.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If size is less than one.</p>"},{"location":"reference/flow/#fpstreams.Flow.batch_by_size","title":"batch_by_size","text":"<pre><code>batch_by_size(\n    max_size: int,\n    *,\n    max_count: int | None = None,\n    get_size: Callable[[T], int] = _default_item_size,\n    strict: bool = True,\n) -&gt; Flow[tuple[T, ...]]\n</code></pre> <p>Build batches constrained by item count and total measured size.</p> <p>Parameters:</p> Name Type Description Default <code>max_size</code> <code>int</code> <p>The maximum measured size allowed in one batch.</p> required <code>max_count</code> <code>int | None</code> <p>The maximum number of items emitted in one batch.</p> <code>None</code> <code>get_size</code> <code>Callable[[T], int]</code> <p>A callable that returns the measured size of one item.</p> <code>_default_item_size</code> <code>strict</code> <code>bool</code> <p>Whether invalid or empty input should raise instead of returning a fallback.</p> <code>True</code> <p>Returns:</p> Type Description <code>Flow[tuple[T, ...]]</code> <p>A new lazy <code>Flow</code> representing this operation.</p>"},{"location":"reference/flow/#fpstreams.Flow.window","title":"window","text":"<pre><code>window(size: int, *, step: int = 1) -&gt; Flow[tuple[T, ...]]\n</code></pre> <p>Emit sliding tuples of size with the requested step.</p> <p>Parameters:</p> Name Type Description Default <code>size</code> <code>int</code> <p>The requested window, chunk, or batch size.</p> required <code>step</code> <code>int</code> <p>The distance between consecutive windows or numeric increments.</p> <code>1</code> <p>Returns:</p> Type Description <code>Flow[tuple[T, ...]]</code> <p>A new lazy <code>Flow</code> representing this operation.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If size or step is less than one.</p>"},{"location":"reference/flow/#fpstreams.Flow.group_runs","title":"group_runs","text":"<pre><code>group_runs(\n    key: Selector | None = None,\n) -&gt; Flow[tuple[T, ...]]\n</code></pre> <p>Group consecutive items that share the same key.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>Selector | None</code> <p>The callable or selector used to derive a key.</p> <code>None</code> <p>Returns:</p> Type Description <code>Flow[tuple[T, ...]]</code> <p>A new lazy <code>Flow</code> representing this operation.</p>"},{"location":"reference/flow/#fpstreams.Flow.pairwise","title":"pairwise","text":"<pre><code>pairwise() -&gt; Flow[tuple[T, T]]\n</code></pre> <p>Emit each adjacent pair of items.</p> <p>Returns:</p> Type Description <code>Flow[tuple[T, T]]</code> <p>A new lazy <code>Flow</code> representing this operation.</p>"},{"location":"reference/flow/#fpstreams.Flow.pair_map","title":"pair_map","text":"<pre><code>pair_map(function: Callable[[T, T], R]) -&gt; Flow[R]\n</code></pre> <p>Apply a two-argument function to each adjacent pair.</p> <p>Parameters:</p> Name Type Description Default <code>function</code> <code>Callable[[T, T], R]</code> <p>The callable applied by this operation.</p> required <p>Returns:</p> Type Description <code>Flow[R]</code> <p>A new lazy <code>Flow</code> representing this operation.</p>"},{"location":"reference/flow/#fpstreams.Flow.enumerate","title":"enumerate","text":"<pre><code>enumerate(start: int = 0) -&gt; Flow[tuple[int, T]]\n</code></pre> <p>Pair each item with a consecutive index starting at start.</p> <p>Parameters:</p> Name Type Description Default <code>start</code> <code>int</code> <p>The first index, numeric value, or additive identity to use.</p> <code>0</code> <p>Returns:</p> Type Description <code>Flow[tuple[int, T]]</code> <p>A new lazy <code>Flow</code> representing this operation.</p>"},{"location":"reference/flow/#fpstreams.Flow.zip","title":"zip","text":"<pre><code>zip(\n    other: Iterable[U], *, strict: bool = False\n) -&gt; Flow[tuple[T, U]]\n</code></pre> <p>Pair items with another iterable until one side ends.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>Iterable[U]</code> <p>The other iterable, flow, value, or fallback used by the operation.</p> required <code>strict</code> <code>bool</code> <p>Whether invalid or empty input should raise instead of returning a fallback.</p> <code>False</code> <p>Returns:</p> Type Description <code>Flow[tuple[T, U]]</code> <p>A new lazy <code>Flow</code> representing this operation.</p>"},{"location":"reference/flow/#fpstreams.Flow.zip_longest","title":"zip_longest","text":"<pre><code>zip_longest(\n    other: Iterable[U], *, fillvalue: Any = None\n) -&gt; Flow[tuple[T | Any, U | Any]]\n</code></pre> <p>Pair with another iterable until both sides end, filling missing values.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>Iterable[U]</code> <p>The other iterable, flow, value, or fallback used by the operation.</p> required <code>fillvalue</code> <code>Any</code> <p>The value used when one side of a longest zip is exhausted.</p> <code>None</code> <p>Returns:</p> Type Description <code>Flow[tuple[T | Any, U | Any]]</code> <p>A new lazy <code>Flow</code> representing this operation.</p>"},{"location":"reference/flow/#fpstreams.Flow.intersperse","title":"intersperse","text":"<pre><code>intersperse(separator: T) -&gt; Flow[T]\n</code></pre> <p>Insert separator between consecutive items.</p> <p>Parameters:</p> Name Type Description Default <code>separator</code> <code>T</code> <p>The string inserted between adjacent string representations.</p> required <p>Returns:</p> Type Description <code>Flow[T]</code> <p>A new lazy <code>Flow</code> representing this operation.</p>"},{"location":"reference/flow/#fpstreams.Flow.concat","title":"concat","text":"<pre><code>concat(*others: Iterable[T]) -&gt; Flow[T]\n</code></pre> <p>Emit this flow followed by each supplied iterable.</p> <p>Parameters:</p> Name Type Description Default <code>*others</code> <code>Iterable[T]</code> <p>Additional sources combined with this pipeline.</p> <code>()</code> <p>Returns:</p> Type Description <code>Flow[T]</code> <p>A new lazy <code>Flow</code> representing this operation.</p>"},{"location":"reference/flow/#fpstreams.Flow.cross","title":"cross","text":"<pre><code>cross(\n    other: Iterable[U], *, max_right: int | None = None\n) -&gt; Flow[tuple[T, U]]\n</code></pre> <p>Emit the Cartesian product with a bounded or reiterable right side.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>Iterable[U]</code> <p>The other iterable, flow, value, or fallback used by the operation.</p> required <code>max_right</code> <code>int | None</code> <p>The maximum right-side size allowed when buffering is required.</p> <code>None</code> <p>Returns:</p> Type Description <code>Flow[tuple[T, U]]</code> <p>A new lazy <code>Flow</code> representing this operation.</p>"},{"location":"reference/flow/#fpstreams.Flow.scan","title":"scan","text":"<pre><code>scan(initial: R, function: Callable[[R, T], R]) -&gt; Flow[R]\n</code></pre> <p>Emit each left-to-right accumulator state.</p> <p>Unlike <code>reduce</code>, <code>scan</code> emits every intermediate accumulator state.</p> <p>Parameters:</p> Name Type Description Default <code>initial</code> <code>R</code> <p>The initial accumulator value. When omitted, the first item is used where supported.</p> required <code>function</code> <code>Callable[[R, T], R]</code> <p>The callable applied by this operation.</p> required <p>Returns:</p> Type Description <code>Flow[R]</code> <p>A new lazy <code>Flow</code> representing this operation.</p>"},{"location":"reference/flow/#fpstreams.Flow.scan_right","title":"scan_right","text":"<pre><code>scan_right(\n    initial: R,\n    function: Callable[[T, R], R],\n    *,\n    max_items: int | None = None,\n) -&gt; Flow[R]\n</code></pre> <p>Emit accumulator states while combining items from right to left.</p> <p>Parameters:</p> Name Type Description Default <code>initial</code> <code>R</code> <p>The initial accumulator value. When omitted, the first item is used where supported.</p> required <code>function</code> <code>Callable[[T, R], R]</code> <p>The callable applied by this operation.</p> required <code>max_items</code> <code>int | None</code> <p>The maximum number of source items allowed in the right-side buffer.</p> <code>None</code> <p>Returns:</p> Type Description <code>Flow[R]</code> <p>A new lazy <code>Flow</code> representing this operation.</p>"},{"location":"reference/flow/#fpstreams.Flow.gather","title":"gather","text":"<pre><code>gather(gatherer: Gatherer[T, Any, R]) -&gt; Flow[R]\n</code></pre> <p>Apply a stateful Gatherer that may emit zero or more values per item.</p> <p>A gatherer may retain state and emit zero, one, or many outputs for each input item.</p> <p>Parameters:</p> Name Type Description Default <code>gatherer</code> <code>Gatherer[T, Any, R]</code> <p>The stateful gatherer applied to this pipeline.</p> required <p>Returns:</p> Type Description <code>Flow[R]</code> <p>A new lazy <code>Flow</code> representing this operation.</p>"},{"location":"reference/flow/#fpstreams.Flow.fold","title":"fold","text":"<pre><code>fold(\n    initializer: Callable[[], R],\n    function: Callable[[R, T], R],\n) -&gt; Flow[R]\n</code></pre> <p>Consume all items into one emitted value using fresh state per iteration.</p> <p>Parameters:</p> Name Type Description Default <code>initializer</code> <code>Callable[[], R]</code> <p>A zero-argument callable that creates fresh mutable state.</p> required <code>function</code> <code>Callable[[R, T], R]</code> <p>The callable applied by this operation.</p> required <p>Returns:</p> Type Description <code>Flow[R]</code> <p>A new lazy <code>Flow</code> representing this operation.</p>"},{"location":"reference/flow/#fpstreams.Flow.prepend","title":"prepend","text":"<pre><code>prepend(*values: T) -&gt; Flow[T]\n</code></pre> <p>Emit values before the items from this flow.</p> <p>Parameters:</p> Name Type Description Default <code>*values</code> <code>T</code> <p>Values supplied to this operation in encounter order.</p> <code>()</code> <p>Returns:</p> Type Description <code>Flow[T]</code> <p>A new lazy <code>Flow</code> representing this operation.</p>"},{"location":"reference/flow/#fpstreams.Flow.append","title":"append","text":"<pre><code>append(*values: T) -&gt; Flow[T]\n</code></pre> <p>Emit values after the items from this flow.</p> <p>Parameters:</p> Name Type Description Default <code>*values</code> <code>T</code> <p>Values supplied to this operation in encounter order.</p> <code>()</code> <p>Returns:</p> Type Description <code>Flow[T]</code> <p>A new lazy <code>Flow</code> representing this operation.</p>"},{"location":"reference/flow/#fpstreams.Flow.map_first","title":"map_first","text":"<pre><code>map_first(function: Callable[[T], T]) -&gt; Flow[T]\n</code></pre> <p>Transform only the first item, if one exists.</p> <p>Parameters:</p> Name Type Description Default <code>function</code> <code>Callable[[T], T]</code> <p>The callable applied by this operation.</p> required <p>Returns:</p> Type Description <code>Flow[T]</code> <p>A new lazy <code>Flow</code> representing this operation.</p>"},{"location":"reference/flow/#fpstreams.Flow.map_last","title":"map_last","text":"<pre><code>map_last(function: Callable[[T], T]) -&gt; Flow[T]\n</code></pre> <p>Transform only the last item, if one exists.</p> <p>Parameters:</p> Name Type Description Default <code>function</code> <code>Callable[[T], T]</code> <p>The callable applied by this operation.</p> required <p>Returns:</p> Type Description <code>Flow[T]</code> <p>A new lazy <code>Flow</code> representing this operation.</p>"},{"location":"reference/flow/#fpstreams.Flow.collapse","title":"collapse","text":"<pre><code>collapse(\n    collapsible: Callable[[T, T], bool],\n    merger: Callable[[T, T], T],\n) -&gt; Flow[T]\n</code></pre> <p>Merge adjacent items while collapsible returns true.</p> <p>Parameters:</p> Name Type Description Default <code>collapsible</code> <code>Callable[[T, T], bool]</code> <p>A callable deciding whether two adjacent items should be combined.</p> required <code>merger</code> <code>Callable[[T, T], T]</code> <p>A callable that merges two downstream results.</p> required <p>Returns:</p> Type Description <code>Flow[T]</code> <p>A new lazy <code>Flow</code> representing this operation.</p>"},{"location":"reference/flow/#fpstreams.Flow.attempt","title":"attempt","text":"<pre><code>attempt(function: Callable[[T], R]) -&gt; Flow[Result[R]]\n</code></pre> <p>Map each item and wrap success or failure in a Result.</p> <p>Parameters:</p> Name Type Description Default <code>function</code> <code>Callable[[T], R]</code> <p>The callable applied by this operation.</p> required <p>Returns:</p> Type Description <code>Flow[Result[R]]</code> <p>A new lazy <code>Flow</code> representing this operation.</p>"},{"location":"reference/flow/#fpstreams.Flow.with_engine","title":"with_engine","text":"<pre><code>with_engine(engine: Engine) -&gt; Flow[T]\n</code></pre> <p>Request automatic, Python, or native execution for this plan.</p> <p>Selecting an engine changes execution policy without consuming the source.</p> <p>Parameters:</p> Name Type Description Default <code>engine</code> <code>Engine</code> <p>The execution engine requested for this pipeline.</p> required <p>Returns:</p> Type Description <code>Flow[T]</code> <p>A new lazy <code>Flow</code> representing this operation.</p>"},{"location":"reference/flow/#fpstreams.Flow.explain","title":"explain","text":"<pre><code>explain(\n    terminal: TerminalName = \"iterate\",\n) -&gt; PlanExplanation\n</code></pre> <p>Describe engine selection, stages, and fused operations without executing.</p> <p>This inspection method does not consume or execute the source.</p> <p>Returns:</p> Type Description <code>PlanExplanation</code> <p>A structured explanation of the selected engine and planned stages.</p>"},{"location":"reference/flow/#fpstreams.Flow.pairs","title":"pairs","text":"<pre><code>pairs() -&gt; Any\n</code></pre> <p>View a flow of two-tuples as a key/value Pairs pipeline.</p> <p>Returns:</p> Type Description <code>Any</code> <p>A lazy <code>Pairs</code> view over this flow.</p>"},{"location":"reference/flow/#fpstreams.Flow.take","title":"take","text":"<pre><code>take(count: int) -&gt; Flow[T]\n</code></pre> <p>Emit at most count items, then close the upstream iterator.</p> <p>Parameters:</p> Name Type Description Default <code>count</code> <code>int</code> <p>The requested number of items.</p> required <p>Returns:</p> Type Description <code>Flow[T]</code> <p>A new lazy <code>Flow</code> representing this operation.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If count is negative.</p>"},{"location":"reference/flow/#fpstreams.Flow.limit","title":"limit","text":"<pre><code>limit(count: int) -&gt; Flow[T]\n</code></pre> <p>Emit at most count items; alias of take.</p> <p>Parameters:</p> Name Type Description Default <code>count</code> <code>int</code> <p>The requested number of items.</p> required <p>Returns:</p> Type Description <code>Flow[T]</code> <p>A new lazy <code>Flow</code> representing this operation.</p>"},{"location":"reference/flow/#fpstreams.Flow.drop","title":"drop","text":"<pre><code>drop(count: int) -&gt; Flow[T]\n</code></pre> <p>Skip count items before yielding the remainder.</p> <p>Parameters:</p> Name Type Description Default <code>count</code> <code>int</code> <p>The requested number of items.</p> required <p>Returns:</p> Type Description <code>Flow[T]</code> <p>A new lazy <code>Flow</code> representing this operation.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If count is negative.</p>"},{"location":"reference/flow/#fpstreams.Flow.skip","title":"skip","text":"<pre><code>skip(count: int) -&gt; Flow[T]\n</code></pre> <p>Skip count items; alias of drop.</p> <p>Parameters:</p> Name Type Description Default <code>count</code> <code>int</code> <p>The requested number of items.</p> required <p>Returns:</p> Type Description <code>Flow[T]</code> <p>A new lazy <code>Flow</code> representing this operation.</p>"},{"location":"reference/flow/#fpstreams.Flow.take_while","title":"take_while","text":"<pre><code>take_while(predicate: Callable[[T], bool]) -&gt; Flow[T]\n</code></pre> <p>Emit the longest prefix that satisfies predicate.</p> <p>Parameters:</p> Name Type Description Default <code>predicate</code> <code>Callable[[T], bool]</code> <p>A callable that decides whether an item matches.</p> required <p>Returns:</p> Type Description <code>Flow[T]</code> <p>A new lazy <code>Flow</code> representing this operation.</p>"},{"location":"reference/flow/#fpstreams.Flow.take_while_inclusive","title":"take_while_inclusive","text":"<pre><code>take_while_inclusive(\n    predicate: Callable[[T], bool],\n) -&gt; Flow[T]\n</code></pre> <p>Emit through the first item that fails predicate.</p> <p>Parameters:</p> Name Type Description Default <code>predicate</code> <code>Callable[[T], bool]</code> <p>A callable that decides whether an item matches.</p> required <p>Returns:</p> Type Description <code>Flow[T]</code> <p>A new lazy <code>Flow</code> representing this operation.</p>"},{"location":"reference/flow/#fpstreams.Flow.drop_while","title":"drop_while","text":"<pre><code>drop_while(predicate: Callable[[T], bool]) -&gt; Flow[T]\n</code></pre> <p>Skip the longest prefix that satisfies predicate.</p> <p>Parameters:</p> Name Type Description Default <code>predicate</code> <code>Callable[[T], bool]</code> <p>A callable that decides whether an item matches.</p> required <p>Returns:</p> Type Description <code>Flow[T]</code> <p>A new lazy <code>Flow</code> representing this operation.</p>"},{"location":"reference/functional/","title":"Functional utilities","text":"<p><code>pipe</code>, <code>curry</code>, and <code>retry</code> are small helpers for function composition, partial application, and retrying asynchronous work.</p>"},{"location":"reference/functional/#fpstreams.functional","title":"fpstreams.functional","text":"<p>Small functional helpers that are independent of pipeline execution.</p>"},{"location":"reference/functional/#fpstreams.functional.pipe","title":"pipe","text":"<pre><code>pipe(value: T, *functions: Callable[[Any], Any]) -&gt; Any\n</code></pre> <p>Pass value through functions from left to right.</p> <p>The first callable receives <code>value</code>; each later callable receives the previous result.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>T</code> <p>The value consumed by this operation.</p> required <code>*functions</code> <code>Callable[[Any], Any]</code> <p>Callables applied from left to right.</p> <code>()</code> <p>Returns:</p> Type Description <code>Any</code> <p>The result returned by the final callable, or <code>value</code> when no callables are supplied.</p>"},{"location":"reference/functional/#fpstreams.functional.curry","title":"curry","text":"<pre><code>curry(function: Callable[..., T]) -&gt; Callable[..., Any]\n</code></pre> <p>Return a callable that accepts the original arguments in stages.</p> <p>Required parameters are detected from <code>inspect.signature</code>, so defaults, builtins, and callable objects are supported.</p> <p>Parameters:</p> Name Type Description Default <code>function</code> <code>Callable[..., T]</code> <p>The callable applied by this operation.</p> required <p>Returns:</p> Type Description <code>Callable[..., Any]</code> <p>A callable implementing the described behavior.</p>"},{"location":"reference/functional/#fpstreams.functional.retry","title":"retry","text":"<pre><code>retry(\n    attempts: int = 3,\n    backoff: float = 2.0,\n    jitter: bool = True,\n    exceptions: tuple[type[Exception], ...] = (Exception,),\n    *,\n    delay: float = 0.0,\n) -&gt; Callable[\n    [Callable[P, Awaitable[T]]], Callable[P, Awaitable[T]]\n]\n</code></pre> <p>Retry an async function with configurable delay and backoff.</p> <p>Only the configured exception types are retried; other exceptions propagate immediately.</p> <p>Parameters:</p> Name Type Description Default <code>attempts</code> <code>int</code> <p>The maximum number of calls, including the first attempt.</p> <code>3</code> <code>backoff</code> <code>float</code> <p>The multiplier applied to the delay after each failed attempt.</p> <code>2.0</code> <code>jitter</code> <code>bool</code> <p>Random delay added to each retry interval.</p> <code>True</code> <code>exceptions</code> <code>tuple[type[Exception], ...]</code> <p>The exception type or tuple of types that should trigger a retry.</p> <code>(Exception,)</code> <code>delay</code> <code>float</code> <p>The initial delay in seconds before retrying.</p> <code>0.0</code> <p>Returns:</p> Type Description <code>Callable[[Callable[P, Awaitable[T]]], Callable[P, Awaitable[T]]]</code> <p>A callable implementing the described behavior.</p>"},{"location":"reference/gatherers/","title":"Gatherers","text":"<p>A <code>Gatherer</code> is a stateful intermediate operation. Unlike a <code>Collector</code>, it can emit zero or more values while the rest of the pipeline is still running.</p> <p>Use the regular constructor for the iterable-returning interface:</p> <pre><code>from fpstreams import Gatherer, flow\n\npairs = Gatherer(\n    initializer=lambda: None,\n    integrator=lambda _state, value: (value, -value),\n)\n\nassert flow([1, 2]).gather(pairs).to_list() == [1, -1, 2, -2]\n</code></pre> <p><code>Gatherer.of()</code> uses a push interface. Its integrator receives the current state, the input item, and a <code>Downstream</code>. Return <code>False</code> to stop reading the source. <code>Gatherer.of_sequential()</code> declares that its state cannot be combined in parallel. <code>and_then()</code> composes two gatherers without collecting the intermediate output.</p>"},{"location":"reference/gatherers/#gatherer","title":"Gatherer","text":""},{"location":"reference/gatherers/#fpstreams.Gatherer","title":"fpstreams.Gatherer  <code>dataclass</code>","text":"<p>               Bases: <code>Generic[T, S, R]</code></p> <p>A stateful intermediate operation that can emit zero or more values.</p>"},{"location":"reference/gatherers/#fpstreams.Gatherer.of","title":"of  <code>classmethod</code>","text":"<pre><code>of(\n    initializer_or_integrator: PushIntegrator[None, T, R],\n    *,\n    finisher: PushFinisher[None, R] | None = None,\n    combiner: Combiner[None] | None = None,\n    greedy: bool = False,\n) -&gt; Gatherer[T, None, R]\n</code></pre><pre><code>of(\n    initializer_or_integrator: Callable[[], S],\n    integrator: PushIntegrator[S, T, R],\n    *,\n    finisher: PushFinisher[S, R] | None = None,\n    combiner: Combiner[S] | None = None,\n    greedy: bool = False,\n) -&gt; Gatherer[T, S, R]\n</code></pre> <pre><code>of(\n    initializer_or_integrator: Callable[..., Any],\n    integrator: Callable[..., Any] | None = None,\n    *,\n    finisher: Callable[..., Any] | None = None,\n    combiner: Callable[..., Any] | None = None,\n    greedy: bool = False,\n) -&gt; Gatherer[Any, Any, Any]\n</code></pre> <p>Build a push-based gatherer with optional state, finishing, and combining.</p> <p>Parameters:</p> Name Type Description Default <code>initializer_or_integrator</code> <code>Callable[..., Any]</code> <p>A state initializer, or the integrator when no initializer is required.</p> required <code>integrator</code> <code>Callable[..., Any] | None</code> <p>A callable that consumes one item and may emit downstream values.</p> <code>None</code> <code>finisher</code> <code>Callable[..., Any] | None</code> <p>A callable that converts accumulated state into the final result.</p> <code>None</code> <code>combiner</code> <code>Callable[..., Any] | None</code> <p>A callable that combines two partial gatherer states.</p> <code>None</code> <code>greedy</code> <code>bool</code> <p>Whether the integrator should continue after downstream rejection.</p> <code>False</code> <p>Returns:</p> Type Description <code>Gatherer[Any, Any, Any]</code> <p>A reusable <code>Gatherer</code> implementing the described stateful operation.</p>"},{"location":"reference/gatherers/#fpstreams.Gatherer.of_sequential","title":"of_sequential  <code>classmethod</code>","text":"<pre><code>of_sequential(\n    initializer_or_integrator: PushIntegrator[None, T, R],\n    *,\n    finisher: PushFinisher[None, R] | None = None,\n    greedy: bool = False,\n) -&gt; Gatherer[T, None, R]\n</code></pre><pre><code>of_sequential(\n    initializer_or_integrator: Callable[[], S],\n    integrator: PushIntegrator[S, T, R],\n    *,\n    finisher: PushFinisher[S, R] | None = None,\n    greedy: bool = False,\n) -&gt; Gatherer[T, S, R]\n</code></pre> <pre><code>of_sequential(\n    initializer_or_integrator: Callable[..., Any],\n    integrator: Callable[..., Any] | None = None,\n    *,\n    finisher: Callable[..., Any] | None = None,\n    greedy: bool = False,\n) -&gt; Gatherer[Any, Any, Any]\n</code></pre> <p>Build a push-based gatherer whose state cannot be combined in parallel.</p> <p>Parameters:</p> Name Type Description Default <code>initializer_or_integrator</code> <code>Callable[..., Any]</code> <p>A state initializer, or the integrator when no initializer is required.</p> required <code>integrator</code> <code>Callable[..., Any] | None</code> <p>A callable that consumes one item and may emit downstream values.</p> <code>None</code> <code>finisher</code> <code>Callable[..., Any] | None</code> <p>A callable that converts accumulated state into the final result.</p> <code>None</code> <code>greedy</code> <code>bool</code> <p>Whether the integrator should continue after downstream rejection.</p> <code>False</code> <p>Returns:</p> Type Description <code>Gatherer[Any, Any, Any]</code> <p>A reusable <code>Gatherer</code> implementing the described stateful operation.</p>"},{"location":"reference/gatherers/#fpstreams.Gatherer.and_then","title":"and_then","text":"<pre><code>and_then(\n    other: Gatherer[R, Any, RR],\n) -&gt; Gatherer[T, Any, RR]\n</code></pre> <p>Compose this gatherer with another gatherer without an intermediate collection.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>Gatherer[R, Any, RR]</code> <p>The other iterable, flow, value, or fallback used by the operation.</p> required <p>Returns:</p> Type Description <code>Gatherer[T, Any, RR]</code> <p>A reusable <code>Gatherer</code> implementing the described stateful operation.</p>"},{"location":"reference/gatherers/#downstream","title":"Downstream","text":"<p><code>Downstream.push(value)</code> emits a value and reports whether downstream still wants more. Check <code>is_rejecting()</code> before expensive work when downstream may have short-circuited.</p>"},{"location":"reference/gatherers/#fpstreams.Downstream","title":"fpstreams.Downstream","text":"<p>               Bases: <code>Generic[R]</code></p> <p>A short-circuit-aware output channel owned by a Gatherer.</p>"},{"location":"reference/gatherers/#fpstreams.Downstream.push","title":"push","text":"<pre><code>push(value: R) -&gt; bool\n</code></pre> <p>Emit value and return whether the downstream accepts more output.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>R</code> <p>The value consumed by this operation.</p> required <p>Returns:</p> Type Description <code>bool</code> <p>Whether the condition described above is true.</p>"},{"location":"reference/gatherers/#fpstreams.Downstream.is_rejecting","title":"is_rejecting","text":"<pre><code>is_rejecting() -&gt; bool\n</code></pre> <p>Return whether the downstream has stopped accepting output.</p> <p>Returns:</p> Type Description <code>bool</code> <p>Whether the condition described above is true.</p>"},{"location":"reference/pairs/","title":"Pairs","text":"<p><code>Pairs[K, V]</code> is a key/value view over a lazy flow of two-tuples. It provides key-, value-, and pair-aware transforms plus per-key collection and aggregation.</p>"},{"location":"reference/pairs/#fpstreams.Pairs","title":"fpstreams.Pairs","text":"<p>               Bases: <code>Generic[K, V]</code></p> <p>A lazy key-value view backed by a synchronous Flow.</p>"},{"location":"reference/pairs/#fpstreams.Pairs.to_flow","title":"to_flow","text":"<pre><code>to_flow() -&gt; Flow[tuple[K, V]]\n</code></pre> <p>Return the underlying flow of key/value tuples.</p> <p>Returns:</p> Type Description <code>Flow[tuple[K, V]]</code> <p>A new lazy <code>Flow</code> representing this operation.</p>"},{"location":"reference/pairs/#fpstreams.Pairs.keys","title":"keys","text":"<pre><code>keys() -&gt; Flow[K]\n</code></pre> <p>Select only keys as a Flow.</p> <p>Returns:</p> Type Description <code>Flow[K]</code> <p>A new lazy <code>Flow</code> representing this operation.</p>"},{"location":"reference/pairs/#fpstreams.Pairs.values","title":"values","text":"<pre><code>values() -&gt; Flow[V]\n</code></pre> <p>Select only values as a Flow.</p> <p>Returns:</p> Type Description <code>Flow[V]</code> <p>A new lazy <code>Flow</code> representing this operation.</p>"},{"location":"reference/pairs/#fpstreams.Pairs.map_pairs","title":"map_pairs","text":"<pre><code>map_pairs(\n    function: Callable[[K, V], tuple[R, U]],\n) -&gt; Pairs[R, U]\n</code></pre> <p>Transform each key/value pair into a new pair.</p> <p>Parameters:</p> Name Type Description Default <code>function</code> <code>Callable[[K, V], tuple[R, U]]</code> <p>The callable applied by this operation.</p> required <p>Returns:</p> Type Description <code>Pairs[R, U]</code> <p>A new lazy <code>Pairs</code> pipeline representing this operation.</p>"},{"location":"reference/pairs/#fpstreams.Pairs.flat_map_pairs","title":"flat_map_pairs","text":"<pre><code>flat_map_pairs(\n    function: Callable[[K, V], Iterable[tuple[R, U]]],\n) -&gt; Pairs[R, U]\n</code></pre> <p>Transform each pair into zero or more pairs.</p> <p>Parameters:</p> Name Type Description Default <code>function</code> <code>Callable[[K, V], Iterable[tuple[R, U]]]</code> <p>The callable applied by this operation.</p> required <p>Returns:</p> Type Description <code>Pairs[R, U]</code> <p>A new lazy <code>Pairs</code> pipeline representing this operation.</p>"},{"location":"reference/pairs/#fpstreams.Pairs.filter_pairs","title":"filter_pairs","text":"<pre><code>filter_pairs(\n    predicate: Callable[[K, V], bool],\n) -&gt; Pairs[K, V]\n</code></pre> <p>Keep pairs for which predicate returns true.</p> <p>Parameters:</p> Name Type Description Default <code>predicate</code> <code>Callable[[K, V], bool]</code> <p>A callable that decides whether an item matches.</p> required <p>Returns:</p> Type Description <code>Pairs[K, V]</code> <p>A new lazy <code>Pairs</code> pipeline representing this operation.</p>"},{"location":"reference/pairs/#fpstreams.Pairs.tap","title":"tap","text":"<pre><code>tap(action: Callable[[K, V], object]) -&gt; Pairs[K, V]\n</code></pre> <p>Run a side effect for each pair while passing the pair through.</p> <p>Parameters:</p> Name Type Description Default <code>action</code> <code>Callable[[K, V], object]</code> <p>The side-effecting callable invoked for each matching item.</p> required <p>Returns:</p> Type Description <code>Pairs[K, V]</code> <p>A new lazy <code>Pairs</code> pipeline representing this operation.</p>"},{"location":"reference/pairs/#fpstreams.Pairs.take","title":"take","text":"<pre><code>take(count: int) -&gt; Pairs[K, V]\n</code></pre> <p>Emit at most count pairs.</p> <p>Parameters:</p> Name Type Description Default <code>count</code> <code>int</code> <p>The requested number of items.</p> required <p>Returns:</p> Type Description <code>Pairs[K, V]</code> <p>A new lazy <code>Pairs</code> pipeline representing this operation.</p>"},{"location":"reference/pairs/#fpstreams.Pairs.drop","title":"drop","text":"<pre><code>drop(count: int) -&gt; Pairs[K, V]\n</code></pre> <p>Skip count pairs before yielding the remainder.</p> <p>Parameters:</p> Name Type Description Default <code>count</code> <code>int</code> <p>The requested number of items.</p> required <p>Returns:</p> Type Description <code>Pairs[K, V]</code> <p>A new lazy <code>Pairs</code> pipeline representing this operation.</p>"},{"location":"reference/pairs/#fpstreams.Pairs.sort_by_key","title":"sort_by_key","text":"<pre><code>sort_by_key(*, reverse: bool = False) -&gt; Pairs[K, V]\n</code></pre> <p>Sort pairs by key.</p> <p>Parameters:</p> Name Type Description Default <code>reverse</code> <code>bool</code> <p>If true, produce values in descending order.</p> <code>False</code> <p>Returns:</p> Type Description <code>Pairs[K, V]</code> <p>A new lazy <code>Pairs</code> pipeline representing this operation.</p>"},{"location":"reference/pairs/#fpstreams.Pairs.sort_by_value","title":"sort_by_value","text":"<pre><code>sort_by_value(*, reverse: bool = False) -&gt; Pairs[K, V]\n</code></pre> <p>Sort pairs by value.</p> <p>Parameters:</p> Name Type Description Default <code>reverse</code> <code>bool</code> <p>If true, produce values in descending order.</p> <code>False</code> <p>Returns:</p> Type Description <code>Pairs[K, V]</code> <p>A new lazy <code>Pairs</code> pipeline representing this operation.</p>"},{"location":"reference/pairs/#fpstreams.Pairs.unique_keys","title":"unique_keys","text":"<pre><code>unique_keys() -&gt; Pairs[K, V]\n</code></pre> <p>Keep the first pair for each key.</p> <p>Returns:</p> Type Description <code>Pairs[K, V]</code> <p>A new lazy <code>Pairs</code> pipeline representing this operation.</p>"},{"location":"reference/pairs/#fpstreams.Pairs.map_keys","title":"map_keys","text":"<pre><code>map_keys(function: Callable[[K], R]) -&gt; Pairs[R, V]\n</code></pre> <p>Transform keys while leaving values unchanged.</p> <p>Parameters:</p> Name Type Description Default <code>function</code> <code>Callable[[K], R]</code> <p>The callable applied by this operation.</p> required <p>Returns:</p> Type Description <code>Pairs[R, V]</code> <p>A new lazy <code>Pairs</code> pipeline representing this operation.</p>"},{"location":"reference/pairs/#fpstreams.Pairs.map_values","title":"map_values","text":"<pre><code>map_values(function: Callable[[V], R]) -&gt; Pairs[K, R]\n</code></pre> <p>Transform values while leaving keys unchanged.</p> <p>Parameters:</p> Name Type Description Default <code>function</code> <code>Callable[[V], R]</code> <p>The callable applied by this operation.</p> required <p>Returns:</p> Type Description <code>Pairs[K, R]</code> <p>A new lazy <code>Pairs</code> pipeline representing this operation.</p>"},{"location":"reference/pairs/#fpstreams.Pairs.filter_keys","title":"filter_keys","text":"<pre><code>filter_keys(predicate: Callable[[K], bool]) -&gt; Pairs[K, V]\n</code></pre> <p>Keep pairs whose key satisfies predicate.</p> <p>Parameters:</p> Name Type Description Default <code>predicate</code> <code>Callable[[K], bool]</code> <p>A callable that decides whether an item matches.</p> required <p>Returns:</p> Type Description <code>Pairs[K, V]</code> <p>A new lazy <code>Pairs</code> pipeline representing this operation.</p>"},{"location":"reference/pairs/#fpstreams.Pairs.filter_values","title":"filter_values","text":"<pre><code>filter_values(\n    predicate: Callable[[V], bool],\n) -&gt; Pairs[K, V]\n</code></pre> <p>Keep pairs whose value satisfies predicate.</p> <p>Parameters:</p> Name Type Description Default <code>predicate</code> <code>Callable[[V], bool]</code> <p>A callable that decides whether an item matches.</p> required <p>Returns:</p> Type Description <code>Pairs[K, V]</code> <p>A new lazy <code>Pairs</code> pipeline representing this operation.</p>"},{"location":"reference/pairs/#fpstreams.Pairs.invert","title":"invert","text":"<pre><code>invert() -&gt; Pairs[V, K]\n</code></pre> <p>Swap the key and value in every pair.</p> <p>Returns:</p> Type Description <code>Pairs[V, K]</code> <p>A new lazy <code>Pairs</code> pipeline representing this operation.</p>"},{"location":"reference/pairs/#fpstreams.Pairs.to_dict","title":"to_dict","text":"<pre><code>to_dict(\n    *,\n    on_duplicate: Literal[\n        \"error\", \"first\", \"last\"\n    ] = \"error\",\n) -&gt; dict[K, V]\n</code></pre> <p>Collect pairs into a dictionary using the duplicate-key policy.</p> <p>Parameters:</p> Name Type Description Default <code>on_duplicate</code> <code>Literal['error', 'first', 'last']</code> <p>The policy used when the same key appears more than once.</p> <code>'error'</code> <p>Returns:</p> Type Description <code>dict[K, V]</code> <p>A dictionary containing the computed keys and values.</p>"},{"location":"reference/pairs/#fpstreams.Pairs.group_values","title":"group_values","text":"<pre><code>group_values() -&gt; dict[K, list[V]]\n</code></pre> <p>Collect all values for each key in encounter order.</p> <p>Returns:</p> Type Description <code>dict[K, list[V]]</code> <p>A dictionary containing the computed keys and values.</p>"},{"location":"reference/pairs/#fpstreams.Pairs.collect_values","title":"collect_values","text":"<pre><code>collect_values(\n    collector: Collector[V, Any, R]\n    | Callable[[Iterable[V]], R],\n) -&gt; dict[K, R]\n</code></pre> <p>Run one Collector or callable independently for every key.</p> <p>Parameters:</p> Name Type Description Default <code>collector</code> <code>Collector[V, Any, R] | Callable[[Iterable[V]], R]</code> <p>The collector used to reduce input items.</p> required <p>Returns:</p> Type Description <code>dict[K, R]</code> <p>A dictionary containing the computed keys and values.</p>"},{"location":"reference/pairs/#fpstreams.Pairs.aggregate_values","title":"aggregate_values","text":"<pre><code>aggregate_values(\n    **aggregations: Aggregator,\n) -&gt; dict[K, dict[str, Any]]\n</code></pre> <p>Run named Aggregators independently for every key.</p> <p>Parameters:</p> Name Type Description Default <code>**aggregations</code> <code>Aggregator</code> <p>Named aggregators evaluated during the same traversal.</p> <code>{}</code> <p>Returns:</p> Type Description <code>dict[K, dict[str, Any]]</code> <p>A dictionary containing the computed keys and values.</p>"},{"location":"reference/rows/","title":"Rows","text":"<p><code>Rows[T]</code> adds record selectors, expressions, joins, grouping, reshape operations, and data-system adapters to a lazy flow.</p> <p>Create a pipeline with <code>rows(source)</code> or one of the adapter factories.</p>"},{"location":"reference/rows/#creating-rows","title":"Creating rows","text":"Call Source <code>rows(source)</code> Records, a <code>Flow</code>, pandas, or Polars data <code>rows.from_csv(path)</code> CSV rows <code>rows.from_jsonl(path)</code> JSON objects stored one per line <code>rows.from_arrow(source)</code> Arrow tables, batches, readers, datasets, or streams <code>rows.from_dataframe(frame)</code> The dataframe interchange protocol <code>rows.from_polars(frame)</code> A Polars <code>DataFrame</code> or <code>LazyFrame</code> <code>rows.from_parquet(source)</code> Parquet data with optional projection and filtering <code>rows.from_db(connect, query)</code> A DB-API query using a connection factory <code>rows.from_sqlite(database, query)</code> A SQLite query <p>Adapters open files and connections only when the pipeline executes. Their method docstrings list supported options and return types.</p>"},{"location":"reference/rows/#bounded-jsonl-and-spreadsheet-safe-csv","title":"Bounded JSONL and spreadsheet-safe CSV","text":"<p><code>rows.from_jsonl()</code> limits each physical line to 8 MiB by default. An oversized record raises <code>BufferLimitError</code> before text decoding or JSON parsing. Adjust the limit with <code>max_record_bytes</code>; use <code>None</code> only when unlimited records from a trusted local file are intentional.</p> <p>CSV writers preserve raw cells by default. Use <code>to_csv(..., spreadsheet_safe=True)</code> for untrusted text that will be opened in a spreadsheet. A string whose first non-whitespace character is <code>=</code>, <code>+</code>, <code>-</code>, or <code>@</code> receives a leading single quote. Non-string values are unchanged. CSV and JSON/JSONL writers return <code>None</code>.</p>"},{"location":"reference/rows/#safer-joins","title":"Safer joins","text":"<p>A duplicate lookup key can multiply records without warning. Declare the relationship you expect when duplicates would indicate bad data:</p> <pre><code>customers = [\n    {\"customer_id\": 1, \"name\": \"Ada\"},\n    {\"customer_id\": 2, \"name\": \"Lin\"},\n]\n\nenriched = (\n    rows(orders)\n    .join(\n        customers,\n        on=\"customer_id\",\n        how=\"left\",\n        validate=\"m:1\",\n    )\n    .to_list()\n)\n</code></pre> <p>This many-to-one join permits several orders per customer and requires unique customer keys. Use <code>1:m</code> for a unique left side, <code>1:1</code> when both sides must be unique, or <code>m:m</code> to permit duplicates on both sides. Partitioned joins use the same checks, and errors name the duplicate side and key.</p> <p>Partitioning is governed by <code>SpillLimits</code>. Its defaults are:</p> Limit Default Rows in one loaded partition 100,000 Serialized bytes in one loaded partition 64 MiB Matches for one join key 100,000 Total output rows 1,000,000 Recursive repartition levels 3 <p>Oversized partitions are repartitioned with a new deterministic salt. If skew still exceeds the configured depth\u2014or match, group-state, or output expansion exceeds another limit\u2014the operation raises <code>BufferLimitError</code> before loading an unbounded bucket. Temporary files are removed. Pass a custom <code>SpillLimits</code> to <code>join(..., partitions=..., limits=...)</code> or <code>group_by(...).spill(limits=...)</code>.</p> <p>This is bounded processing up to configured limits, not guaranteed completion for every skewed or many-to-many input.</p>"},{"location":"reference/rows/#methods","title":"Methods","text":""},{"location":"reference/rows/#fpstreams.Rows","title":"fpstreams.Rows","text":"<p>               Bases: <code>RowsIOMixin[T]</code>, <code>Generic[T]</code></p> <p>A lazy record pipeline with joins, grouping, and data-system adapters.</p>"},{"location":"reference/rows/#fpstreams.Rows.to_columns","title":"to_columns","text":"<pre><code>to_columns() -&gt; dict[str, list[Any]]\n</code></pre> <p>Collect records into a dictionary of column lists.</p> <p>Returns:</p> Type Description <code>dict[str, list[Any]]</code> <p>A dictionary containing the computed keys and values.</p>"},{"location":"reference/rows/#fpstreams.Rows.to_pandas","title":"to_pandas","text":"<pre><code>to_pandas(\n    *, batch_size: int = 65536, schema: Any = None\n) -&gt; Any\n</code></pre> <p>Collect rows into a pandas DataFrame through Arrow.</p> <p>Parameters:</p> Name Type Description Default <code>batch_size</code> <code>int</code> <p>The maximum number of rows processed in each batch.</p> <code>65536</code> <code>schema</code> <code>Any</code> <p>The explicit schema used to interpret or write records.</p> <code>None</code> <p>Returns:</p> Type Description <code>Any</code> <p>A pandas <code>DataFrame</code> containing the rows.</p>"},{"location":"reference/rows/#fpstreams.Rows.arrow_batches","title":"arrow_batches","text":"<pre><code>arrow_batches(\n    *, batch_size: int = 65536, schema: Any = None\n) -&gt; Flow[Any]\n</code></pre> <p>Stream PyArrow RecordBatch objects.</p> <p>Parameters:</p> Name Type Description Default <code>batch_size</code> <code>int</code> <p>The maximum number of rows processed in each batch.</p> <code>65536</code> <code>schema</code> <code>Any</code> <p>The explicit schema used to interpret or write records.</p> <code>None</code> <p>Returns:</p> Type Description <code>Flow[Any]</code> <p>A new lazy <code>Flow</code> representing this operation.</p>"},{"location":"reference/rows/#fpstreams.Rows.to_arrow","title":"to_arrow","text":"<pre><code>to_arrow(\n    *, batch_size: int = 65536, schema: Any = None\n) -&gt; Any\n</code></pre> <p>Collect rows into a PyArrow Table.</p> <p>Parameters:</p> Name Type Description Default <code>batch_size</code> <code>int</code> <p>The maximum number of rows processed in each batch.</p> <code>65536</code> <code>schema</code> <code>Any</code> <p>The explicit schema used to interpret or write records.</p> <code>None</code> <p>Returns:</p> Type Description <code>Any</code> <p>A PyArrow <code>Table</code> containing the rows.</p>"},{"location":"reference/rows/#fpstreams.Rows.polars_batches","title":"polars_batches","text":"<pre><code>polars_batches(\n    *, batch_size: int = 65536, schema: Any = None\n) -&gt; Flow[Any]\n</code></pre> <p>Stream Polars DataFrames converted from Arrow batches.</p> <p>Parameters:</p> Name Type Description Default <code>batch_size</code> <code>int</code> <p>The maximum number of rows processed in each batch.</p> <code>65536</code> <code>schema</code> <code>Any</code> <p>The explicit schema used to interpret or write records.</p> <code>None</code> <p>Returns:</p> Type Description <code>Flow[Any]</code> <p>A new lazy <code>Flow</code> representing this operation.</p>"},{"location":"reference/rows/#fpstreams.Rows.to_polars","title":"to_polars","text":"<pre><code>to_polars(\n    *, batch_size: int = 65536, schema: Any = None\n) -&gt; Any\n</code></pre> <p>Collect rows into a Polars DataFrame.</p> <p>Parameters:</p> Name Type Description Default <code>batch_size</code> <code>int</code> <p>The maximum number of rows processed in each batch.</p> <code>65536</code> <code>schema</code> <code>Any</code> <p>The explicit schema used to interpret or write records.</p> <code>None</code> <p>Returns:</p> Type Description <code>Any</code> <p>A Polars <code>DataFrame</code> containing the rows.</p>"},{"location":"reference/rows/#fpstreams.Rows.to_csv","title":"to_csv","text":"<pre><code>to_csv(\n    path: str | PathLike[str],\n    *,\n    fieldnames: Iterable[str] | None = None,\n    encoding: str = \"utf-8\",\n    include_header: bool = True,\n    extrasaction: Literal[\"raise\", \"ignore\"] = \"raise\",\n    spreadsheet_safe: bool = False,\n) -&gt; None\n</code></pre> <p>Write rows to a CSV file without retaining the full input.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str | PathLike[str]</code> <p>The filesystem path to read from or write to.</p> required <code>fieldnames</code> <code>Iterable[str] | None</code> <p>The CSV field order. Required when it cannot be inferred.</p> <code>None</code> <code>encoding</code> <code>str</code> <p>The text encoding used to open the file.</p> <code>'utf-8'</code> <code>include_header</code> <code>bool</code> <p>Whether to write a CSV header row.</p> <code>True</code> <code>extrasaction</code> <code>Literal['raise', 'ignore']</code> <p>How CSV writing handles fields absent from fieldnames.</p> <code>'raise'</code> <code>spreadsheet_safe</code> <code>bool</code> <p>Whether to neutralize cells that spreadsheet programs may execute.</p> <code>False</code>"},{"location":"reference/rows/#fpstreams.Rows.to_jsonl","title":"to_jsonl","text":"<pre><code>to_jsonl(\n    path: str | PathLike[str],\n    *,\n    encoding: str = \"utf-8\",\n    ensure_ascii: bool = False,\n) -&gt; None\n</code></pre> <p>Write one JSON record per line.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str | PathLike[str]</code> <p>The filesystem path to read from or write to.</p> required <code>encoding</code> <code>str</code> <p>The text encoding used to open the file.</p> <code>'utf-8'</code> <code>ensure_ascii</code> <code>bool</code> <p>Whether JSON output escapes non-ASCII characters.</p> <code>False</code>"},{"location":"reference/rows/#fpstreams.Rows.to_parquet","title":"to_parquet","text":"<pre><code>to_parquet(\n    path: str | PathLike[str],\n    *,\n    if_exists: Literal[\"error\", \"replace\"] = \"error\",\n    batch_size: int = 65536,\n    schema: Any = None,\n    compression: Any = \"zstd\",\n    use_dictionary: Any = True,\n    write_statistics: Any = True,\n    writer_options: Mapping[str, Any] | None = None,\n) -&gt; int\n</code></pre> <p>Write rows to Parquet in bounded Arrow batches.</p> <p>Rows are converted and written in bounded batches rather than materialized as one table.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str | PathLike[str]</code> <p>The filesystem path to read from or write to.</p> required <code>if_exists</code> <code>Literal['error', 'replace']</code> <p>How to handle an existing destination table.</p> <code>'error'</code> <code>batch_size</code> <code>int</code> <p>The maximum number of rows processed in each batch.</p> <code>65536</code> <code>schema</code> <code>Any</code> <p>The explicit schema used to interpret or write records.</p> <code>None</code> <code>compression</code> <code>Any</code> <p>The compression codec requested by the output format.</p> <code>'zstd'</code> <code>use_dictionary</code> <code>Any</code> <p>Whether Parquet writing should use dictionary encoding.</p> <code>True</code> <code>write_statistics</code> <code>Any</code> <p>Whether Parquet metadata should include column statistics.</p> <code>True</code> <code>writer_options</code> <code>Mapping[str, Any] | None</code> <p>Additional options passed to the underlying writer.</p> <code>None</code> <p>Returns:</p> Type Description <code>int</code> <p>The number of rows written.</p>"},{"location":"reference/rows/#fpstreams.Rows.to_db","title":"to_db","text":"<pre><code>to_db(\n    connect: ConnectionFactory,\n    statement: str,\n    *,\n    parameters: Callable[[T], Any] | None = None,\n    batch_size: int = 1000,\n) -&gt; int\n</code></pre> <p>Execute a DB-API statement for rows in batches.</p> <p>Rows are submitted in bounded batches inside one transaction.</p> <p>Parameters:</p> Name Type Description Default <code>connect</code> <code>ConnectionFactory</code> <p>A zero-argument callable that opens a new database connection.</p> required <code>statement</code> <code>str</code> <p>The SQL statement executed for each batch of rows.</p> required <code>parameters</code> <code>Callable[[T], Any] | None</code> <p>Parameters passed to the database query or statement.</p> <code>None</code> <code>batch_size</code> <code>int</code> <p>The maximum number of rows processed in each batch.</p> <code>1000</code> <p>Returns:</p> Type Description <code>int</code> <p>The number of rows submitted to the database.</p>"},{"location":"reference/rows/#fpstreams.Rows.to_sqlite","title":"to_sqlite","text":"<pre><code>to_sqlite(\n    database: str | PathLike[str],\n    table: str,\n    *,\n    if_exists: Literal[\n        \"append\", \"fail\", \"replace\"\n    ] = \"append\",\n    conflict: Literal[\n        \"error\", \"ignore\", \"replace\"\n    ] = \"error\",\n    columns: Iterable[str] | None = None,\n    schema: Mapping[str, str] | None = None,\n    batch_size: int = 1000,\n    timeout: float = 5.0,\n    uri: bool = False,\n) -&gt; int\n</code></pre> <p>Insert rows into a SQLite table in batches.</p> <p>Rows are submitted in bounded batches inside one SQLite transaction.</p> <p>Parameters:</p> Name Type Description Default <code>database</code> <code>str | PathLike[str]</code> <p>The SQLite database path or connection target.</p> required <code>table</code> <code>str</code> <p>The destination table name.</p> required <code>if_exists</code> <code>Literal['append', 'fail', 'replace']</code> <p>How to handle an existing destination table.</p> <code>'append'</code> <code>conflict</code> <code>Literal['error', 'ignore', 'replace']</code> <p>The duplicate-key or name conflict policy.</p> <code>'error'</code> <code>columns</code> <code>Iterable[str] | None</code> <p>The columns or column mapping used by the operation.</p> <code>None</code> <code>schema</code> <code>Mapping[str, str] | None</code> <p>The explicit schema used to interpret or write records.</p> <code>None</code> <code>batch_size</code> <code>int</code> <p>The maximum number of rows processed in each batch.</p> <code>1000</code> <code>timeout</code> <code>float</code> <p>The optional maximum duration in seconds before the operation fails.</p> <code>5.0</code> <code>uri</code> <code>bool</code> <p>Whether the SQLite database string should be interpreted as a URI.</p> <code>False</code> <p>Returns:</p> Type Description <code>int</code> <p>The number of rows written to SQLite.</p>"},{"location":"reference/rows/#fpstreams.Rows.from_csv","title":"from_csv  <code>staticmethod</code>","text":"<pre><code>from_csv(\n    path: str | PathLike[str],\n    *,\n    encoding: str = \"utf-8\",\n    **format_parameters: Any,\n) -&gt; Rows[dict[str, Any]]\n</code></pre> <p>Read CSV rows lazily as dictionaries.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str | PathLike[str]</code> <p>The filesystem path to read from or write to.</p> required <code>encoding</code> <code>str</code> <p>The text encoding used to open the file.</p> <code>'utf-8'</code> <code>**format_parameters</code> <code>Any</code> <p>Additional keyword arguments passed to the underlying file-format reader.</p> <code>{}</code> <p>Returns:</p> Type Description <code>Rows[dict[str, Any]]</code> <p>A new lazy <code>Rows</code> pipeline representing this operation.</p>"},{"location":"reference/rows/#fpstreams.Rows.from_jsonl","title":"from_jsonl  <code>staticmethod</code>","text":"<pre><code>from_jsonl(\n    path: str | PathLike[str],\n    *,\n    encoding: str = \"utf-8\",\n    max_record_bytes: int | None = 8 * 1024 * 1024,\n) -&gt; Rows[dict[str, Any]]\n</code></pre> <p>Read one JSON object per line lazily.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str | PathLike[str]</code> <p>The filesystem path to read from or write to.</p> required <code>encoding</code> <code>str</code> <p>The text encoding used to open the file.</p> <code>'utf-8'</code> <code>max_record_bytes</code> <code>int | None</code> <p>Maximum encoded bytes per physical record, or None to disable.</p> <code>8 * 1024 * 1024</code> <p>Returns:</p> Type Description <code>Rows[dict[str, Any]]</code> <p>A new lazy <code>Rows</code> pipeline representing this operation.</p>"},{"location":"reference/rows/#fpstreams.Rows.from_arrow","title":"from_arrow  <code>staticmethod</code>","text":"<pre><code>from_arrow(\n    source: Any, *, batch_size: int = 65536\n) -&gt; Rows[dict[str, Any]]\n</code></pre> <p>Create rows from an Arrow table, batch, reader, dataset, or stream.</p> <p>Parameters:</p> Name Type Description Default <code>source</code> <code>Any</code> <p>The iterable, async iterable, or data source to read lazily.</p> required <code>batch_size</code> <code>int</code> <p>The maximum number of rows processed in each batch.</p> <code>65536</code> <p>Returns:</p> Type Description <code>Rows[dict[str, Any]]</code> <p>A new lazy <code>Rows</code> pipeline representing this operation.</p>"},{"location":"reference/rows/#fpstreams.Rows.from_dataframe","title":"from_dataframe  <code>staticmethod</code>","text":"<pre><code>from_dataframe(\n    frame: Any,\n    *,\n    batch_size: int = 65536,\n    allow_copy: bool = True,\n) -&gt; Rows[dict[str, Any]]\n</code></pre> <p>Create rows from an object implementing the dataframe interchange protocol.</p> <p>Parameters:</p> Name Type Description Default <code>frame</code> <code>Any</code> <p>The dataframe-like object used as the row source.</p> required <code>batch_size</code> <code>int</code> <p>The maximum number of rows processed in each batch.</p> <code>65536</code> <code>allow_copy</code> <code>bool</code> <p>Whether an adapter may copy data when zero-copy conversion is unavailable.</p> <code>True</code> <p>Returns:</p> Type Description <code>Rows[dict[str, Any]]</code> <p>A new lazy <code>Rows</code> pipeline representing this operation.</p>"},{"location":"reference/rows/#fpstreams.Rows.from_polars","title":"from_polars  <code>staticmethod</code>","text":"<pre><code>from_polars(\n    frame: Any,\n    *,\n    batch_size: int = 65536,\n    maintain_order: bool = True,\n    engine: Any = \"auto\",\n) -&gt; Rows[dict[str, Any]]\n</code></pre> <p>Create rows from a Polars DataFrame or LazyFrame.</p> <p>Parameters:</p> Name Type Description Default <code>frame</code> <code>Any</code> <p>The dataframe-like object used as the row source.</p> required <code>batch_size</code> <code>int</code> <p>The maximum number of rows processed in each batch.</p> <code>65536</code> <code>maintain_order</code> <code>bool</code> <p>Whether output must preserve the source row order.</p> <code>True</code> <code>engine</code> <code>Any</code> <p>The execution engine requested for this pipeline.</p> <code>'auto'</code> <p>Returns:</p> Type Description <code>Rows[dict[str, Any]]</code> <p>A new lazy <code>Rows</code> pipeline representing this operation.</p>"},{"location":"reference/rows/#fpstreams.Rows.from_parquet","title":"from_parquet  <code>staticmethod</code>","text":"<pre><code>from_parquet(\n    source: Any,\n    *,\n    columns: Iterable[str] | None = None,\n    filter: Any = None,\n    batch_size: int = 65536,\n    use_threads: bool = True,\n    filesystem: Any = None,\n    partitioning: Any = None,\n) -&gt; Rows[dict[str, Any]]\n</code></pre> <p>Read Parquet batches lazily through PyArrow.</p> <p>Parameters:</p> Name Type Description Default <code>source</code> <code>Any</code> <p>The iterable, async iterable, or data source to read lazily.</p> required <code>columns</code> <code>Iterable[str] | None</code> <p>The columns or column mapping used by the operation.</p> <code>None</code> <code>filter</code> <code>Any</code> <p>An optional dataset filter pushed into the underlying reader.</p> <code>None</code> <code>batch_size</code> <code>int</code> <p>The maximum number of rows processed in each batch.</p> <code>65536</code> <code>use_threads</code> <code>bool</code> <p>Whether the underlying Arrow operation may use worker threads.</p> <code>True</code> <code>filesystem</code> <code>Any</code> <p>The optional filesystem implementation used to access the data source.</p> <code>None</code> <code>partitioning</code> <code>Any</code> <p>The partitioning scheme used to interpret a dataset.</p> <code>None</code> <p>Returns:</p> Type Description <code>Rows[dict[str, Any]]</code> <p>A new lazy <code>Rows</code> pipeline representing this operation.</p>"},{"location":"reference/rows/#fpstreams.Rows.from_db","title":"from_db  <code>staticmethod</code>","text":"<pre><code>from_db(\n    connect: ConnectionFactory,\n    query: str,\n    parameters: DBParameters = None,\n    *,\n    batch_size: int = 1000,\n) -&gt; Rows[dict[str, Any]]\n</code></pre> <p>Run a DB-API query and stream rows in batches.</p> <p>Parameters:</p> Name Type Description Default <code>connect</code> <code>ConnectionFactory</code> <p>A zero-argument callable that opens a new database connection.</p> required <code>query</code> <code>str</code> <p>The SQL query executed for each fresh iteration.</p> required <code>parameters</code> <code>DBParameters</code> <p>Parameters passed to the database query or statement.</p> <code>None</code> <code>batch_size</code> <code>int</code> <p>The maximum number of rows processed in each batch.</p> <code>1000</code> <p>Returns:</p> Type Description <code>Rows[dict[str, Any]]</code> <p>A new lazy <code>Rows</code> pipeline representing this operation.</p>"},{"location":"reference/rows/#fpstreams.Rows.from_sqlite","title":"from_sqlite  <code>staticmethod</code>","text":"<pre><code>from_sqlite(\n    database: str | PathLike[str],\n    query: str,\n    parameters: DBParameters = None,\n    *,\n    batch_size: int = 1000,\n    timeout: float = 5.0,\n    uri: bool = False,\n) -&gt; Rows[dict[str, Any]]\n</code></pre> <p>Execute a SQLite query lazily and emit rows as dictionaries.</p> <p>Parameters:</p> Name Type Description Default <code>database</code> <code>str | PathLike[str]</code> <p>The SQLite database path or connection target.</p> required <code>query</code> <code>str</code> <p>The SQL query executed for each fresh iteration.</p> required <code>parameters</code> <code>DBParameters</code> <p>Parameters passed to the database query or statement.</p> <code>None</code> <code>batch_size</code> <code>int</code> <p>The maximum number of rows processed in each batch.</p> <code>1000</code> <code>timeout</code> <code>float</code> <p>The optional maximum duration in seconds before the operation fails.</p> <code>5.0</code> <code>uri</code> <code>bool</code> <p>Whether the SQLite database string should be interpreted as a URI.</p> <code>False</code> <p>Returns:</p> Type Description <code>Rows[dict[str, Any]]</code> <p>A new lazy <code>Rows</code> pipeline representing this operation.</p>"},{"location":"reference/rows/#fpstreams.Rows.to_list","title":"to_list","text":"<pre><code>to_list() -&gt; list[T]\n</code></pre> <p>Execute the record pipeline and collect its rows.</p> <p>Returns:</p> Type Description <code>list[T]</code> <p>A list containing the consumed results in encounter order.</p>"},{"location":"reference/rows/#fpstreams.Rows.count","title":"count","text":"<pre><code>count() -&gt; int\n</code></pre> <p>Count all rows produced by the pipeline.</p> <p>Returns:</p> Type Description <code>int</code> <p>The number of matching input items.</p>"},{"location":"reference/rows/#fpstreams.Rows.first","title":"first","text":"<pre><code>first() -&gt; T\n</code></pre> <p>Return the first row, a default, or raise EmptyFlowError.</p> <p>Returns:</p> Type Description <code>T</code> <p>The first row.</p> <p>Raises:</p> Type Description <code>EmptyFlowError</code> <p>If the pipeline is empty and no default is supplied.</p>"},{"location":"reference/rows/#fpstreams.Rows.last","title":"last","text":"<pre><code>last() -&gt; T\n</code></pre> <p>Return the last row, a default, or raise EmptyFlowError.</p> <p>Returns:</p> Type Description <code>T</code> <p>The last row.</p>"},{"location":"reference/rows/#fpstreams.Rows.take","title":"take","text":"<pre><code>take(count: int) -&gt; Rows[T]\n</code></pre> <p>Keep at most count rows.</p> <p>Parameters:</p> Name Type Description Default <code>count</code> <code>int</code> <p>The requested number of items.</p> required <p>Returns:</p> Type Description <code>Rows[T]</code> <p>A new lazy <code>Rows</code> pipeline representing this operation.</p>"},{"location":"reference/rows/#fpstreams.Rows.skip","title":"skip","text":"<pre><code>skip(count: int) -&gt; Rows[T]\n</code></pre> <p>Skip count rows; alias of drop.</p> <p>Parameters:</p> Name Type Description Default <code>count</code> <code>int</code> <p>The requested number of items.</p> required <p>Returns:</p> Type Description <code>Rows[T]</code> <p>A new lazy <code>Rows</code> pipeline representing this operation.</p>"},{"location":"reference/rows/#fpstreams.Rows.unique_by","title":"unique_by","text":"<pre><code>unique_by(selector: Selector) -&gt; Rows[T]\n</code></pre> <p>Keep the first row for each selected key.</p> <p>Parameters:</p> Name Type Description Default <code>selector</code> <code>Selector</code> <p>A callable, field name, index, path, or expression used to select a value.</p> required <p>Returns:</p> Type Description <code>Rows[T]</code> <p>A new lazy <code>Rows</code> pipeline representing this operation.</p>"},{"location":"reference/rows/#fpstreams.Rows.filter","title":"filter","text":"<pre><code>filter(predicate: Callable[[T], bool]) -&gt; Rows[T]\n</code></pre> <p>Keep rows for which predicate returns true.</p> <p>Rows remain lazy; the predicate is evaluated only while the returned pipeline is consumed.</p> <p>Parameters:</p> Name Type Description Default <code>predicate</code> <code>Callable[[T], bool]</code> <p>A callable that decides whether an item matches.</p> required <p>Returns:</p> Type Description <code>Rows[T]</code> <p>A new lazy <code>Rows</code> pipeline representing this operation.</p>"},{"location":"reference/rows/#fpstreams.Rows.where","title":"where","text":"<pre><code>where(\n    predicate: Callable[[T], bool] | None = None,\n    **equalities: Any,\n) -&gt; Rows[T]\n</code></pre> <p>Filter rows with a predicate and/or field equalities.</p> <p>A row must satisfy the optional predicate and every supplied field equality.</p> <p>Parameters:</p> Name Type Description Default <code>predicate</code> <code>Callable[[T], bool] | None</code> <p>A callable that decides whether an item matches.</p> <code>None</code> <code>**equalities</code> <code>Any</code> <p>Field names mapped to values that rows must equal.</p> <code>{}</code> <p>Returns:</p> Type Description <code>Rows[T]</code> <p>A new lazy <code>Rows</code> pipeline representing this operation.</p>"},{"location":"reference/rows/#fpstreams.Rows.with_columns","title":"with_columns","text":"<pre><code>with_columns(**columns: Selector) -&gt; Rows[dict[str, Any]]\n</code></pre> <p>Add or replace fields using selectors and row expressions.</p> <p>Parameters:</p> Name Type Description Default <code>**columns</code> <code>Selector</code> <p>Names mapped to selectors, expressions, or replacement values.</p> <code>{}</code> <p>Returns:</p> Type Description <code>Rows[dict[str, Any]]</code> <p>A new lazy <code>Rows</code> pipeline representing this operation.</p>"},{"location":"reference/rows/#fpstreams.Rows.rename","title":"rename","text":"<pre><code>rename(**columns: str) -&gt; Rows[dict[str, Any]]\n</code></pre> <p>Rename fields and reject duplicate output names.</p> <p>Parameters:</p> Name Type Description Default <code>**columns</code> <code>str</code> <p>Names mapped to selectors, expressions, or replacement values.</p> <code>{}</code> <p>Returns:</p> Type Description <code>Rows[dict[str, Any]]</code> <p>A new lazy <code>Rows</code> pipeline representing this operation.</p>"},{"location":"reference/rows/#fpstreams.Rows.drop","title":"drop","text":"<pre><code>drop(*columns: str) -&gt; Rows[dict[str, Any]]\n</code></pre> <p>Remove the named fields from each row.</p> <p>Parameters:</p> Name Type Description Default <code>*columns</code> <code>str</code> <p>Column names selected by the operation.</p> <code>()</code> <p>Returns:</p> Type Description <code>Rows[dict[str, Any]]</code> <p>A new lazy <code>Rows</code> pipeline representing this operation.</p>"},{"location":"reference/rows/#fpstreams.Rows.cast","title":"cast","text":"<pre><code>cast(\n    **columns: Callable[[Any], Any],\n) -&gt; Rows[dict[str, Any]]\n</code></pre> <p>Convert selected field values with the supplied callables.</p> <p>Parameters:</p> Name Type Description Default <code>**columns</code> <code>Callable[[Any], Any]</code> <p>Names mapped to selectors, expressions, or replacement values.</p> <code>{}</code> <p>Returns:</p> Type Description <code>Rows[dict[str, Any]]</code> <p>A new lazy <code>Rows</code> pipeline representing this operation.</p>"},{"location":"reference/rows/#fpstreams.Rows.fill_nulls","title":"fill_nulls","text":"<pre><code>fill_nulls(**replacements: object) -&gt; Rows[dict[str, Any]]\n</code></pre> <p>Replace None values with constants, selectors, or row expressions.</p> <p>Parameters:</p> Name Type Description Default <code>**replacements</code> <code>object</code> <p>Field names mapped to their new names.</p> <code>{}</code> <p>Returns:</p> Type Description <code>Rows[dict[str, Any]]</code> <p>A new lazy <code>Rows</code> pipeline representing this operation.</p>"},{"location":"reference/rows/#fpstreams.Rows.drop_nulls","title":"drop_nulls","text":"<pre><code>drop_nulls(\n    *selectors: Selector, how: Literal[\"any\", \"all\"] = \"any\"\n) -&gt; Rows[T]\n</code></pre> <p>Drop rows containing None in selected fields.</p> <p>Parameters:</p> Name Type Description Default <code>*selectors</code> <code>Selector</code> <p>Selectors that define projected fields or grouping keys.</p> <code>()</code> <code>how</code> <code>Literal['any', 'all']</code> <p>The join mode or operation policy to apply.</p> <code>'any'</code> <p>Returns:</p> Type Description <code>Rows[T]</code> <p>A new lazy <code>Rows</code> pipeline representing this operation.</p>"},{"location":"reference/rows/#fpstreams.Rows.explode","title":"explode","text":"<pre><code>explode(\n    selector: Selector,\n    *,\n    into: str | None = None,\n    outer: bool = False,\n) -&gt; Rows[dict[str, Any]]\n</code></pre> <p>Emit one row for each element of a selected nested iterable.</p> <p>Parameters:</p> Name Type Description Default <code>selector</code> <code>Selector</code> <p>A callable, field name, index, path, or expression used to select a value.</p> required <code>into</code> <code>str | None</code> <p>A collector or container factory used for the final values.</p> <code>None</code> <code>outer</code> <code>bool</code> <p>Whether unnesting should retain the original nested field.</p> <code>False</code> <p>Returns:</p> Type Description <code>Rows[dict[str, Any]]</code> <p>A new lazy <code>Rows</code> pipeline representing this operation.</p>"},{"location":"reference/rows/#fpstreams.Rows.unnest","title":"unnest","text":"<pre><code>unnest(\n    column: str, *, prefix: str = \"\"\n) -&gt; Rows[dict[str, Any]]\n</code></pre> <p>Expand a nested record into top-level fields.</p> <p>Parameters:</p> Name Type Description Default <code>column</code> <code>str</code> <p>The field whose nested values should be expanded.</p> required <code>prefix</code> <code>str</code> <p>The prefix added to fields expanded from a nested record.</p> <code>''</code> <p>Returns:</p> Type Description <code>Rows[dict[str, Any]]</code> <p>A new lazy <code>Rows</code> pipeline representing this operation.</p>"},{"location":"reference/rows/#fpstreams.Rows.unpivot","title":"unpivot","text":"<pre><code>unpivot(\n    *columns: str,\n    names_to: str = \"variable\",\n    values_to: str = \"value\",\n) -&gt; Rows[dict[str, Any]]\n</code></pre> <p>Convert selected columns from wide form to name/value rows.</p> <p>Parameters:</p> Name Type Description Default <code>*columns</code> <code>str</code> <p>Column names selected by the operation.</p> <code>()</code> <code>names_to</code> <code>str</code> <p>The output field that stores former column names.</p> <code>'variable'</code> <code>values_to</code> <code>str</code> <p>The output field that stores former column values.</p> <code>'value'</code> <p>Returns:</p> Type Description <code>Rows[dict[str, Any]]</code> <p>A new lazy <code>Rows</code> pipeline representing this operation.</p>"},{"location":"reference/rows/#fpstreams.Rows.pivot","title":"pivot","text":"<pre><code>pivot(\n    *,\n    index: Selector | tuple[Selector, ...],\n    columns: Selector,\n    values: Selector,\n    aggregate: str | Callable[[Any, Any], Any] = \"error\",\n    fill: Any = None,\n) -&gt; Rows[dict[str, Any]]\n</code></pre> <p>Convert long-form rows to columns with an explicit duplicate policy.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>Selector | tuple[Selector, ...]</code> <p>The zero-based item or field position to select.</p> required <code>columns</code> <code>Selector</code> <p>The columns or column mapping used by the operation.</p> required <code>values</code> <code>Selector</code> <p>The values consumed by this operation.</p> required <code>aggregate</code> <code>str | Callable[[Any, Any], Any]</code> <p>The aggregator used to combine duplicate pivot cells.</p> <code>'error'</code> <code>fill</code> <code>Any</code> <p>The value used for pivot cells with no matching input row.</p> <code>None</code> <p>Returns:</p> Type Description <code>Rows[dict[str, Any]]</code> <p>A new lazy <code>Rows</code> pipeline representing this operation.</p>"},{"location":"reference/rows/#fpstreams.Rows.select","title":"select","text":"<pre><code>select(\n    *selectors: str | int, **named: Selector\n) -&gt; Rows[dict[str, Any]]\n</code></pre> <p>Project fields and computed selectors into new dictionaries.</p> <p>Parameters:</p> Name Type Description Default <code>*selectors</code> <code>str | int</code> <p>Selectors that define projected fields or grouping keys.</p> <code>()</code> <code>**named</code> <code>Selector</code> <p>Names mapped to collectors or expressions.</p> <code>{}</code> <p>Returns:</p> Type Description <code>Rows[dict[str, Any]]</code> <p>A new lazy <code>Rows</code> pipeline representing this operation.</p>"},{"location":"reference/rows/#fpstreams.Rows.sort_by","title":"sort_by","text":"<pre><code>sort_by(\n    selector: Selector,\n    *,\n    reverse: bool = False,\n    buffer_size: int | None = None,\n    tempdir: str | PathLike[str] | None = None,\n) -&gt; Rows[T]\n</code></pre> <p>Sort rows by a selector, optionally with a bounded buffer.</p> <p>Parameters:</p> Name Type Description Default <code>selector</code> <code>Selector</code> <p>A callable, field name, index, path, or expression used to select a value.</p> required <code>reverse</code> <code>bool</code> <p>If true, produce values in descending order.</p> <code>False</code> <code>buffer_size</code> <code>int | None</code> <p>The maximum number of items held in an in-memory buffer or batch.</p> <code>None</code> <code>tempdir</code> <code>str | PathLike[str] | None</code> <p>The directory used for temporary spill files.</p> <code>None</code> <p>Returns:</p> Type Description <code>Rows[T]</code> <p>A new lazy <code>Rows</code> pipeline representing this operation.</p>"},{"location":"reference/rows/#fpstreams.Rows.external_sort_by","title":"external_sort_by","text":"<pre><code>external_sort_by(\n    selector: Selector,\n    *,\n    reverse: bool = False,\n    buffer_size: int = 100000,\n    tempdir: str | PathLike[str] | None = None,\n) -&gt; Rows[T]\n</code></pre> <p>Sort rows with bounded in-memory runs and temporary files.</p> <p>Sorted runs are written to temporary files and merged lazily, keeping peak in-memory rows bounded.</p> <p>Parameters:</p> Name Type Description Default <code>selector</code> <code>Selector</code> <p>A callable, field name, index, path, or expression used to select a value.</p> required <code>reverse</code> <code>bool</code> <p>If true, produce values in descending order.</p> <code>False</code> <code>buffer_size</code> <code>int</code> <p>The maximum number of items held in an in-memory buffer or batch.</p> <code>100000</code> <code>tempdir</code> <code>str | PathLike[str] | None</code> <p>The directory used for temporary spill files.</p> <code>None</code> <p>Returns:</p> Type Description <code>Rows[T]</code> <p>A new lazy <code>Rows</code> pipeline representing this operation.</p>"},{"location":"reference/rows/#fpstreams.Rows.aggregate","title":"aggregate","text":"<pre><code>aggregate(\n    **aggregations: Aggregator,\n) -&gt; Rows[dict[str, Any]]\n</code></pre> <p>Run named Aggregators and return a one-row pipeline.</p> <p>The computation is deferred and produces a <code>Rows</code> pipeline containing one result record.</p> <p>Parameters:</p> Name Type Description Default <code>**aggregations</code> <code>Aggregator</code> <p>Named aggregators evaluated during the same traversal.</p> <code>{}</code> <p>Returns:</p> Type Description <code>Rows[dict[str, Any]]</code> <p>A lazy one-row pipeline containing the named results.</p>"},{"location":"reference/rows/#fpstreams.Rows.group_by","title":"group_by","text":"<pre><code>group_by(\n    *selectors: Selector, **named: Selector\n) -&gt; GroupedRows[T]\n</code></pre> <p>Create a deferred grouped aggregation by one or more selectors.</p> <p>Parameters:</p> Name Type Description Default <code>*selectors</code> <code>Selector</code> <p>Selectors that define projected fields or grouping keys.</p> <code>()</code> <code>**named</code> <code>Selector</code> <p>Names mapped to collectors or expressions.</p> <code>{}</code> <p>Returns:</p> Type Description <code>GroupedRows[T]</code> <p>A new lazy <code>Rows</code> pipeline representing this operation.</p>"},{"location":"reference/rows/#fpstreams.Rows.join","title":"join","text":"<pre><code>join(\n    other: Iterable[Any] | Rows[Any],\n    *,\n    on: JoinSelector | None = None,\n    left_on: JoinSelector | None = None,\n    right_on: JoinSelector | None = None,\n    how: str = \"inner\",\n    suffix: str = \"_right\",\n    validate: JoinValidation = \"m:m\",\n    partitions: int | None = None,\n    tempdir: str | PathLike[str] | None = None,\n    limits: SpillLimits | None = None,\n) -&gt; Rows[dict[str, Any]]\n</code></pre> <p>Join this record pipeline with another source.</p> <p>Joins are lazy and preserve stable input order. Inner, left, semi, and anti joins stream the left source after indexing the right source. Right and full joins materialize both sides. Set partitions to use bounded-memory hash partitioning through temporary files.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>Iterable[Any] | Rows[Any]</code> <p>The record iterable or Rows pipeline to join.</p> required <code>on</code> <code>JoinSelector | None</code> <p>A selector used for both left and right keys.</p> <code>None</code> <code>left_on</code> <code>JoinSelector | None</code> <p>The left key selector when the two sides use different fields.</p> <code>None</code> <code>right_on</code> <code>JoinSelector | None</code> <p>The right key selector when the two sides use different fields.</p> <code>None</code> <code>how</code> <code>str</code> <p>One of inner, left, right, full, semi, or anti.</p> <code>'inner'</code> <code>suffix</code> <code>str</code> <p>Text appended to conflicting right-side field names.</p> <code>'_right'</code> <code>validate</code> <code>JoinValidation</code> <p>Expected key cardinality. 1:m requires unique left keys, m:1 requires unique right keys, 1:1 requires both, and m:m permits duplicates on both sides.</p> <code>'m:m'</code> <code>partitions</code> <code>int | None</code> <p>Number of hash partitions for bounded-memory execution. Must be between 2 and 256.</p> <code>None</code> <code>tempdir</code> <code>str | PathLike[str] | None</code> <p>Parent directory for temporary partition files. Requires partitions.</p> <code>None</code> <code>limits</code> <code>SpillLimits | None</code> <p>Finite partition, match, and output budgets for spilled execution.</p> <code>None</code> <p>Returns:</p> Type Description <code>Rows[dict[str, Any]]</code> <p>A new lazy Rows pipeline containing the joined records.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If selectors, modes, partition options, or key cardinality are invalid.</p> <code>TypeError</code> <p>If a key is unhashable or spilled data cannot be serialized.</p> <code>DuplicateKeyError</code> <p>If suffixing would create an ambiguous output field.</p> <code>BufferLimitError</code> <p>If spilled execution exceeds a configured resource budget.</p>"},{"location":"reference/rows/#groupedrows","title":"GroupedRows","text":"<p><code>group_by()</code> returns a grouped plan. Call <code>aggregate()</code> directly, or call <code>spill()</code> first to use partitioned temporary storage.</p>"},{"location":"reference/rows/#fpstreams.tabular.GroupedRows","title":"fpstreams.tabular.GroupedRows","text":"<p>               Bases: <code>Generic[T]</code></p> <p>A deferred grouping that chooses in-memory or partitioned aggregation.</p>"},{"location":"reference/rows/#fpstreams.tabular.GroupedRows.spill","title":"spill","text":"<pre><code>spill(\n    partitions: int = 32,\n    *,\n    tempdir: str | PathLike[str] | None = None,\n    limits: SpillLimits | None = None,\n) -&gt; GroupedRows[T]\n</code></pre> <p>Configure partitioned temporary storage for grouped aggregation.</p> <p>Parameters:</p> Name Type Description Default <code>partitions</code> <code>int</code> <p>The number of spill partitions used for bounded-memory processing.</p> <code>32</code> <code>tempdir</code> <code>str | PathLike[str] | None</code> <p>The directory used for temporary spill files.</p> <code>None</code> <code>limits</code> <code>SpillLimits | None</code> <p>Finite partition, state, match, and output budgets.</p> <code>None</code> <p>Returns:</p> Type Description <code>GroupedRows[T]</code> <p>A new lazy <code>Rows</code> pipeline representing this operation.</p>"},{"location":"reference/rows/#fpstreams.tabular.GroupedRows.aggregate","title":"aggregate","text":"<pre><code>aggregate(\n    **aggregations: Aggregator,\n) -&gt; Rows[dict[str, Any]]\n</code></pre> <p>Compute named aggregations independently for each group.</p> <p>Grouping and aggregation remain deferred until the returned <code>Rows</code> pipeline is consumed.</p> <p>Parameters:</p> Name Type Description Default <code>**aggregations</code> <code>Aggregator</code> <p>Named aggregators evaluated during the same traversal.</p> <code>{}</code> <p>Returns:</p> Type Description <code>Rows[dict[str, Any]]</code> <p>A lazy row pipeline containing one aggregate record per group.</p>"}]}